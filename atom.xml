<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>armsword的涅槃之地</title>
  
  <subtitle>莫思身外无穷事，且读生平未见书 || 不为无益之事，何以遣有涯之生</subtitle>
  <link href="http://armsword.com/atom.xml" rel="self"/>
  
  <link href="http://armsword.com/"/>
  <updated>2020-11-03T08:55:43.477Z</updated>
  <id>http://armsword.com/</id>
  
  <author>
    <name>armsword</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Presto 在滴滴的探索和实践</title>
    <link href="http://armsword.com/2020/11/03/presto-in-didichuxing/"/>
    <id>http://armsword.com/2020/11/03/presto-in-didichuxing/</id>
    <published>2020-11-03T07:59:12.000Z</published>
    <updated>2020-11-03T08:55:43.477Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Presto-简介"><a href="#Presto-简介" class="headerlink" title="Presto 简介"></a>Presto 简介</h2><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>Presto是Facebook 开源的 MPP (Massive Parallel Processing) SQL 引擎，其理念来源于一个叫 Volcano 的并行数据库，该数据库提出了一个并行执行 SQL 的模型，它被设计为用来专门进行高速、实时的数据分析。Presto是一个SQL计算引擎，分离计算层和存储层，其不存储数据，通过Connector SPI实现对各种数据源（Storage）的访问。</p><h3 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h3><img src="/img/2020/11/架构.png" width = "71%" /><p>Presto沿用了通用的Master-Slave架构，一个Coordinator，多个Worker。Coordinator负责解析SQL语句，生成执行计划，分发执行任务给Worker节点执行；Worker节点负责实际执行查询任务。Presto提供了一套Connector接口，用于读取元信息和原始数据，Presto 内置有多种数据源，如 Hive、MySQL、Kudu、Kafka 等。同时，Presto 的扩展机制允许自定义 Connector，从而实现对定制数据源的查询。假如配置了Hive Connector，需要配置一个Hive MetaStore服务为Presto提供Hive元信息，Worker节点通过Hive Connector与HDFS交互，读取原始数据。</p><h3 id="实现低延时原理"><a href="#实现低延时原理" class="headerlink" title="实现低延时原理"></a>实现低延时原理</h3><p>Presto是一个交互式查询引擎，我们最关心的是Presto实现低延时查询的原理，以下几点是其性能脱颖而出的主要原因。</p><ul><li>完全基于内存的并行计算</li><li>流水线</li><li>本地化计算</li><li>动态编译执行计划</li><li>小心使用内存和数据结构</li><li>GC控制</li><li>无容错</li></ul><h2 id="Presto-在滴滴的应用"><a href="#Presto-在滴滴的应用" class="headerlink" title="Presto 在滴滴的应用"></a>Presto 在滴滴的应用</h2><h3 id="业务场景"><a href="#业务场景" class="headerlink" title="业务场景"></a>业务场景</h3><ul><li>Hive SQL查询加速</li><li>数据平台Ad-Hoc查询</li><li>报表（BI报表、自定义报表）</li><li>活动营销</li><li>数据质量检测</li><li>资产管理</li><li>固定数据产品</li></ul><a id="more"></a><img src="/img/2020/11/场景.png" width = "71%" /><h3 id="业务规模"><a href="#业务规模" class="headerlink" title="业务规模"></a>业务规模</h3><img src="/img/2020/11/规模.png" width = "71%" /><h3 id="业务增长"><a href="#业务增长" class="headerlink" title="业务增长"></a>业务增长</h3><img src="/img/2020/11/增长.png" width = "71%" /><h3 id="集群部署"><a href="#集群部署" class="headerlink" title="集群部署"></a>集群部署</h3><img src="/img/2020/11/deploy.png" width = "71%" /><p>目前Presto分为混合集群和高性能集群，如上图所示，混合集群共用HDFS集群，与离线Hadoop大集群混合部署，为了防止集群内大查询影响小查询， 而单独搭建集群会导致集群太多，维护成本太高，我们通过指定Label来做到物理集群隔离（详细后文会讲到）。而高性能集群，HDFS是单独部署的，且可以访问Druid， 使Presto 具备查询实时数据和离线数据能力。</p><h3 id="接入方式"><a href="#接入方式" class="headerlink" title="接入方式"></a>接入方式</h3><ul><li>二次开发了JDBC、Go、Python、Cli、R、NodeJs 、HTTP等多种接入方式，打通了公司内部权限体系，让业务方方便快捷的接入 Presto 的，满足了业务方多种技术栈的接入需求。</li><li>Presto 接入了查询路由 Gateway，Gateway会智能选择合适的引擎，用户查询优先请求Presto，如果查询失败，会使用Spark查询，如果依然失败，最后会请求Hive。在Gateway层，我们做了一些优化来区分大查询、中查询及小查询，对于查询时间小于3分钟的，我们即认为适合Presto查询，比如通过HBO（基于历史的统计信息）及JOIN数量来区分查询大小，架构图见：</li></ul><img src="/img/2020/11/接入.png" width = "71%" /><h2 id="引擎迭代"><a href="#引擎迭代" class="headerlink" title="引擎迭代"></a>引擎迭代</h2><img src="/img/2020/11/time.png" width = "71%" /><p>我们从2017年09月份开始调研Presto，经历过0.192、0.215，共发布56次版本。而在19年初（0.215版本是社区分家版本），Presto社区分家，分为两个项目，叫PrestoDB和PrestoSQL，两者都成立了自己的基金会。我们决定升级到PrestoSQL 最新版本（340版本）原因是：</p><ul><li>PrestoSQL社区活跃度更高，PR和用户问题能够及时回复</li><li>PrestoDB主要主力还是Facebook维护，以其内部需求为主</li><li>PrestoDB未来方向主要是ETL相关的，我们有Spark兜底，ETL功能依赖Spark、Hive</li></ul><h2 id="引擎改进"><a href="#引擎改进" class="headerlink" title="引擎改进"></a>引擎改进</h2><p>在滴滴内部，Presto主要用于Ad-Hoc查询及Hive SQL查询加速，为了方便用户能尽快将SQL迁移到Presto引擎上，且提高Presto引擎查询性能，我们对Presto做了大量二次开发。同时，因为使用Gateway，即使SQL查询出错，SQL也会转发到Spark及Hive上，所以我们没有使用Presto的Spill to Disk功能。这样一个纯内存SQL引擎在使用过程中会遇到很多稳定问题，我们在解决这些问题时，也积累了很多经验，下面将一一介绍：</p><h3 id="1、Hive-SQL兼容"><a href="#1、Hive-SQL兼容" class="headerlink" title="1、Hive SQL兼容"></a>1、Hive SQL兼容</h3><p>18年上半年，Presto刚起步，滴滴内部很多用户不愿意迁移业务，主要是因为Presto是ANSI SQL，与HiveQL差距较大，且查询结果也会出现结果不一致问题，迁移成本比较高，为了方便Hive用户能顺利迁移业务，我们对Presto做了Hive SQL兼容。而在技术选型时，我们没有在Presto上层，即没有在Gateway这层做SQL兼容，主要是因为开发量较大，且UDF相关的开发和转换成本太高，另外就是需要多做一次SQL解析，查询性能会受到影响，同时增加了Hive Metastore的请求次数，当时Hive Metastore的压力比较大，考虑到成本和稳定性，我们最后选择在Presto引擎层上兼容。</p><img src="/img/2020/11/sql.png" width = "71%" /><p>主要工作：</p><ul><li>隐式类型转换</li><li>语义兼容</li><li>语法兼容</li><li>支持Hive视图</li><li>Parquet HDFS文件读取支持</li><li>大量UDF支持</li><li>其他</li></ul><p>Hive SQL兼容，我们迭代了三个大版本，目前线上SQL通过率97% ~ 99%。而业务从Spark/Hive迁移到Presto后，查询性能平均提升30% ~ 50%，甚至一些场景提升10倍，Ad-Hoc场景共节省80%机器资源。下图是线上Presto集群的SQL查询通过率及失败原因占比，’null’ 表示查询成功的SQL，其他表示错误原因：<br><img src="/img/2020/11/兼容.png" width = "71%" /></p><h3 id="2、物理资源隔离"><a href="#2、物理资源隔离" class="headerlink" title="2、物理资源隔离"></a>2、物理资源隔离</h3><p>上文说到，对性能要求高的业务与大查询业务方混合跑，查询性能容易受到影响，只有单独搭建集群。而单独搭建集群导致Presto集群太多，维护成本太高。因为目前我们Presto Coordinator还没有遇到瓶颈，大查询主要影响Worker性能，比如一条大SQL导致Worker CPU打满，导致其他业务方SQL查询变慢。所以我们修改调度模块，让Presto支持可以动态打Label，动态调度指定的 Label 机器。如下图所示：</p><img src="/img/2020/11/druid.png" width = "71%" /><p>根据不同的业务划分不同的label，通过配置文件配置业务方指定的label和其对应的机器列表，Coordinator会加载配置，在内存里维护集群label信息，同时如果配置文件里label信息变动，Coordinator会定时更新label信息，这样调度时根据SQL指定的label信息来获取对应的Worker机器，如指定label A时，那调度机器里只选择Worker A 和 Worker B 即可。这样就可以做到让机器物理隔离了，对性能要求高的业务查询既有保障了。</p><h3 id="3、Druid-Connector"><a href="#3、Druid-Connector" class="headerlink" title="3、Druid Connector"></a>3、Druid Connector</h3><p>使用 Presto + HDFS 有一些痛点：</p><ul><li>latency高，QPS较低 </li><li>不能查实时数据，如果有实时数据需求，需要再构建一条实时数据链路，增加了系统的复杂性</li><li>要想获得极限性能，必须与HDFS DataNode 混部，且DataNode使用高级硬件，有自建HDFS的需求，增加了运维的负担</li></ul><p>所以我们在0.215版本实现了Presto on Druid Connector，此插件有如下优点：</p><ul><li>结合 Druid 的预聚合、计算能力（过滤聚合）、Cache能力，提升Presto性能（RT与QPS）</li><li>让 Presto 具备查询 Druid 实时数据能力</li><li>为Druid提供全面的SQL能力支持，扩展Druid数据的应用场景</li><li>通过Druid Broker获取Druid元数据信息</li><li>从Druid Historical直接获取数据</li><li>实现了Limit下推、Filter下推、Project下推及Agg下推</li></ul><p>在PrestoSQL 340版本，社区也实现了Presto on Druid Connector，但是此Connector是通过JDBC实现的，缺点比较明显：</p><ul><li>无法划分多个Split，查询性能差</li><li>请求查询Broker，之后再查询Historical，多一次网络通信</li><li>对于一些场景，如大量Scan场景，会导致Broker OOM</li><li>Project及Agg下推支持不完善</li></ul><p>详细架构图见：<br><img src="/img/2020/11/pod.png" width = "80%" /></p><p>使用了Presto on Druid后，一些场景，性能提升4~5倍。</p><h3 id="4、易用性建设"><a href="#4、易用性建设" class="headerlink" title="4、易用性建设"></a>4、易用性建设</h3><p>为了支持公司的几个核心数据平台，包括：数梦、提取工具、数易及特征加速及各种散户，我们对Presto做了很多二次开发，包括权限管理、语法支持等，保证了业务的快速接入。主要工作：</p><ul><li>租户与权限<ul><li>与内部Hadoop打通，使用HDFS SIMPLE协议做认证</li><li>使用Ranger做鉴权，解析SQL使Presto拥有将列信息传递给下游的能力，提供用户名+数据库名/表名/列名，四元组的鉴权能力，同时提供多表同时鉴权的能力</li><li>用户指定用户名做鉴权和认证，大账号用于读写HDFS数据</li><li>支持视图、表别名鉴权</li></ul></li><li>语法拓展<ul><li>支持add partition</li><li>支持数字开头的表</li><li>支持数字开头的字段</li></ul></li><li>特性增强<ul><li>insert数据时，将插入数据的总行数写入HMS，为业务方提供毫秒级的元数据感知能力</li><li>支持查询进度滚动更新，提升了用户体验</li><li>支持查询可以指定优先级，为用户不同等级的业务提供了优先级控制的能力</li><li>修改通信协议，支持业务方可以传达自定义信息，满足了用户的日志审计需要等</li><li>支持DeprecatedLzoTextInputFormat格式</li><li>支持读HDFS Parquet文件路径</li></ul></li></ul><h3 id="5、稳定性建设"><a href="#5、稳定性建设" class="headerlink" title="5、稳定性建设"></a>5、稳定性建设</h3><p>Presto在使用过程中会遇到很多稳定性问题，比如Coordinator OOM，Worker Full GC等，为了解决和方便定位这些问题，首先我们做了监控体系建设，主要包括：</p><ul><li>通过Presto Plugin实现日志审计功能</li><li>通过JMX获取引擎指标将监控信息写入Ganglia</li><li>将日志审计采集到HDFS和ES；统一接入运维监控体系，将所有指标发到 Kafka；</li><li>Presto UI改进：可以查看Worker信息，可以查看Worker死活信息</li></ul><p>通过以上功能，在每次出现稳定性问题时，方便我们及时定位问题，包括指标查看及SQL回放等，如下图所示，可以查看某集群的成功及失败SQL数，我们可以通过定义查询失败率来触发报警：</p><img src="/img/2020/11/query.png" width = "80%" /><p>在Presto交流社区，Presto的稳定性问题困扰了很多Presto使用者，包括Coordinator和Worker挂掉，集群运行一段时间后查询性能变慢等。我们在解决这些问题时积累了很多经验，这里说下解决思路和方法。<br>根据职责划分，Presto分为Coordinator和Worker模块，Coordinator主要负责SQL解析、生成查询计划、Split调度及查询状态管理等，所以当Coordinator遇到OOM或者Coredump时，获取元信息及生成Splits是重点怀疑的地方。而内存问题，推荐使用MAT分析具体原因。如下图是通过MAT分析，得出开启了FileSystem Cache，内存泄漏导致 OOM。</p><img src="/img/2020/11/oom.png" width = "80%" /><p>这里我们总结了Coordinator常见的问题和解决方法：</p><ul><li>使用HDFS FileSystem Cache导致内存泄漏，解决方法禁止FileSystem Cache，后续Presto自己维护了FileSystem Cache</li><li>Jetty导致堆外内存泄漏，原因是Gzip导致了堆外内存泄漏，升级Jetty版本解决</li><li>Splits太多，无可用端口，TIME_WAIT太高，修改TCP参数解决</li><li>JVM Coredump，显示”unable to create new native thread”，通过修改pid_max及max_map_count解决</li><li>Presto内核Bug，查询失败的SQL太多，导致Coordinator内存泄漏，社区已修复</li></ul><p>而Presto Worker主要用于计算，性能瓶颈点主要是内存和CPU。内存方面通过三种方法来保障和查找问题：</p><ul><li>通过Resource Group控制业务并发，防止严重超卖</li><li>通过JVM调优，解决一些常见内存问题，如Young GC Exhausted</li><li>善用MAT工具，发现内存瓶颈</li></ul><p>而Presto Worker常会遇到查询变慢问题，两方面原因，一是确定是否开启了Swap内存，当Free内存不足时，使用Swap会严重影响查询性能。第二是CPU问题，解决此类问题，要善用Perf工具，多做Perf来分析CPU为什么不在干活，看CPU主要在做什么，是GC问题还是JVM Bug。如下图所示，为线上Presto集群触发了JVM Bug，导致运行一段时间后查询变慢，重启后恢复，Perf后找到原因，分析JVM代码，可通过JVM调优或升级JVM版本解决：</p><img src="/img/2020/11/flame.png" width = "80%" /><p>这里我们也总结了Worker常见的问题和解决方法：</p><ul><li>Sys load过高，导致业务查询性能影响很大，研究jvm原理，通过参数（-XX:PerMethodRecompilationCutoff=10000 及 -XX:PerBytecodeRecompilationCutoff=10000）解决，也可升级最新JVM解决</li><li>Worker查询hang住问题，原因HDFS客户端存在bug，当Presto与HDFS混部署，数据和客户端在同一台机器上时，短路读时一直wait锁，导致查询Hang住超时，Hadoop社区已解决</li><li>超卖导致Worker Young GC Exhausted，优化GC参数，如设置-XX:G1ReservePercent=25 及 -XX:InitiatingHeapOccupancyPercent=15</li><li>ORC太大，导致Presto读取ORC Stripe Statistics出现OOM，解决方法是限制ProtoBuf报文大小，同时协助业务方合理数据治理</li><li>修改Presto内存管理逻辑，优化Kill策略，保障当内存不够时，Presto Worker不会OOM，只需要将大查询Kill掉，后续熔断机制会改为基于JVM，类似ES的熔断器，比如95% JVM 内存时，Kill掉最大SQL</li></ul><h3 id="6、引擎优化及调研"><a href="#6、引擎优化及调研" class="headerlink" title="6、引擎优化及调研"></a>6、引擎优化及调研</h3><p>作为一个Ad-Hoc引擎，Presto查询性能越快，用户体验越好，为了提高Presto的查询性能，在Presto on Hive场景，我们做了很多引擎优化工作，主要工作：</p><ul><li>某业务集群进行了JVM调优，将Ref Proc由单线程改为并行执行，普通查询由30S~1分钟降低为3-4S，性能提升10倍+</li><li>ORC数据优化，将指定string字段添加了布隆过滤器，查询性能提升20-30%，针对一些业务做了调优</li><li>数据治理和小文件合并，某业务方查询性能由20S降低为10S，性能提升一倍，且查询性能稳定</li><li>ORC格式性能优化，查询耗时减少5%</li><li>分区裁剪优化，解决指定分区但获取所有分区元信息问题，减少了HMS的压力</li><li>下推优化，实现了Limit、Filter、Project、Agg下推到存储层</li></ul><p>18年我们为了提高Presto查询性能，也调研了一些技术方案，包括Presto on Alluxio和Presto on Carbondata，但是这2种方案最后都被舍弃了，原因是：</p><ul><li>Presto on Alluxio查询性能提升35%，但是内存占用和性能提升不成正比，所以我们放弃了Presto on Alluxio，后续可能会对一些性能要求敏感的业务使用</li><li>Presto on Carbondata是在18年8月份测试的，当时的版本，Carbondata稳定性较差，性能没有明显优势，一些场景ORC更快，所以我们没有再继续跟踪调研Presto on Carbondata。因为滴滴有专门维护Druid的团队，所以我们对接了Presto on Druid，一些场景性能提升4~5倍，后续我们会更多关注Presto on Clickhouse及Presto on Elasticsearch</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>通过以上工作，滴滴Presto逐渐接入公司各大数据平台，并成为了公司首选Ad-Hoc查询引擎及Hive SQL加速引擎，下图可以看到某产品接入后的性能提升：<br><img src="/img/2020/11/performance.png" width = "80%" /></p><p>上图可以看到大约2018年10月该平台开始接入Presto，查询耗时TP50性能提升了10+倍，由400S降低到31S。且在任务数逐渐增长的情况下，查询耗时保证稳定不变。<br>而高性能集群，我们做了很多稳定性和性能优化工作，保证了平均查询时间小于2S。如下图所示：</p><img src="/img/2020/11/高性能.png" width = "80%" /><h2 id="展望"><a href="#展望" class="headerlink" title="展望"></a>展望</h2><p>Presto主要应用场景是Ad-Hoc查询，所以其高峰期主要在白天，如下图所示，是网约车业务下午12-16点的查询，可以看到平均CPU使用率在40%以上。</p><img src="/img/2020/11/cpu.png" width = "80%" /><p>但是如果看最近一个月的CPU使用率会发现，平均CPU使用率比较低，且波峰在白天10~18点，晚上基本上没有查询，CPU使用率不到5%。如下图所示：</p><img src="/img/2020/11/cpu_low.png" width = "80%" /><p>所以，解决晚上资源浪费问题是我们今后需要解决的难题。<br>同时，为了不与开源社区脱节，我们打算升级PrestoDB 0.215到PrestoSQL 340版本，届时会把我们的Presto on Druid代码开源出来，回馈社区。</p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;Presto-简介&quot;&gt;&lt;a href=&quot;#Presto-简介&quot; class=&quot;headerlink&quot; title=&quot;Presto 简介&quot;&gt;&lt;/a&gt;Presto 简介&lt;/h2&gt;&lt;h3 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h3&gt;&lt;p&gt;Presto是Facebook 开源的 MPP (Massive Parallel Processing) SQL 引擎，其理念来源于一个叫 Volcano 的并行数据库，该数据库提出了一个并行执行 SQL 的模型，它被设计为用来专门进行高速、实时的数据分析。Presto是一个SQL计算引擎，分离计算层和存储层，其不存储数据，通过Connector SPI实现对各种数据源（Storage）的访问。&lt;/p&gt;
&lt;h3 id=&quot;架构&quot;&gt;&lt;a href=&quot;#架构&quot; class=&quot;headerlink&quot; title=&quot;架构&quot;&gt;&lt;/a&gt;架构&lt;/h3&gt;&lt;img src=&quot;/img/2020/11/架构.png&quot; width = &quot;71%&quot; /&gt;

&lt;p&gt;Presto沿用了通用的Master-Slave架构，一个Coordinator，多个Worker。Coordinator负责解析SQL语句，生成执行计划，分发执行任务给Worker节点执行；Worker节点负责实际执行查询任务。Presto提供了一套Connector接口，用于读取元信息和原始数据，Presto 内置有多种数据源，如 Hive、MySQL、Kudu、Kafka 等。同时，Presto 的扩展机制允许自定义 Connector，从而实现对定制数据源的查询。假如配置了Hive Connector，需要配置一个Hive MetaStore服务为Presto提供Hive元信息，Worker节点通过Hive Connector与HDFS交互，读取原始数据。&lt;/p&gt;
&lt;h3 id=&quot;实现低延时原理&quot;&gt;&lt;a href=&quot;#实现低延时原理&quot; class=&quot;headerlink&quot; title=&quot;实现低延时原理&quot;&gt;&lt;/a&gt;实现低延时原理&lt;/h3&gt;&lt;p&gt;Presto是一个交互式查询引擎，我们最关心的是Presto实现低延时查询的原理，以下几点是其性能脱颖而出的主要原因。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;完全基于内存的并行计算&lt;/li&gt;
&lt;li&gt;流水线&lt;/li&gt;
&lt;li&gt;本地化计算&lt;/li&gt;
&lt;li&gt;动态编译执行计划&lt;/li&gt;
&lt;li&gt;小心使用内存和数据结构&lt;/li&gt;
&lt;li&gt;GC控制&lt;/li&gt;
&lt;li&gt;无容错&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;Presto-在滴滴的应用&quot;&gt;&lt;a href=&quot;#Presto-在滴滴的应用&quot; class=&quot;headerlink&quot; title=&quot;Presto 在滴滴的应用&quot;&gt;&lt;/a&gt;Presto 在滴滴的应用&lt;/h2&gt;&lt;h3 id=&quot;业务场景&quot;&gt;&lt;a href=&quot;#业务场景&quot; class=&quot;headerlink&quot; title=&quot;业务场景&quot;&gt;&lt;/a&gt;业务场景&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;Hive SQL查询加速&lt;/li&gt;
&lt;li&gt;数据平台Ad-Hoc查询&lt;/li&gt;
&lt;li&gt;报表（BI报表、自定义报表）&lt;/li&gt;
&lt;li&gt;活动营销&lt;/li&gt;
&lt;li&gt;数据质量检测&lt;/li&gt;
&lt;li&gt;资产管理&lt;/li&gt;
&lt;li&gt;固定数据产品&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>Jetty导致Presto堆外内存泄露的排查过程</title>
    <link href="http://armsword.com/2020/06/23/jetty-cause-presto-memory-leak/"/>
    <id>http://armsword.com/2020/06/23/jetty-cause-presto-memory-leak/</id>
    <published>2020-06-23T10:52:11.000Z</published>
    <updated>2020-11-03T07:37:50.320Z</updated>
    
    <content type="html"><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>一个Presto集群报警，显示Coordinator内存不足。看了下，发现RES内存(86.7G) &gt; XMX(72G) + 堆外内存(10G)，然后看了下Presto UI里Resource Utilization的Non-Heap Memory Used，只用了200M。问题奇怪，pmap查看进程的内存映像信息，如下：</p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">[presto@hostname ~/presto-current]</span>$ pmap -x <span class="number">107870</span> | sort -rn -k3 | head -n <span class="number">10</span></span><br><span class="line">total kB         <span class="number">106752092</span> <span class="number">86763996</span> <span class="number">86684760</span></span><br><span class="line"><span class="number">00007f</span>d673000000 <span class="number">75644928</span> <span class="number">61078708</span> <span class="number">61078708</span> rw---   [ anon ]</span><br><span class="line"><span class="number">00007f</span>d5c8000000 <span class="number">2654208</span> <span class="number">2537116</span> <span class="number">2537116</span> rw---   [ anon ]</span><br><span class="line"><span class="number">000000000120</span>a000 <span class="number">1244772</span> <span class="number">1244644</span> <span class="number">1244644</span> rw---   [ anon ]</span><br><span class="line"><span class="number">00007f</span>d5803cf000  <span class="number">520388</span>  <span class="number">497972</span>  <span class="number">497972</span> rw---   [ anon ]</span><br><span class="line"><span class="number">00007f</span>d08c836000  <span class="number">149720</span>  <span class="number">149720</span>  <span class="number">149720</span> rw---   [ anon ]</span><br><span class="line"><span class="number">00007f</span>d0e2fe2000  <span class="number">147620</span>  <span class="number">147620</span>  <span class="number">147620</span> rw---   [ anon ]</span><br><span class="line"><span class="number">00007f</span>d0d9b35000  <span class="number">146920</span>  <span class="number">146920</span>  <span class="number">146920</span> rw---   [ anon ]</span><br><span class="line"><span class="number">00007fe964000000</span>  <span class="number">116672</span>  <span class="number">114164</span>  <span class="number">114164</span> rwx--   [ anon ]</span><br><span class="line"><span class="number">00007f</span>d0612b1000  <span class="number">111932</span>  <span class="number">111932</span>  <span class="number">111932</span> rw---   [ anon ]</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>第一列表示虚拟空间地址，第二列表示该项在虚拟空间中占用的大小，第三列表示RSS，第四列表示DIRTY，第五列表示该项名称（anon是佚名的）。</p><p>jmap -heap 发现堆内内存占用正常，然后我jmap dump了一份内存，dump后发现只有700M+，应该与堆内存无关了，既然这样查找直接从堆外内存入手了。</p><a id="more"></a><h2 id="查找过程"><a href="#查找过程" class="headerlink" title="查找过程"></a>查找过程</h2><p>启动<code>Native Memory Tracker</code>，jvm配置-XX:NativeMemoryTracking=detail参数，重启服务，等现场复现时，通过<code>jcmd pid VM.native_memory detail</code>查看到内存分布如下：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line">[presto@hostname ~/presto-current]$ jcmd 107870 VM.native_memory</span><br><span class="line">107870:</span><br><span class="line"></span><br><span class="line">Native Memory Tracking:</span><br><span class="line"></span><br><span class="line">Total: <span class="attribute">reserved</span>=85091386KB, <span class="attribute">committed</span>=84530010KB</span><br><span class="line">-                 Java Heap (<span class="attribute">reserved</span>=75497472KB, <span class="attribute">committed</span>=75497472KB)</span><br><span class="line">                            (mmap: <span class="attribute">reserved</span>=75497472KB, <span class="attribute">committed</span>=75497472KB) </span><br><span class="line"> </span><br><span class="line">-                     Class (<span class="attribute">reserved</span>=167037KB, <span class="attribute">committed</span>=167037KB)</span><br><span class="line">                            (classes #24712)</span><br><span class="line">                            (<span class="attribute">malloc</span>=7293KB #110480) </span><br><span class="line">                            (mmap: <span class="attribute">reserved</span>=159744KB, <span class="attribute">committed</span>=159744KB) </span><br><span class="line"> </span><br><span class="line">-                    Thread (<span class="attribute">reserved</span>=461357KB, <span class="attribute">committed</span>=461357KB)</span><br><span class="line">                            (thread #447)</span><br><span class="line">                            (stack: <span class="attribute">reserved</span>=458488KB, <span class="attribute">committed</span>=458488KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=1451KB #2268) </span><br><span class="line">                            (<span class="attribute">arena</span>=1418KB #892)</span><br><span class="line"> </span><br><span class="line">-                      Code (<span class="attribute">reserved</span>=555591KB, <span class="attribute">committed</span>=141671KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=23111KB #31456) </span><br><span class="line">                            (mmap: <span class="attribute">reserved</span>=532480KB, <span class="attribute">committed</span>=118560KB) </span><br><span class="line"> </span><br><span class="line">-                        GC (<span class="attribute">reserved</span>=2928579KB, <span class="attribute">committed</span>=2928579KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=94147KB #57989) </span><br><span class="line">                            (mmap: <span class="attribute">reserved</span>=2834432KB, <span class="attribute">committed</span>=2834432KB) </span><br><span class="line"> </span><br><span class="line">-                  Compiler (<span class="attribute">reserved</span>=719KB, <span class="attribute">committed</span>=719KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=588KB #919) </span><br><span class="line">                            (<span class="attribute">arena</span>=131KB #3)</span><br><span class="line"> </span><br><span class="line">-                  Internal (<span class="attribute">reserved</span>=5296433KB, <span class="attribute">committed</span>=5296433KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=5296401KB #75822) </span><br><span class="line">                            (mmap: <span class="attribute">reserved</span>=32KB, <span class="attribute">committed</span>=32KB) </span><br><span class="line"> </span><br><span class="line">-                    Symbol (<span class="attribute">reserved</span>=22611KB, <span class="attribute">committed</span>=22611KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=20110KB #202465) </span><br><span class="line">                            (<span class="attribute">arena</span>=2501KB #1)</span><br><span class="line"> </span><br><span class="line">-    Native Memory<span class="built_in"> Tracking </span>(<span class="attribute">reserved</span>=8111KB, <span class="attribute">committed</span>=8111KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=463KB #6860) </span><br><span class="line">                            (tracking <span class="attribute">overhead</span>=7648KB)</span><br><span class="line"> </span><br><span class="line">-               Arena Chunk (<span class="attribute">reserved</span>=6019KB, <span class="attribute">committed</span>=6019KB)</span><br><span class="line">                            (<span class="attribute">malloc</span>=6019KB) </span><br><span class="line"> </span><br><span class="line">-                   Unknown (<span class="attribute">reserved</span>=147456KB, <span class="attribute">committed</span>=0KB)</span><br><span class="line">                            (mmap: <span class="attribute">reserved</span>=147456KB, <span class="attribute">committed</span>=0KB)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>Internal主要是堆外内存占用，但是不太正常，显示占用5G+,但是我们通过Presto UI发现堆外内存占用只有200M+，难道有堆外内存泄露？查找jcmd里Internal块内存区间，发现Internal只占用了32KB。如下所示：</p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">[0x00007fe98dff4000 - 0x00007fe98dffc000]</span> reserved <span class="keyword">and</span> committed <span class="number">32</span>KB <span class="keyword">for</span> Internal <span class="keyword">from</span></span><br><span class="line"><span class="string">    [0x00007fe98c9599d2]</span> PerfMemory::create_memory_region(unsigned long)+<span class="number">0xac2</span></span><br><span class="line"><span class="string">    [0x00007fe98c958624]</span> PerfMemory::initialize()+<span class="number">0x44</span></span><br><span class="line"><span class="string">    [0x00007fe98ca63dcd]</span> Threads::create_vm(JavaVMInitArgs*, <span class="built_in">bool</span>*)+<span class="number">0x1ad</span></span><br><span class="line"><span class="string">    [0x00007fe98c6c1e64]</span> JNI_CreateJavaVM+<span class="number">0x74</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>那malloc申请的malloc=5296401KB去哪里了呢？只好把jcmd和pmap里面的内存对应起来了。命令：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">pmap</span> -x <span class="number">107870</span> | sort -rn -k<span class="number">3</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>排序一次比照jcmd和pmap内存块，如在jcmd里查找00007fd673000000地址，发现是mmap申请了个xmx堆大小的内存，依次比较，发现很多64M大小的内存块是在jcmd里找不到的。那这些块里存储的是什么的？使用gdb dump一份内存吧，命令：</p><figure class="highlight dsconfig"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">gdb </span><span class="built_in">--batch</span> <span class="built_in">--pid</span> <span class="string">107870 </span>-<span class="string">ex </span><span class="string">&quot;dump memory gdb_107870.dump 0x7fd5b4000000  0x7fd5b5000000&quot;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>导出了16MB数据，之后strings gdb_107870.dump 查看，发现内容如下：<br><img src="/img/2020/06/meta.png"></p><p>这些应该是堆内内存里，为啥会其他内存分配呢？没有想法，只好去perf了下。<br>命令：</p><figure class="highlight ada"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">perf <span class="keyword">record</span> -g -p <span class="number">107870</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>几十秒后Ctrl+C，执行以下命令查看报告：</p><figure class="highlight elm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="title">perf</span> re<span class="keyword">port</span> -i perf.data</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>报告如下，发现除了系统调用外，调用libzip的Deflater非常可疑。</p><p><img src="/img/2020/06/perf.png"></p><p>那哪里调用的zip？直接perf打出调用栈即可，内容如下：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">Started [cpu] profiling</span><br><span class="line">--- Execution profile ---</span><br><span class="line">Total samples       : <span class="number">152</span></span><br><span class="line">unknown_Java        : <span class="number">2</span> (<span class="number">1.32%</span>)</span><br><span class="line"></span><br><span class="line">Frame buffer usage  : <span class="number">0.5744%</span></span><br><span class="line"></span><br><span class="line">--- <span class="number">120038581</span> ns (<span class="number">7.87%</span>), <span class="number">12</span> samples</span><br><span class="line">  [ <span class="number">0</span>] longest_match</span><br><span class="line">  [ <span class="number">1</span>] deflate_slow</span><br><span class="line">  [ <span class="number">2</span>] deflate</span><br><span class="line">  [ <span class="number">3</span>] Java_java_util_zip_Deflater_deflateBytes</span><br><span class="line">  [ <span class="number">4</span>] java<span class="selector-class">.util</span><span class="selector-class">.zip</span><span class="selector-class">.Deflater</span>.deflateBytes</span><br><span class="line">  [ <span class="number">5</span>] java<span class="selector-class">.util</span><span class="selector-class">.zip</span><span class="selector-class">.Deflater</span>.deflate</span><br><span class="line">  [ <span class="number">6</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.server</span><span class="selector-class">.handler</span><span class="selector-class">.gzip</span>.GzipHttpOutputInterceptor<span class="variable">$GzipBufferCB</span>.process</span><br><span class="line">  [ <span class="number">7</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.util</span><span class="selector-class">.IteratingCallback</span>.processing</span><br><span class="line">  [ <span class="number">8</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.util</span><span class="selector-class">.IteratingCallback</span>.iterate</span><br><span class="line">  [ <span class="number">9</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.server</span><span class="selector-class">.handler</span><span class="selector-class">.gzip</span><span class="selector-class">.GzipHttpOutputInterceptor</span>.gzip</span><br><span class="line">  [<span class="number">10</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.server</span><span class="selector-class">.handler</span><span class="selector-class">.gzip</span><span class="selector-class">.GzipHttpOutputInterceptor</span>.write</span><br><span class="line">  [<span class="number">11</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.server</span><span class="selector-class">.HttpOutput</span>.write</span><br><span class="line">  [<span class="number">12</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.server</span><span class="selector-class">.HttpOutput</span>.write</span><br><span class="line">  [<span class="number">13</span>] org<span class="selector-class">.eclipse</span><span class="selector-class">.jetty</span><span class="selector-class">.server</span><span class="selector-class">.HttpOutput</span>.write</span><br><span class="line">  [<span class="number">14</span>] io<span class="selector-class">.airlift</span><span class="selector-class">.http</span><span class="selector-class">.server</span>.TimingFilter<span class="variable">$TimedServletOutputStream</span>.write</span><br><span class="line">  [<span class="number">15</span>] org<span class="selector-class">.glassfish</span><span class="selector-class">.jersey</span><span class="selector-class">.servlet</span><span class="selector-class">.internal</span>.ResponseWriter<span class="variable">$NonCloseableOutputStreamWrapper</span>.write</span><br><span class="line">  [<span class="number">16</span>] org<span class="selector-class">.glassfish</span><span class="selector-class">.jersey</span><span class="selector-class">.message</span><span class="selector-class">.internal</span><span class="selector-class">.CommittingOutputStream</span>.write</span><br><span class="line">  [<span class="number">17</span>] org<span class="selector-class">.glassfish</span><span class="selector-class">.jersey</span><span class="selector-class">.message</span><span class="selector-class">.internal</span>.WriterInterceptorExecutor<span class="variable">$UnCloseableOutputStream</span>.write</span><br><span class="line">  [<span class="number">18</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.core</span><span class="selector-class">.json</span><span class="selector-class">.UTF8JsonGenerator</span>._flushBuffer</span><br><span class="line">  [<span class="number">19</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.core</span><span class="selector-class">.json</span><span class="selector-class">.UTF8JsonGenerator</span>._writeCustomStringSegment2</span><br><span class="line">  [<span class="number">20</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.core</span><span class="selector-class">.json</span><span class="selector-class">.UTF8JsonGenerator</span>._writeStringSegment</span><br><span class="line">  [<span class="number">21</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.core</span><span class="selector-class">.json</span><span class="selector-class">.UTF8JsonGenerator</span>._writeStringSegments</span><br><span class="line">  [<span class="number">22</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.core</span><span class="selector-class">.json</span><span class="selector-class">.UTF8JsonGenerator</span>.writeString</span><br><span class="line">  [<span class="number">23</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.databind</span><span class="selector-class">.ser</span><span class="selector-class">.std</span><span class="selector-class">.StringSerializer</span>.serialize</span><br><span class="line">  [<span class="number">24</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.databind</span><span class="selector-class">.ser</span><span class="selector-class">.BeanPropertyWriter</span>.serializeAsField</span><br><span class="line">  [<span class="number">25</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.databind</span><span class="selector-class">.ser</span><span class="selector-class">.std</span><span class="selector-class">.BeanSerializerBase</span>.serializeFields</span><br><span class="line">  [<span class="number">26</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.databind</span><span class="selector-class">.ser</span><span class="selector-class">.BeanSerializer</span>.serialize</span><br><span class="line">  [<span class="number">27</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.databind</span><span class="selector-class">.ser</span><span class="selector-class">.std</span><span class="selector-class">.CollectionSerializer</span>.serializeContentsUsing</span><br><span class="line">  [<span class="number">28</span>] com<span class="selector-class">.fasterxml</span><span class="selector-class">.jackson</span><span class="selector-class">.databind</span><span class="selector-class">.ser</span><span class="selector-class">.std</span><span class="selector-class">.CollectionSerializer</span>.serializeContents</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>发现是jetty库调用的zip，因为zip的调用方式是这样的：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    Deflater zip = <span class="keyword">new</span> <span class="constructor">Deflater(<span class="params">level</span>)</span>;</span><br><span class="line">    DeflaterOutputStream defStream = <span class="keyword">new</span> <span class="constructor">DeflaterOutputStream(<span class="params">out</span>)</span>;</span><br><span class="line">&#125;  </span><br><span class="line">finally &#123;</span><br><span class="line">    defStream.close<span class="literal">()</span>;</span><br><span class="line">    zip.<span class="keyword">end</span><span class="literal">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>已知zip是通过jni调用的C语言库，需要close和end，否则会导致堆外内存泄露。找下jetty-server查看下GzipHttpOutputInterceptor的git history，发现jetty调用gzip确实存在着内存泄露：</p><p><a href="https://github.com/eclipse/jetty.project/pull/1429">https://github.com/eclipse/jetty.project/pull/1429</a><br><a href="https://github.com/eclipse/jetty.project/issues/300">https://github.com/eclipse/jetty.project/issues/300</a></p><p>具体原因大家可以看下上面那个2个PR，jetty在jetty 9.4.20修复了这个问题，我看了下最新的PrestoSQL，airlift里已经将jetty改为新版本了，而我们的Druid.io服务也存在着这个问题，所以先修改jetty依赖，之后看下线上表现吧。</p><p>最后，本文只是随手记录了下查找过程，顺便给同事点查找思路，后面的猜测gzip导致了内存泄露，也是因为前面分析是有堆外内存泄露产生，后面发现gzip cpu使用率比较高，且gzip是通过jni调用的，属于堆外内存，顺着这个思路去查找其代码逻辑，然后发现社区修复了这些问题，而我们的jetty依赖还是比较老的版本，所以修改jetty版本，之后上线验证下。</p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h2&gt;&lt;p&gt;一个Presto集群报警，显示Coordinator内存不足。看了下，发现RES内存(86.7G) &amp;gt; XMX(72G) + 堆外内存(10G)，然后看了下Presto UI里Resource Utilization的Non-Heap Memory Used，只用了200M。问题奇怪，pmap查看进程的内存映像信息，如下：&lt;/p&gt;
&lt;figure class=&quot;highlight angelscript&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;10&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;11&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;12&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;string&quot;&gt;[presto@hostname ~/presto-current]&lt;/span&gt;$ pmap -x &lt;span class=&quot;number&quot;&gt;107870&lt;/span&gt; | sort -rn -k3 | head -n &lt;span class=&quot;number&quot;&gt;10&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;total kB         &lt;span class=&quot;number&quot;&gt;106752092&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;86763996&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;86684760&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007f&lt;/span&gt;d673000000 &lt;span class=&quot;number&quot;&gt;75644928&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;61078708&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;61078708&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007f&lt;/span&gt;d5c8000000 &lt;span class=&quot;number&quot;&gt;2654208&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;2537116&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;2537116&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;000000000120&lt;/span&gt;a000 &lt;span class=&quot;number&quot;&gt;1244772&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;1244644&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;1244644&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007f&lt;/span&gt;d5803cf000  &lt;span class=&quot;number&quot;&gt;520388&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;497972&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;497972&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007f&lt;/span&gt;d08c836000  &lt;span class=&quot;number&quot;&gt;149720&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;149720&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;149720&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007f&lt;/span&gt;d0e2fe2000  &lt;span class=&quot;number&quot;&gt;147620&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;147620&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;147620&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007f&lt;/span&gt;d0d9b35000  &lt;span class=&quot;number&quot;&gt;146920&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;146920&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;146920&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007fe964000000&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;116672&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;114164&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;114164&lt;/span&gt; rwx--   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;number&quot;&gt;00007f&lt;/span&gt;d0612b1000  &lt;span class=&quot;number&quot;&gt;111932&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;111932&lt;/span&gt;  &lt;span class=&quot;number&quot;&gt;111932&lt;/span&gt; rw---   [ anon ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;第一列表示虚拟空间地址，第二列表示该项在虚拟空间中占用的大小，第三列表示RSS，第四列表示DIRTY，第五列表示该项名称（anon是佚名的）。&lt;/p&gt;
&lt;p&gt;jmap -heap 发现堆内内存占用正常，然后我jmap dump了一份内存，dump后发现只有700M+，应该与堆内存无关了，既然这样查找直接从堆外内存入手了。&lt;/p&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>记一次Presto Worker OOM的查找过程</title>
    <link href="http://armsword.com/2020/06/03/the-solution-of-presto-oom-caused-by-orc-statistics/"/>
    <id>http://armsword.com/2020/06/03/the-solution-of-presto-oom-caused-by-orc-statistics/</id>
    <published>2020-06-03T02:57:48.000Z</published>
    <updated>2020-11-03T07:37:50.311Z</updated>
    
    <content type="html"><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>最近Presto集群又上线了几个新业务，伴之而来的是OOM很频繁，且发生时间多在早晨8点左右，线上稳定性是高优需要解决的，所以查找了下导致Presto集群OOM的原因，发现了一些问题，这里抛砖引玉下，可能其他使用Presto的用户也会遇到类似的问题。</p><h2 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h2><p>我给一些业务划分了不同的label，这里说明下我们把Presto引擎改进了下，可以动态将机器划分不同的label，这样SQL查询时候指定不同的label，SQL调度时只根据指定的label查找机器即可。之后发现一个业务方的SQL会导致集群OOM。具体表现为，多次Full GC，之后OOM，看GC日志第一感觉应该是有内存泄露。</p><p>我通过审计日志（之前通过event-listener实现了个日志审计模块）拿到OOM时2K左右条SQL，发现SQL都是简单的SQL，类似这种：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> <span class="keyword">table</span> <span class="keyword">WHERE</span> <span class="keyword">year</span>=<span class="string">&#x27;2020&#x27;</span> <span class="keyword">AND</span> <span class="keyword">month</span>=<span class="string">&#x27;06&#x27;</span> <span class="keyword">AND</span> <span class="keyword">day</span>=<span class="string">&#x27;01&#x27;</span> <span class="keyword">LIMIT</span> <span class="number">10</span>;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>根据SQL，我猜测可能以下2种原因导致了OOM：</p><ul><li>查询的表存在Hive视图（我让Presto支持了Hive视图）</li><li>异常SQL触发了内存泄露</li></ul><a id="more"></a><p>因为这个业务方的SQL都是小查询，90时间也就1S样子，所以懒得推测原因了，直接把SQL拿到，二分法测试环境跑一遍SQL，通过jstat -gcutil查看GC情况，Old区飙升的会认为存在潜在问题，最终定位了6个表有问题，并且在查询这些表SQL时，Old区疯涨，然后Full GC，连续多次Full GC后OOM。</p><p>show create table 查看下表字段信息，70-100个左右字段，但惊奇的发现这几个表都是ORC格式，所以OOM与ORC有关系吗？</p><p>那通过orcdump查看下这些orc的元信息吧，命令：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">.<span class="regexp">/bin/</span>hive --orcfiledump hdfs:<span class="regexp">//</span>localhost:<span class="number">9000</span><span class="regexp">/user/</span>hive<span class="regexp">/warehouse/</span>orc/<span class="number">000000</span>_0</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>惊奇的发现这个ORC文件竟然有2W+个Stripe Statistics，猜测是Stripe Statistics太大导致的问题，如下图所示：<br><img src="/img/2020/06/stripe.png"></p><p>第一反应，是不是用户生成orc参数配置错了，比如stripe的大小设置过小，但是看了下业务方的生成orc的参数，属于正常的，如下图：<br><img src="/img/2020/06/hivecode.png"></p><p>hadoop du看了下orc文件，发现一个表只有一个orc文件，且竟然大于20GB，根据orc predicate pushdown的先后顺序策略：</p><ul><li>文件级别，整个文件级别的统计信息</li><li>stripe级别，每个stripe每列中的值的统计信息</li><li>行级别 (行组)，stripe中每组10000行（默认值）的每列值的统计信息</li></ul><p>那文件的大小应该会影响stripe的大小，于是让业务方将repartition改为10，即生成10个文件，测试发现查询小于1S，且无OOM。<br><img src="/img/2020/06/spark.png"></p><p>验证成功，即Orc Stripe Statistics太大导致的问题。那为什么Stripe Statistics太大会导致Presto OOM呢？</p><p>我们测试环境查询一个有问题的SQL，然后perf下，看下火山图，看下当时Presto当时都在做什么，如下图所示：<br><img src="/img/2020/06/perf.jpg"></p><p>发现大约70%的CPU都用于GC，剩下的都在执行SQL计算。我们生成一份CPU采样，看下执行SQL计算时主要做什么，如下图：<br><img src="/img/2020/06/call.png"></p><p>根据上图可以看到每个Driver对应的split都在读取Stripe Statistics，我们的猜测是符合预期的，所以原因就明朗了：</p><p>业务方使用spark产出orc文件，其repartition设置为1，就会产生一个大于20+G的orc文件，导致stripe个数太多，比如产生2W个stripe（有些表orcdump都不成功，stripe statistics太大了，无法dump，远超2W个），假如表有75个字段，那就会产生2W * 75 个实例，如果presto再划分多个split，比如100，那就是100 * 2W * 75 个instance，因为这个内存是untracked memory，无法使用Presto的OOM Killer逻辑，如果机器内存比较小，那可能2个sql就导致oom了。</p><h2 id="引擎改进"><a href="#引擎改进" class="headerlink" title="引擎改进"></a>引擎改进</h2><p>我们通知了业务方如果ORC文件太大，将ORC拆分为多个文件，但是可能业务方比较多，下次依然遇到类似的问题，所以引擎需要改进下，尽量让这种不符合预期的SQL直接报错。我们知道ORC使用Protocol buffers来定义一个orc file中元数据的结构，PB报文是限制大小的，网上搜索了下是64MB，但是为什么Presto依然能查询报文超过64MB的ORC文件？研究了下，发现CodedInputStream有个setSizeLimit接口可以限制PB报文的大小，我将其改为64MB，发现有问题的SQL将会查询失败，符合预期。</p><p>但是我发现一个问题，当报文超过64M，SQL查询失败后，worker old区依然快满了，我jmap触发了full gc，old区没有任何变化，说明有类似内存泄露的问题，但是过了15分钟后，old区恢复正常，我们知道task info会在内存里保留15分钟，难道与这个有关？于是我dump了份内存，使用MAT分析了下。</p><p>如下图所示，发现Stripe Statistics依然在内存中：</p><p><img src="/img/2020/06/memory.jpg"></p><p>看下哪里引用了这些类，导致无法被内存释放，如下图所示：</p><p><img src="/img/2020/06/exception.jpg"></p><p>这样就明白了，读取ORC文件失败时候，每个split都抛出了异常，然后异常信息扔入一个链表里，每个异常信息竟然引用了Orc Statistics信息。既然原因明了了，我们将异常信息替换下，这样就避免了Stripe Statistics信息被引用导致无法被释放，简单修改下代码测试下：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">@Override</span><br><span class="line">public Metadata read<span class="constructor">Metadata(HiveWriterVersion <span class="params">hiveWriterVersion</span>, InputStream <span class="params">inputStream</span>)</span></span><br><span class="line">       throws IOException</span><br><span class="line">&#123;</span><br><span class="line">    CodedInputStream input = <span class="module-access"><span class="module"><span class="identifier">CodedInputStream</span>.</span></span><span class="keyword">new</span><span class="constructor">Instance(<span class="params">inputStream</span>)</span>;</span><br><span class="line">    input.set<span class="constructor">SizeLimit(PROTOBUF_MESSAGE_MAX_LIMIT)</span>; <span class="comment">//通过配置修改为可更改，默认为64MB</span></span><br><span class="line">    OrcProto.Metadata metadata;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        metadata = <span class="module-access"><span class="module"><span class="identifier">OrcProto</span>.</span><span class="module"><span class="identifier">Metadata</span>.</span></span>parse<span class="constructor">From(<span class="params">input</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    catch (IOException e) &#123;</span><br><span class="line">        throw <span class="keyword">new</span> <span class="constructor">PrestoException(NOT_SUPPORTED, <span class="string">&quot;read orc proto failed, may be the orc file statistic is too large&quot;</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    return <span class="keyword">new</span> <span class="constructor">Metadata(<span class="params">toStripeStatistics</span>(<span class="params">hiveWriterVersion</span>, <span class="params">metadata</span>.<span class="params">getStripeStatsList</span>()</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>之后测试，查询超过64MB报文的ORC文件，查询失败，无内存泄露产生，世界终于清净了。</p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h2&gt;&lt;p&gt;最近Presto集群又上线了几个新业务，伴之而来的是OOM很频繁，且发生时间多在早晨8点左右，线上稳定性是高优需要解决的，所以查找了下导致Presto集群OOM的原因，发现了一些问题，这里抛砖引玉下，可能其他使用Presto的用户也会遇到类似的问题。&lt;/p&gt;
&lt;h2 id=&quot;排查过程&quot;&gt;&lt;a href=&quot;#排查过程&quot; class=&quot;headerlink&quot; title=&quot;排查过程&quot;&gt;&lt;/a&gt;排查过程&lt;/h2&gt;&lt;p&gt;我给一些业务划分了不同的label，这里说明下我们把Presto引擎改进了下，可以动态将机器划分不同的label，这样SQL查询时候指定不同的label，SQL调度时只根据指定的label查找机器即可。之后发现一个业务方的SQL会导致集群OOM。具体表现为，多次Full GC，之后OOM，看GC日志第一感觉应该是有内存泄露。&lt;/p&gt;
&lt;p&gt;我通过审计日志（之前通过event-listener实现了个日志审计模块）拿到OOM时2K左右条SQL，发现SQL都是简单的SQL，类似这种：&lt;/p&gt;
&lt;figure class=&quot;highlight sql&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;SELECT&lt;/span&gt; * &lt;span class=&quot;keyword&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;year&lt;/span&gt;=&lt;span class=&quot;string&quot;&gt;&amp;#x27;2020&amp;#x27;&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;month&lt;/span&gt;=&lt;span class=&quot;string&quot;&gt;&amp;#x27;06&amp;#x27;&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;day&lt;/span&gt;=&lt;span class=&quot;string&quot;&gt;&amp;#x27;01&amp;#x27;&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;10&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;根据SQL，我猜测可能以下2种原因导致了OOM：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;查询的表存在Hive视图（我让Presto支持了Hive视图）&lt;/li&gt;
&lt;li&gt;异常SQL触发了内存泄露&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>PrestoDB和PrestoSQL比较及选择</title>
    <link href="http://armsword.com/2020/05/02/the-difference-between-prestodb-and-prestosql/"/>
    <id>http://armsword.com/2020/05/02/the-difference-between-prestodb-and-prestosql/</id>
    <published>2020-05-02T06:50:34.000Z</published>
    <updated>2020-11-03T07:37:50.321Z</updated>
    
    <content type="html"><![CDATA[<p>Presto 社区分家后搞了2个项目，分别为 PrestoDB 和 PrestoSQL，同时他们都成立了自己的基金会。而去年国庆时候，abei写了篇文章比对了2个分支的进展，链接见：<a href="https://zhuanlan.zhihu.com/p/87621360">PrestoDB VS PrestoSQL发展比较</a>。 而现在已经分家17个月了，那我们简单梳理下这2个分支的主要核心功能：</p><h3 id="PrestoDB："><a href="#PrestoDB：" class="headerlink" title="PrestoDB："></a>PrestoDB：</h3><ul><li>Connector：ElasticSearch 及 Druid</li><li>Master 和Worker 通信协议支持二进制</li><li>Orc 及 Parquet读写性能优化</li><li>Hive写数据支持指定压缩格式</li><li>task通信协议可以指定 thrift</li><li>spi pushdown</li><li>MapReduce-style shuffle，支持部分 etl 任务及大查询</li><li>fix bug 及 improve performace</li></ul><h3 id="PrestoSQL："><a href="#PrestoSQL：" class="headerlink" title="PrestoSQL："></a>PrestoSQL：</h3><ul><li>Connector：ElasticSearch和MemSQL</li><li>spi pushdown</li><li>S3优化读取</li><li>join 延时物化</li><li>大量的 fix bug 及 improve performance</li></ul><a id="more"></a><p>从以上功能实现可以看到，PrestoDB更符合我对Presto未来的定位：</p><ul><li>支持大集群</li><li>提高并发能力</li><li>减少指定场景的查询耗时，比如ORC</li><li>下推能力存储计算分离，比如agg扔到存储层去做（2个分支都有）</li></ul><p>除了以上功能外，PrestoDB还有其他优点：</p><ul><li>核心功能会有技术文章输出</li><li>issue里技术实现和细节比较全</li><li>基金会里包含Facebook、阿里巴巴、twitter及Uber，有场景支持和实践，目前twitter员工很活跃</li><li>老的PrestoDB版本升级简单</li><li>PrestoDB代码质量比PrestoSQL要好，PrestoSQL有些地方代码改的有些混乱</li><li>wiki和文章排版很舒服，PrestoSQL Blog也没有任何技术输出</li></ul><h3 id="PrestoDB缺点："><a href="#PrestoDB缺点：" class="headerlink" title="PrestoDB缺点："></a>PrestoDB缺点：</h3><ul><li>主要是Facebook员工维护，Facebook的文化迫使他们员工没有办法花时间在社区上</li><li>社区活跃度不如PrestoSQL</li><li>Release Notes 上的改动没有对应的issue链接</li></ul><p>19年我经常在slack上与PrestoSQL社区的人打交道，别人问我选择版本时候，我都是建议选择PrestoSQL，但是目前我有了不同的想法，如果说只是使用Presto，2个分支其实都无所谓，如果说公司体量较大，会有不少二次开发，我认为PrestoDB做的事情和提供的能力更符合国内很多公司的需求，比如让Presto即要支持ad-hoc，又要支持etl（听说目前Facebook就在这么搞），以及很多实时需求等等。</p><h3 id="2020-5-20补充："><a href="#2020-5-20补充：" class="headerlink" title="2020.5.20补充："></a>2020.5.20补充：</h3><p>我们决定升级到PrestoSQL最新版本了（333），原因是：</p><ul><li>社区活跃度</li><li>PrestoDB主要主力还是Facebook维护，不可控，且有概率被C++重写</li><li>PrestoDB未来方向主要是ETL相关的，我们有Spark兜底，ETL功能靠Spark、Hive就好了</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;Presto 社区分家后搞了2个项目，分别为 PrestoDB 和 PrestoSQL，同时他们都成立了自己的基金会。而去年国庆时候，abei写了篇文章比对了2个分支的进展，链接见：&lt;a href=&quot;https://zhuanlan.zhihu.com/p/87621360&quot;&gt;PrestoDB VS PrestoSQL发展比较&lt;/a&gt;。 而现在已经分家17个月了，那我们简单梳理下这2个分支的主要核心功能：&lt;/p&gt;
&lt;h3 id=&quot;PrestoDB：&quot;&gt;&lt;a href=&quot;#PrestoDB：&quot; class=&quot;headerlink&quot; title=&quot;PrestoDB：&quot;&gt;&lt;/a&gt;PrestoDB：&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;Connector：ElasticSearch 及 Druid&lt;/li&gt;
&lt;li&gt;Master 和Worker 通信协议支持二进制&lt;/li&gt;
&lt;li&gt;Orc 及 Parquet读写性能优化&lt;/li&gt;
&lt;li&gt;Hive写数据支持指定压缩格式&lt;/li&gt;
&lt;li&gt;task通信协议可以指定 thrift&lt;/li&gt;
&lt;li&gt;spi pushdown&lt;/li&gt;
&lt;li&gt;MapReduce-style shuffle，支持部分 etl 任务及大查询&lt;/li&gt;
&lt;li&gt;fix bug 及 improve performace&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;PrestoSQL：&quot;&gt;&lt;a href=&quot;#PrestoSQL：&quot; class=&quot;headerlink&quot; title=&quot;PrestoSQL：&quot;&gt;&lt;/a&gt;PrestoSQL：&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;Connector：ElasticSearch和MemSQL&lt;/li&gt;
&lt;li&gt;spi pushdown&lt;/li&gt;
&lt;li&gt;S3优化读取&lt;/li&gt;
&lt;li&gt;join 延时物化&lt;/li&gt;
&lt;li&gt;大量的 fix bug 及 improve performance&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>Presto调度task选择Worker方法</title>
    <link href="http://armsword.com/2020/04/08/presto-scheduling-task/"/>
    <id>http://armsword.com/2020/04/08/presto-scheduling-task/</id>
    <published>2020-04-08T06:30:22.000Z</published>
    <updated>2020-11-03T07:37:50.317Z</updated>
    
    <content type="html"><![CDATA[<h3 id="Presto调度task方式："><a href="#Presto调度task方式：" class="headerlink" title="Presto调度task方式："></a>Presto调度task方式：</h3><figure class="highlight crystal"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">public final <span class="class"><span class="keyword">class</span> <span class="title">SystemPartitioningHandle</span></span></span><br><span class="line">        implements ConnectorPartitioningHandle</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="class"><span class="keyword">enum</span> <span class="title">SystemPartitioning</span></span></span><br><span class="line">    &#123;</span><br><span class="line">        SINGLE,</span><br><span class="line">        FIXED,</span><br><span class="line">        SOURCE,</span><br><span class="line">        SCALED,</span><br><span class="line">        COORDINATOR_ONLY,</span><br><span class="line">        ARBITRARY</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>常见的场景主要包含SINGLE、FIXED及SOURCE类型，其中SINGLE表示最后数据的汇总输出，FIXED表示中间数据的计算，如JOIN等，SOURCE类型表示与源数据打交道的类型。</p><p>以下SQL为例：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> (<span class="keyword">select</span> * <span class="keyword">from</span> <span class="number">1</span><span class="keyword">test</span> <span class="keyword">join</span> <span class="number">2</span>test1 <span class="keyword">on</span> <span class="number">1</span>test.id = <span class="number">2</span>test1<span class="number">.123</span><span class="keyword">id</span>);</span><br><span class="line"></span><br></pre></td></tr></table></figure><a id="more"></a><h3 id="其执行计划为："><a href="#其执行计划为：" class="headerlink" title="其执行计划为："></a>其执行计划为：</h3><p><img src="/img/2020/04/plan.png"></p><p>如上图所示，左右两个stage调度task时都是SOURCE类型，中间为两张表JOIN，其为FIXED类型，最上面的output为SINGLE类型，做最后的汇总输出。</p><p>无论哪种类型，在调度task时，都要选择Worker，那其是怎么选择Worker的呢？</p><p>根据上面我们提到的Presto调度task方式，可以划分为两类：非源头fragment和源头fragment</p><h3 id="非源头fragment"><a href="#非源头fragment" class="headerlink" title="非源头fragment"></a>非源头fragment</h3><p>依赖SystemPartitioningHandle策略，通过当前集群可用节点以及hash partition count配置（query.initial-hash-partitions，默认为100）来共同确定该fragment需要的节点个数, 之后再调用 NodeSelector 选出Worker。逻辑为下：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">public NodePartitionMap get<span class="constructor">NodePartitionMap(Session <span class="params">session</span>, NodeScheduler <span class="params">nodeScheduler</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">        NodeSelector nodeSelector = nodeScheduler.create<span class="constructor">NodeSelector(<span class="params">null</span>)</span>;</span><br><span class="line">        List&lt;Node&gt; nodes;</span><br><span class="line">        <span class="keyword">if</span> (partitioning<span class="operator"> == </span>SystemPartitioning.COORDINATOR_ONLY) &#123;</span><br><span class="line">            nodes = <span class="module-access"><span class="module"><span class="identifier">ImmutableList</span>.</span></span><span class="keyword">of</span>(nodeSelector.select<span class="constructor">CurrentNode()</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (partitioning<span class="operator"> == </span>SystemPartitioning.SINGLE) &#123;</span><br><span class="line">            nodes = nodeSelector.select<span class="constructor">RandomNodes(1)</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (partitioning<span class="operator"> == </span>SystemPartitioning.FIXED) &#123;</span><br><span class="line">            nodes = nodeSelector.select<span class="constructor">RandomNodes(<span class="params">getHashPartitionCount</span>(<span class="params">session</span>)</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            throw <span class="keyword">new</span> <span class="constructor">IllegalArgumentException(<span class="string">&quot;Unsupported plan distribution &quot;</span> + <span class="params">partitioning</span>)</span>;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        check<span class="constructor">Condition(!<span class="params">nodes</span>.<span class="params">isEmpty</span>()</span>, NO_NODES_AVAILABLE, <span class="string">&quot;No worker nodes available&quot;</span>);</span><br><span class="line"> </span><br><span class="line">        ImmutableMap.Builder&lt;Integer, Node&gt; partitionToNode = <span class="module-access"><span class="module"><span class="identifier">ImmutableMap</span>.</span></span>builder<span class="literal">()</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nodes.size<span class="literal">()</span>; i++) &#123;</span><br><span class="line">            Node node = nodes.get(i);</span><br><span class="line">            partitionToNode.put(i, node);</span><br><span class="line">        &#125;</span><br><span class="line">        return <span class="keyword">new</span> <span class="constructor">NodePartitionMap(<span class="params">partitionToNode</span>.<span class="params">build</span>()</span>, split -&gt; &#123;</span><br><span class="line">            throw <span class="keyword">new</span> <span class="constructor">UnsupportedOperationException(<span class="string">&quot;System distribution does not support source splits&quot;</span>)</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>根据上面代码，我们可以看到SINGLE及FIXED会根据调度task的类型选择Node，最终调用的是同一个函数，函数逻辑为：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">public List&lt;Node&gt; select<span class="constructor">RandomNodes(<span class="params">int</span> <span class="params">limit</span>, Set&lt;Node&gt; <span class="params">excludedNodes</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    return select<span class="constructor">Nodes(<span class="params">limit</span>, <span class="params">randomizedNodes</span>(<span class="params">nodeMap</span>.<span class="params">get</span>()</span>.get<span class="literal">()</span>, includeCoordinator, excludedNodes));</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="源头fragment"><a href="#源头fragment" class="headerlink" title="源头fragment"></a>源头fragment</h3><p>源头fragment依赖于其connector id，以hive connector为例，其通过discovery服务注册上来，具体实现是在DiscoveryNodeManger#refreshNodesInternal。在真正Schedule task时，为split分配Node时，采用DynamicSplitPlacementPolicy策略调用以下接口：</p><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">public static ResettableRandomizedIterator<span class="tag">&lt;Node&gt;</span> randomizedNodes(NodeMap nodeMap, boolean includeCoordinator, Set<span class="tag">&lt;Node&gt;</span> excludedNodes)</span><br><span class="line">&#123;</span><br><span class="line">        ImmutableList<span class="tag">&lt;Node&gt;</span> nodes = nodeMap.getNodesByHostAndPort().values().stream()</span><br><span class="line">                .filter(<span class="keyword">node</span> <span class="title">-&gt; includeCoordinator</span> || !nodeMap.getCoordinatorNodeIds().contains(node.getNodeIdentifier()))</span><br><span class="line">                .filter(<span class="keyword">node</span> <span class="title">-&gt; !excludedNodes</span>.contains(<span class="keyword">node</span><span class="title">))</span></span><br><span class="line"><span class="title">                .collect</span>(toImmutableList());</span><br><span class="line">        return new ResettableRandomizedIterator<span class="tag">&lt;&gt;</span>(nodes);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>可以看到无论是哪种选择方式，最终都绕不开NodeMap，那NodeMap怎么来的，由上文代码可以看到，其需要创建nodeScheduler.createNodeSelector，代码如下：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">public NodeSelector create<span class="constructor">NodeSelector(ConnectorId <span class="params">connectorId</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">        <span class="comment">// this supplier is thread-safe. <span class="doctag">TODO:</span> this logic should probably move to the scheduler since the choice of which node to run in should be</span></span><br><span class="line">        <span class="comment">// done as close to when the the split is about to be scheduled</span></span><br><span class="line">        Supplier&lt;NodeMap&gt; nodeMap = <span class="module-access"><span class="module"><span class="identifier">Suppliers</span>.</span></span>memoize<span class="constructor">WithExpiration(()</span> -&gt; &#123;</span><br><span class="line">            ImmutableSetMultimap.Builder&lt;HostAddress, Node&gt; byHostAndPort = <span class="module-access"><span class="module"><span class="identifier">ImmutableSetMultimap</span>.</span></span>builder<span class="literal">()</span>;</span><br><span class="line">            ImmutableSetMultimap.Builder&lt;InetAddress, Node&gt; byHost = <span class="module-access"><span class="module"><span class="identifier">ImmutableSetMultimap</span>.</span></span>builder<span class="literal">()</span>;</span><br><span class="line">            ImmutableSetMultimap.Builder&lt;NetworkLocation, Node&gt; workersByNetworkPath = <span class="module-access"><span class="module"><span class="identifier">ImmutableSetMultimap</span>.</span></span>builder<span class="literal">()</span>;</span><br><span class="line"> </span><br><span class="line">            Set&lt;Node&gt; nodes;</span><br><span class="line">            <span class="keyword">if</span> (connectorId != null) &#123;</span><br><span class="line">                nodes = nodeManager.get<span class="constructor">ActiveConnectorNodes(<span class="params">connectorId</span>)</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                nodes = nodeManager.get<span class="constructor">Nodes(ACTIVE)</span>;</span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">            Set&lt;String&gt; coordinatorNodeIds = nodeManager.get<span class="constructor">Coordinators()</span>.stream<span class="literal">()</span></span><br><span class="line">                    .map(Node::getNodeIdentifier)</span><br><span class="line">                    .collect(<span class="keyword">to</span><span class="constructor">ImmutableSet()</span>);</span><br><span class="line"> </span><br><span class="line">            <span class="keyword">for</span> (Node node : nodes) &#123;</span><br><span class="line">            <span class="comment">// 略</span></span><br></pre></td></tr></table></figure><p>这里我们找到源头了，Presto的机器是由Discovery管理的，上文nodeManager即DiscoveryNodeManager封装了Discovery服务接口，Discovery维护Worker死活信息。</p><p>知道Worker怎么选择后，之后就会使用stage的不同的调度器来开始下发task和split，同样根据SystemPartitioningHandle的不同，源头分为SourcePartitionedScheduler、FixedSourcePartitionedScheduler，非源头使用FixedCountScheduler进行调度，其在调度某个stage时下发多少task到Worker以及split下发过程，我们将在后续介绍。</p>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;Presto调度task方式：&quot;&gt;&lt;a href=&quot;#Presto调度task方式：&quot; class=&quot;headerlink&quot; title=&quot;Presto调度task方式：&quot;&gt;&lt;/a&gt;Presto调度task方式：&lt;/h3&gt;&lt;figure class=&quot;highlight crystal&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;10&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;11&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;12&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;13&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;public final &lt;span class=&quot;class&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;SystemPartitioningHandle&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        implements ConnectorPartitioningHandle&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;private&lt;/span&gt; &lt;span class=&quot;class&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;enum&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;SystemPartitioning&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        SINGLE,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        FIXED,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        SOURCE,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        SCALED,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        COORDINATOR_ONLY,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        ARBITRARY&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;常见的场景主要包含SINGLE、FIXED及SOURCE类型，其中SINGLE表示最后数据的汇总输出，FIXED表示中间数据的计算，如JOIN等，SOURCE类型表示与源数据打交道的类型。&lt;/p&gt;
&lt;p&gt;以下SQL为例：&lt;/p&gt;
&lt;figure class=&quot;highlight sql&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;select&lt;/span&gt; * &lt;span class=&quot;keyword&quot;&gt;from&lt;/span&gt; (&lt;span class=&quot;keyword&quot;&gt;select&lt;/span&gt; * &lt;span class=&quot;keyword&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;keyword&quot;&gt;test&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;join&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;2&lt;/span&gt;test1 &lt;span class=&quot;keyword&quot;&gt;on&lt;/span&gt; &lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;test.id = &lt;span class=&quot;number&quot;&gt;2&lt;/span&gt;test1&lt;span class=&quot;number&quot;&gt;.123&lt;/span&gt;&lt;span class=&quot;keyword&quot;&gt;id&lt;/span&gt;);&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>Presto集群内存不足时保护机制</title>
    <link href="http://armsword.com/2020/02/18/presto-memory-kill-policy/"/>
    <id>http://armsword.com/2020/02/18/presto-memory-kill-policy/</id>
    <published>2020-02-18T14:28:21.000Z</published>
    <updated>2020-11-03T07:37:50.312Z</updated>
    
    <content type="html"><![CDATA[<p>为了防止集群里节点OOM，Presto有个循环线程来获取当前集群节点和集群整体内存占用情况。通过这篇文章：<a href="http://armsword.com/2019/11/13/the-configuration-settings-of-presto-memory-management/">Presto内存管理相关参数设置</a>  我们知道Presto里分为RESERVED_POOL和GENERAL_POOL。</p><h2 id="判断节点是否阻塞（内存不足）："><a href="#判断节点是否阻塞（内存不足）：" class="headerlink" title="判断节点是否阻塞（内存不足）："></a>判断节点是否阻塞（内存不足）：</h2><p>如果使用RESERVED_POOL（意思是说最大SQL使用这个POOL），那判断集群内存超出内存的方法就是：</p><p>1、RESERVED_POOL内存被SQL占用了</p><p>2、GENERAL_POOL里有被阻塞的Node</p><p>因为RESERVED_POOL会导致内存浪费，我们集群配置参数没有使用这个POOL，只使用了GENERAL_POOL，所以只需要查看下GENERAL_POOL是怎么判断节点是否Block住的。</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (poolInfo.get<span class="constructor">FreeBytes()</span> + poolInfo.get<span class="constructor">ReservedRevocableBytes()</span> &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">    blockedNodes++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>getReservedRevocableBytes 这个是用于获取spill到磁盘的内存，目前我们集群是不允许内存Spill到磁盘的，因为Presto面向的是ad-hoc场景，要求是快，如果说需要spill到磁盘，那spark是一个更好的选择，且早期版本Presto spill到磁盘之前测试过稳定性比较差，场景也比较少。</p><p>所以就判断GENERAL_POOL里是否还有剩余内存，如果小于等于0，那就表示该节点是个Block状态。</p><a id="more"></a><h2 id="Kill策略："><a href="#Kill策略：" class="headerlink" title="Kill策略："></a>Kill策略：</h2><p>把所有query遍历一遍，如果查询有RESOURCE_OVERCOMMIT标志，且内存溢出了，那就把带有此标志的SQL Kill掉。</p><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (resourceOvercommit &amp;&amp; outOfMemory) &#123;</span><br><span class="line">    <span class="comment">// If a query has requested resource overcommit, only kill it if the cluster has run out of memory</span></span><br><span class="line">    DataSize <span class="keyword">memory</span> = succinctBytes(getQueryMemoryReservation(<span class="keyword">query</span>));</span><br><span class="line">    <span class="keyword">query</span>.fail(new PrestoException(CLUSTER_OUT_OF_MEMORY,</span><br><span class="line">       <span class="keyword">format</span>(<span class="string">&quot;The cluster is out of memory and %s=true, so this query was killed. It was using %s of memory&quot;</span>, RESOURCE_OVERCOMMIT, <span class="keyword">memory</span>)));</span><br><span class="line">    queryKilled = true;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>如果没有RESOURCE_OVERCOMMIT标志，那就看看内存是否超过集群允许的内存：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!resourceOvercommit) &#123;</span><br><span class="line">    long userMemoryLimit = min(maxQueryMemory.<span class="keyword">to</span><span class="constructor">Bytes()</span>, get<span class="constructor">QueryMaxMemory(<span class="params">query</span>.<span class="params">getSession</span>()</span>).<span class="keyword">to</span><span class="constructor">Bytes()</span>);</span><br><span class="line">    <span class="keyword">if</span> (userMemoryReservation &gt; userMemoryLimit) &#123;</span><br><span class="line">        query.fail(exceeded<span class="constructor">GlobalUserLimit(<span class="params">succinctBytes</span>(<span class="params">userMemoryLimit</span>)</span>));</span><br><span class="line">        queryKilled = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// enforce global total memory limit if system pool is disabled</span></span><br><span class="line">    long totalMemoryLimit = min(maxQueryTotalMemory.<span class="keyword">to</span><span class="constructor">Bytes()</span>, get<span class="constructor">QueryMaxTotalMemory(<span class="params">query</span>.<span class="params">getSession</span>()</span>).<span class="keyword">to</span><span class="constructor">Bytes()</span>);</span><br><span class="line">    <span class="keyword">if</span> (!isLegacySystemPoolEnabled<span class="operator"> &amp;&amp; </span>totalMemoryReservation &gt; totalMemoryLimit) &#123;</span><br><span class="line">        query.fail(exceeded<span class="constructor">GlobalTotalLimit(<span class="params">succinctBytes</span>(<span class="params">totalMemoryLimit</span>)</span>));</span><br><span class="line">        queryKilled = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>由上面我们可以知道，想让上述配置生效，query.max-memory需要配置合理的值，比如我们有5台Worker，每台Worker最大允许的内存为10G（超过10GB，每台Worker会自动Kill掉SQL），那此集群允许的最大内存为5*10G = 50GB，如果你配置query.max-memory（maxQueryMemory）100GB，那上面的逻辑将不会走，导致此逻辑失效。</p><p>同时，如果发现集群有节点已经OOM了，但是过了5S，依然没有SQL被Kill掉，那就会使用以下触发Presto SQL Kill策略，一共2种策略，query.low-memory-killer.policy指定：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">LowMemoryKillerPolicy</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">String</span> NONE = <span class="string">&quot;none&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">String</span> TOTAL_RESERVATION = <span class="string">&quot;total-reservation&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">String</span> TOTAL_RESERVATION_ON_BLOCKED_NODES = <span class="string">&quot;total-reservation-on-blocked-nodes&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>total-reservation表示杀掉集群中占有内存最大的SQL，total-reservation-on-blocked-nodes表示杀死在内存不足（阻塞）的节点上使用最多内存的查询。</p><p>假如我们通过RESOURCE_GROUP限制集群的并发大小为10，单个节点最大内存为10，集群允许的最大内存少于100GB，那此配置集群肯定不会出现OOM现象（不考虑内存泄露问题），但是这样的话，会导致资源利用率不够，所以线上一般会配置并发数 * 单机最大内存远大于集群允许的最大内存，这就指望LowMemoryKillerPolicy能帮助我们Kill掉内存占用最大的SQL。但是实际上依然会出现Worker OOM情况，此时一般思路：</p><p>1、dmesg -T查看系统日志，确定是操作系统触发的OOM还是JVM OOM，如果是操作系统触发的OOM，可能堆外内存或Presto内存泄露会导致这个问题，但是更多情况下需要确定下Presto节点是否有其他服务，一般情况下是其他服务内存突然增大，导致系统内存不够。</p><p>2、配置是否正确，是否限制了单机最大内存，是否限制了集群最大内存且小于单机最大内存 * 集群节点数，是否配置了LowMemoryKillerPolicy。</p><p>3、如果上面配置都正确，需要考虑2个问题，1）什么原因没有触发LowMemoryKillerPolicy，可以将判断节点阻塞（内存不足）的逻辑变的更严格点，因为不是个实时过程。2）结点是否有内存泄露情况。</p><p>从上面可以看出Presto内存管理比较粗暴，内存不够就杀掉SQL，而其他一些引擎做法就比较细腻些，SQL会一直Block住，直到有可用内存为止，不过Presto是面向ad-hoc场景，这种内存管理方法更简单有效吧。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;为了防止集群里节点OOM，Presto有个循环线程来获取当前集群节点和集群整体内存占用情况。通过这篇文章：&lt;a href=&quot;http://armsword.com/2019/11/13/the-configuration-settings-of-presto-memory-management/&quot;&gt;Presto内存管理相关参数设置&lt;/a&gt;  我们知道Presto里分为RESERVED_POOL和GENERAL_POOL。&lt;/p&gt;
&lt;h2 id=&quot;判断节点是否阻塞（内存不足）：&quot;&gt;&lt;a href=&quot;#判断节点是否阻塞（内存不足）：&quot; class=&quot;headerlink&quot; title=&quot;判断节点是否阻塞（内存不足）：&quot;&gt;&lt;/a&gt;判断节点是否阻塞（内存不足）：&lt;/h2&gt;&lt;p&gt;如果使用RESERVED_POOL（意思是说最大SQL使用这个POOL），那判断集群内存超出内存的方法就是：&lt;/p&gt;
&lt;p&gt;1、RESERVED_POOL内存被SQL占用了&lt;/p&gt;
&lt;p&gt;2、GENERAL_POOL里有被阻塞的Node&lt;/p&gt;
&lt;p&gt;因为RESERVED_POOL会导致内存浪费，我们集群配置参数没有使用这个POOL，只使用了GENERAL_POOL，所以只需要查看下GENERAL_POOL是怎么判断节点是否Block住的。&lt;/p&gt;
&lt;figure class=&quot;highlight reasonml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (poolInfo.get&lt;span class=&quot;constructor&quot;&gt;FreeBytes()&lt;/span&gt; + poolInfo.get&lt;span class=&quot;constructor&quot;&gt;ReservedRevocableBytes()&lt;/span&gt; &amp;lt;= &lt;span class=&quot;number&quot;&gt;0&lt;/span&gt;) &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    blockedNodes++;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;getReservedRevocableBytes 这个是用于获取spill到磁盘的内存，目前我们集群是不允许内存Spill到磁盘的，因为Presto面向的是ad-hoc场景，要求是快，如果说需要spill到磁盘，那spark是一个更好的选择，且早期版本Presto spill到磁盘之前测试过稳定性比较差，场景也比较少。&lt;/p&gt;
&lt;p&gt;所以就判断GENERAL_POOL里是否还有剩余内存，如果小于等于0，那就表示该节点是个Block状态。&lt;/p&gt;</summary>
    
    
    
    
    <category term="Presto" scheme="http://armsword.com/tags/Presto/"/>
    
  </entry>
  
  <entry>
    <title>Presto内存管理相关参数设置</title>
    <link href="http://armsword.com/2019/11/13/the-configuration-settings-of-presto-memory-management/"/>
    <id>http://armsword.com/2019/11/13/the-configuration-settings-of-presto-memory-management/</id>
    <published>2019-11-13T09:47:24.000Z</published>
    <updated>2020-11-03T07:37:50.316Z</updated>
    
    <content type="html"><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>之前介绍过Presto内存管理和分配策略，但是那个是0.192版本，详细见：<a href="http://armsword.com/2018/05/22/the-memory-management-and-tuning-experience-of-presto/">Presto内存管理原理和调优</a> ，0.201之后内存管理作了新的修改，所以重新简单分析下，然后给出一个配置模板，希望对使用Presto的同学有帮助。</p><h2 id="两种内存"><a href="#两种内存" class="headerlink" title="两种内存"></a>两种内存</h2><p>Presto里面内存只有2种内存，一种是user memory，另一种是system memory。system memory用于input/output/exchange buffers等，user memory 用于hash join、agg这些。</p><h2 id="内存池"><a href="#内存池" class="headerlink" title="内存池"></a>内存池</h2><p>0.201之前有3种内存POOL，分别是GENERAL_POOL、RESERVED_POOL及SYSTEM_POOL。但是0.201之后，默认SYSTEM_POOL是不开启的，以下参数控制，默认值为false</p><figure class="highlight ceylon"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">deprecated</span>.legacy-system-pool-enabled</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>那SYSTEM_POOL不使用了，这块内存怎么控制呢，去代码里确认了下：</p><a id="more"></a><p><code>SqlTaskManager#createQueryContext</code></p><figure class="highlight autohotkey"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">private QueryContext createQueryContext(</span><br><span class="line">            QueryId queryId,</span><br><span class="line">            LocalMemoryManager localMemoryManager,</span><br><span class="line">            NodeMemoryConfig nodeMemoryConfig,</span><br><span class="line">            LocalSpillManager localSpillManager,</span><br><span class="line">            GcMonitor gcMonitor,</span><br><span class="line">            DataSize maxQueryUserMemoryPerNode,</span><br><span class="line">            DataSize maxQueryTotalMemoryPerNode,</span><br><span class="line">            DataSize maxQuerySpillPerNode)</span><br><span class="line">    &#123;</span><br><span class="line">        if (nodeMemoryConfig.isLegacySystemPoolEnabled()) &#123;</span><br><span class="line">            Optional&lt;MemoryPool&gt; systemPool = localMemoryManager.getSystemPool()<span class="comment">;</span></span><br><span class="line">            verify(systemPool.isPresent(), <span class="string">&quot;systemPool must be present&quot;</span>)<span class="comment">;</span></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> LegacyQueryContext(</span><br><span class="line"><span class="built_in">                    queryId,</span></span><br><span class="line"><span class="built_in">                    maxQueryUserMemoryPerNode,</span></span><br><span class="line">                    localMemoryManager.getGeneralPool(),</span><br><span class="line">                    systemPool.get(),</span><br><span class="line"><span class="built_in">                    gcMonitor,</span></span><br><span class="line"><span class="built_in">                    taskNotificationExecutor,</span></span><br><span class="line"><span class="built_in">                    driverYieldExecutor,</span></span><br><span class="line"><span class="built_in">                    maxQuerySpillPerNode,</span></span><br><span class="line">                    localSpillManager.getSpillSpaceTracker())<span class="comment">;</span></span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> DefaultQueryContext(</span><br><span class="line"><span class="built_in">                queryId,</span></span><br><span class="line"><span class="built_in">                maxQueryUserMemoryPerNode,</span></span><br><span class="line"><span class="built_in">                maxQueryTotalMemoryPerNode,</span></span><br><span class="line">                localMemoryManager.getGeneralPool(),</span><br><span class="line"><span class="built_in">                gcMonitor,</span></span><br><span class="line"><span class="built_in">                taskNotificationExecutor,</span></span><br><span class="line"><span class="built_in">                driverYieldExecutor,</span></span><br><span class="line"><span class="built_in">                maxQuerySpillPerNode,</span></span><br><span class="line">                localSpillManager.getSpillSpaceTracker())<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>可以看到systemPool.get()被替换为了localMemoryManager.getGeneralPool()，所以GENERAL_POOL扮演了之前GENERAL_POOL及SYSTEM_POOL的作用，提供user memory和system memory。</p><p>说完SYSTEM_POOL，再说下RESERVED_POOL，RESERVED_POOL作用就是当GENERAL_POOL满的时候，将最占用内存的SQL分配到这块内存上来，但是实际使用时，这块内存很少会被使用，原因是，<br>一般使用Presto的业务是用来SQL提速的，不会使用spill disk功能，第二为了服务的稳定性，会限制最大内存和Kill内存策略，所以会出现查询没有被分配到RESERVED_POOL之前，<br>SQL将会被系统Kill掉。第三是大查询一般会用Spark/Hive替代，所以这块RESERVED_POOL就会浪费掉了，如下图所示：</p><img src="/img/2019/11/01.png" width="800" align=left /><p>目前有9个SQL正在查询，都在使用GENERAL_POOL，RESERVED_POOL将会浪费掉。所以对于绝大部分Presto使用者，禁止掉RESERVED_POOL是正确的选择。</p><h3 id="新的参数："><a href="#新的参数：" class="headerlink" title="新的参数："></a>新的参数：</h3><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">memory.heap-headroom-per-<span class="keyword">node</span><span class="title"></span></span><br></pre></td></tr></table></figure><p>这个内存主要是第三方库的内存分配，无法被统计跟踪，默认值是XMX*0.3。</p><p>所以，Presto内存分配只需要考虑GENERAL_POOL及heap-headroom-per-node。</p><h2 id="分配方法"><a href="#分配方法" class="headerlink" title="分配方法"></a>分配方法</h2><p>既然Presto是一个纯内存OLAP引擎，那肯定不能让查询占用的内存无限大，需要参数控制SQL占用的最大内存，Presto内存管理包括了单机和集群粒度的内存，分别是:</p><ul><li>query.max-memory-per-node: 单个Query在单个Worker上允许的最大user memory</li><li>query.max-total-memory-per-node: 单个Query在单个Worker上允许的最大user memory + system memory</li><li>query.max-memory: 单个Query在整个集群上允许的最大user memory</li><li>query.max-total-memory: 单个Query在整个集群上允许占用的最大user + system memory</li></ul><p>而集群query.max-memory由query.initial-hash-partitions控制，现在默认值为100，但是这个值受限于Worker个数，即值为min(100,Worker Nums)。老版本的默认值为8, 所以我一般还是用8来计算的，那query.max-memroy 一般稍小于query.max-memory-per-node*8即可（考虑到数据倾斜）。</p><p>根据代码和上面分析知道：</p><p>RESERVED_POOL = query.max-total-memory-per-node</p><p>所以新的<br>GENERAL_POOL = TotalMemory - ReservedPool - memory.heap-headroom-per-node</p><p>禁止掉RESERVED_POOL时：</p><p>GENERAL_POOL= TotalMemory  - memory.heap-headroom-per-node</p><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>综上，Presto合适的内存分配配置为如下，假如-Xmx80G，Worker数大于8台：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">query</span>.max-memory=<span class="number">120</span>GB  # 默认为<span class="number">20</span>GB，query.max-memory-per-node * <span class="number">8</span> * <span class="number">0</span>.<span class="number">8</span> ，倾斜按照<span class="number">0</span>.<span class="number">8</span>算即可</span><br><span class="line"><span class="attribute">query</span>.max-memory-per-node=<span class="number">20</span>GB   # 默认值<span class="number">0</span>.<span class="number">1</span> * XMX，一般线上按照<span class="number">0</span>.<span class="number">25</span>*XMX算，这个最好结合自己并发，如果并发大，大查询多，值最好小一点，稳定性考虑</span><br><span class="line"><span class="attribute">query</span>.max-total-memory-per-node=<span class="number">32</span>GB  # 默认值<span class="number">0</span>.<span class="number">3</span>*XMX，一般设置为<span class="number">0</span>.<span class="number">4</span>*XMX</span><br><span class="line"><span class="attribute">experimental</span>.reserved-pool-enabled=false  # 不使用RESERVED_POOL</span><br><span class="line"><span class="attribute">memory</span>.heap-headroom-per-node=<span class="number">16</span>GB  # 默认<span class="number">0</span>.<span class="number">3</span>*XMX，一般使用的是<span class="number">0</span>.<span class="number">2</span>*XMX</span><br><span class="line"><span class="attribute">query</span>.low-memory-killer.policy=total-reservation-<span class="literal">on</span>-blocked-nodes  # Kill策略，干掉Blocked的最大查询，GENERAL_POOL满了，防止OOM</span><br></pre></td></tr></table></figure><p>比如上面分配后，GENERAL_POOL为80GB - 16GB = 64GB，如下图所示，RESERVED_POOL不再存在。query.max-total-memory默认值为2*query.max-memory，这个参数可以保持默认值即可。</p><img src="/img/2019/11/02.png" width="800" align=left />]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h2&gt;&lt;p&gt;之前介绍过Presto内存管理和分配策略，但是那个是0.192版本，详细见：&lt;a href=&quot;http://armsword.com/2018/05/22/the-memory-management-and-tuning-experience-of-presto/&quot;&gt;Presto内存管理原理和调优&lt;/a&gt; ，0.201之后内存管理作了新的修改，所以重新简单分析下，然后给出一个配置模板，希望对使用Presto的同学有帮助。&lt;/p&gt;
&lt;h2 id=&quot;两种内存&quot;&gt;&lt;a href=&quot;#两种内存&quot; class=&quot;headerlink&quot; title=&quot;两种内存&quot;&gt;&lt;/a&gt;两种内存&lt;/h2&gt;&lt;p&gt;Presto里面内存只有2种内存，一种是user memory，另一种是system memory。system memory用于input/output/exchange buffers等，user memory 用于hash join、agg这些。&lt;/p&gt;
&lt;h2 id=&quot;内存池&quot;&gt;&lt;a href=&quot;#内存池&quot; class=&quot;headerlink&quot; title=&quot;内存池&quot;&gt;&lt;/a&gt;内存池&lt;/h2&gt;&lt;p&gt;0.201之前有3种内存POOL，分别是GENERAL_POOL、RESERVED_POOL及SYSTEM_POOL。但是0.201之后，默认SYSTEM_POOL是不开启的，以下参数控制，默认值为false&lt;/p&gt;
&lt;figure class=&quot;highlight ceylon&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;deprecated&lt;/span&gt;.legacy-system-pool-enabled&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;那SYSTEM_POOL不使用了，这块内存怎么控制呢，去代码里确认了下：&lt;/p&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>Presto ORC及其性能优化</title>
    <link href="http://armsword.com/2019/09/30/presto-orc-and-performance-optimization/"/>
    <id>http://armsword.com/2019/09/30/presto-orc-and-performance-optimization/</id>
    <published>2019-09-30T07:06:04.000Z</published>
    <updated>2020-11-03T07:37:50.313Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>ORC的全称是(Optimized Row Columnar)，其是为了加速Hive查询以及节省Hadoop磁盘空间而生的，其使用列式存储，支持多种文件压缩方式。由于其被广泛应用在Hadoop系统中，Presto 0.77版本在Hive Connector里实现了ORC Reader。</p><h2 id="ORC文件结构"><a href="#ORC文件结构" class="headerlink" title="ORC文件结构"></a>ORC文件结构</h2><img src="/img/2019/09/01.jpg" width="800" align=left /><p>上图（图1）来自网络，有很多文章通过这张图片介绍了ORC文件结构，我这里就不多啰嗦了，我们直接通过数据来看文件格式吧。</p><a id="more"></a><h3 id="创建表："><a href="#创建表：" class="headerlink" title="创建表："></a>创建表：</h3><img src="/img/2019/09/07.png" width="800" align=left /><h3 id="插入数据："><a href="#插入数据：" class="headerlink" title="插入数据："></a>插入数据：</h3><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">insert</span> <span class="selector-tag">into</span> <span class="selector-tag">orc</span>(id,name) <span class="selector-tag">values</span>(<span class="number">1</span>,<span class="string">&#x27;a&#x27;</span>),(<span class="number">2</span>,<span class="string">&#x27;b&#x27;</span>),(<span class="number">3</span>,<span class="string">&#x27;c&#x27;</span>),(<span class="number">4</span>,<span class="string">&#x27;d&#x27;</span>),(<span class="number">5</span>,<span class="string">&#x27;e&#x27;</span>),(<span class="number">6</span>,<span class="string">&#x27;f&#x27;</span>),(<span class="number">7</span>,null);</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>注：我们想只生成一个文件，所以一次插入了7条数据，否则会生成多个文件，不利于我们后续分析。</p><h3 id="Dump数据："><a href="#Dump数据：" class="headerlink" title="Dump数据："></a>Dump数据：</h3><p>使用Hive自带的ORC DUMP工具，命令：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">.<span class="regexp">/bin/</span>hive --orcfiledump hdfs:<span class="regexp">//</span>localhost:<span class="number">9000</span><span class="regexp">/user/</span>hive<span class="regexp">/warehouse/</span>orc/<span class="number">000000</span>_0</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>数据格式：<br><img src="/img/2019/09/08.png" width="800" align=left /></p><p>上图由回车划分了5个模块，第五个模块表示该ORC文件数据大小，我们不需要关心。</p><ul><li>模块1<br>对应图1里面的PostScript，包含压缩类型及表结构信息</li><li>模块2<br>Stripe Statistics对应图1里面的Stripe Footer，Stripe粒度的索引，可以看到其包含3列，Column 1 对应<code>id</code>字段，包含其最大值和最小值以及包含其sum总和，其中也有个<code>hasNull</code>标记，用于标记是否含有NULL，此字段可用于SQL里带有 <code>is null</code> 的优化，Column 2对应<code>name</code>字段，与Column 1相似，但是sum表示非NULL值的行数。注意此处只含有1个Stripe，默认为10000，参数hive.exec.orc.default.row.index.stride可控制其大小，简单测试了下，发现此值最小为1000，否则生成MR出错，具体需要代码里再确认下。Column 0只统计其count值，可以忽略。</li><li>模块3<br>对应图1里面的File Footer，与Stripe Footer相似，但是是文件级别的索引。</li><li>模块4<br>stripe详细信息，就是真实列数据块，其中又分为Index data（记录每列的索引信息），Raw Data（记录原始数据），Index data可以根据自身业务特点做些性能调优，比如实现布隆过滤器索引（Hive 1.2实现）。Raw Data是通过row group保存的，其实可以简单的认为就是默认按照10000将原始数据划分更小的块，每一个row group由多个stream保存数据和索引信息。每一个stream的数据会根据该列的类型使用特定的压缩算法保存。在ORC中存在如下几种stream类型：<ul><li>PRESENT：每一个成员值在这个stream中保持一位（bit）用于标识该值是否为NULL</li><li>DATA：当前stripe的成员值，真实数据</li><li>LENGTH：每一个成员的长度，string类型特有，否则你不知道每个string的长度</li><li>DICTIONARY_DATA：对string类型数据编码之后字典的内容</li><li>SECONDARY：存储Decimal、timestamp类型的小数或者纳秒数等</li></ul></li></ul><p>通过模块4，我们可以看到<code>id</code>含列有DATA Stream，而<code>name</code>含有PRESENT、DATA、LENGTH Stream，因为存在空值，所以多了个PRESENT Stream。</p><h2 id="Presto-ORC及优化"><a href="#Presto-ORC及优化" class="headerlink" title="Presto ORC及优化"></a>Presto ORC及优化</h2><p>Presto在实现ORC时，Hive-based ORC reader维护的数据是行式的，Presto想使用官方提供的客户端时还需要将行数据转换为列数据，且当时不支持Predicate pushdown，所以索性Presto自己实现ORC Reader，不过Hive 0.13也实现了VectorizedOrcRecordReader提供列格式。</p><h3 id="Predicate-pushdown"><a href="#Predicate-pushdown" class="headerlink" title="Predicate pushdown"></a>Predicate pushdown</h3><p>由ORC文件格式分析，ORC在每个文件中提供三级索引：</p><ul><li>文件级别，整个文件级别的统计信息</li><li>stripe级别，每个stripe每列中的值的统计信息</li><li>行级别 (行组)，stripe中每组10000行（默认值）的每列值的统计信息</li></ul><p>假如查询过滤条件为WHERE id = 1，首先从文件的统计信息（一级索引）中看看id字段的min/max值，如果1不包含在内，那么跳过该文件；如果在这个文件中，那么继续查看二级索引，每个stripe中id字段的min/max值，如果1不包含在内，那么跳过此stripe；如果在该stripe中，则继续匹配row group中的min/max值（三级索引），如果1不包含在内，那么跳过该row group。如果1包含在内min和max范围内，则利用布隆过滤器再次判断是否一定不在内，不在内则继续跳过该行组。其原理就是通过三级索引，将查询范围缩小到10000行的集合，而原始数据是列式存储，更加适合CPU pipeline的编码方式，有效利用这种局部性，缓存可以达到极高的命中率，所以ORC有非常高效的性能。</p><h3 id="Lazy-reads"><a href="#Lazy-reads" class="headerlink" title="Lazy reads"></a>Lazy reads</h3><p>以SQL为例，<code>SELECT a, b FROM ... WHERE a = ...</code>，如果a不匹配，那么将不会读取b的列。</p><h3 id="Bulk-reads"><a href="#Bulk-reads" class="headerlink" title="Bulk reads"></a>Bulk reads</h3><p>Presto老版本ORC Reader代码可以简化为以下逻辑：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (dataStream<span class="operator"> == </span>null) &#123;</span><br><span class="line">    presentStream.skip(nextBatchSize);</span><br><span class="line">    return <span class="module-access"><span class="module"><span class="identifier">RunLengthEncodedBlock</span>.</span></span>create(<span class="keyword">type</span>, null, nextBatchSize);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">BlockBuilder builder = <span class="keyword">type</span>.create<span class="constructor">BlockBuilder(<span class="params">null</span>, <span class="params">nextBatchSize</span>)</span>;</span><br><span class="line"><span class="keyword">if</span> (presentStream<span class="operator"> == </span>null) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nextBatchSize; i++) &#123;</span><br><span class="line">        <span class="keyword">type</span>.write<span class="constructor">Long(<span class="params">builder</span>, <span class="params">dataStream</span>.<span class="params">next</span>()</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nextBatchSize; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (presentStream.next<span class="constructor">Bit()</span>) &#123;</span><br><span class="line">            <span class="keyword">type</span>.write<span class="constructor">Long(<span class="params">builder</span>, <span class="params">dataStream</span>.<span class="params">next</span>()</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            builder.append<span class="constructor">Null()</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">return builder.build<span class="literal">()</span>;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>比如float及double的datatStream.next()实现为：</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">float</span> next()</span><br><span class="line">        <span class="keyword">throws</span> IOException</span><br><span class="line">&#123;</span><br><span class="line">    input.readFully(buffer, <span class="number">0</span>, SIZE_OF_FLOAT);</span><br><span class="line">    <span class="function"><span class="keyword">return</span> slice.<span class="title">getFloat</span><span class="params">(<span class="number">0</span>)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>一次只读取一个值，将其改为按照Bulk loading（比如8*SIZE_OF_FLOAT），读取性能有明显提升。<br>而对于Boolean reader，之前一次处理 1 bit数据（但是读取按照Byte），优化点是将其改为一次处理 8 bit（1 Byte）。</p><h3 id="Improve-null-reading"><a href="#Improve-null-reading" class="headerlink" title="Improve null reading"></a>Improve null reading</h3><p>从上面的代码可以看到，当有PRESENT Stream时（就是存在null值时），还要每次处理PRESENT Stream，读取DATA Stream及 PRESENT Stream，CPU Cache利用率很低，所以上面代码改为了下面形式：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">// bulk <span class="keyword">read</span> <span class="keyword">and</span> count <span class="keyword">null</span> <span class="keyword">values</span></span><br><span class="line"><span class="type">boolean</span>[] <span class="keyword">isNull</span> = <span class="built_in">new</span> <span class="type">boolean</span>[nextBatchSize];</span><br><span class="line"><span class="type">int</span> nullCount = presentStream.getUnsetBits(nextBatchSize, <span class="keyword">isNull</span>);</span><br><span class="line"></span><br><span class="line">// bulk <span class="keyword">read</span> non-<span class="keyword">values</span> <span class="keyword">into</span> a <span class="keyword">temporary</span> <span class="keyword">array</span></span><br><span class="line">dataStream.next(tempBuffer, nextBatchSize - nullCount);</span><br><span class="line"></span><br><span class="line">// <span class="keyword">copy</span> <span class="keyword">values</span> <span class="keyword">into</span> result</span><br><span class="line">long[] result = <span class="built_in">new</span> long[<span class="keyword">isNull</span>.length];</span><br><span class="line"><span class="type">int</span> position = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="keyword">isNull</span>.length; i++) &#123;</span><br><span class="line">    result[i] = tempBuffer[position];</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">isNull</span>[i]) &#123;</span><br><span class="line">        position++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>先将数据读取临时文件里，然后依次处理。</p><h3 id="Avoid-dynamic-dispatch-in-loops"><a href="#Avoid-dynamic-dispatch-in-loops" class="headerlink" title="Avoid dynamic dispatch in loops"></a>Avoid dynamic dispatch in loops</h3><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nextBatchSize; i++) &#123;</span><br><span class="line">    <span class="keyword">type</span>.write<span class="constructor">Long(<span class="params">builder</span>, <span class="params">dataStream</span>.<span class="params">next</span>()</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>很多Stream Reader只包含一种type，但是LongStreamReader会包含BIGINT、INTEGER、SMALLINT、TINYINT 及 DATE Types。这会让JVM的一些优化失效，比如inline，改动为：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">type</span> instanceof BigintType) &#123;</span><br><span class="line">    BlockBuilder builder = <span class="keyword">type</span>.create<span class="constructor">BlockBuilder(<span class="params">null</span>, <span class="params">nextBatchSize</span>)</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nextBatchSize; i++) &#123;</span><br><span class="line">        <span class="keyword">type</span>.write<span class="constructor">Long(<span class="params">builder</span>, <span class="params">dataStream</span>.<span class="params">next</span>()</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    return builder.build<span class="literal">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">type</span> instanceof IntegerType) &#123;</span><br><span class="line">    BlockBuilder builder = <span class="keyword">type</span>.create<span class="constructor">BlockBuilder(<span class="params">null</span>, <span class="params">nextBatchSize</span>)</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nextBatchSize; i++) &#123;</span><br><span class="line">        <span class="keyword">type</span>.write<span class="constructor">Long(<span class="params">builder</span>, <span class="params">dataStream</span>.<span class="params">next</span>()</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    return builder.build<span class="literal">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>因为早期Hive ORC Reader的一些特性，导致Presto自己实现了ORC Reader，但是现在来看，直接调用社区的ORC Reader效果会更好，因为Presto基本上每2、3个小版本就会修复ORC Bug或者做些简单的性能提升，但是代码里很多都是来源于社区ORC的代码，Presto社区整体进展缓慢，直接调用社区ORC接口，省下了优化和修复Bug的时间，剩下的时间做些Presto引擎更核心的事情应该才是正确的做法。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://orc.apache.org/specification/ORCv1/">https://orc.apache.org/specification/ORCv1/</a></li><li><a href="https://prestosql.io/blog/2019/04/23/even-faster-orc.html">https://prestosql.io/blog/2019/04/23/even-faster-orc.html</a></li></ul>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;ORC的全称是(Optimized Row Columnar)，其是为了加速Hive查询以及节省Hadoop磁盘空间而生的，其使用列式存储，支持多种文件压缩方式。由于其被广泛应用在Hadoop系统中，Presto 0.77版本在Hive Connector里实现了ORC Reader。&lt;/p&gt;
&lt;h2 id=&quot;ORC文件结构&quot;&gt;&lt;a href=&quot;#ORC文件结构&quot; class=&quot;headerlink&quot; title=&quot;ORC文件结构&quot;&gt;&lt;/a&gt;ORC文件结构&lt;/h2&gt;&lt;img src=&quot;/img/2019/09/01.jpg&quot; width=&quot;800&quot; align=left /&gt;

&lt;p&gt;上图（图1）来自网络，有很多文章通过这张图片介绍了ORC文件结构，我这里就不多啰嗦了，我们直接通过数据来看文件格式吧。&lt;/p&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>Presto System load过高问题调研</title>
    <link href="http://armsword.com/2019/09/18/solve-presto-system-load-too-high/"/>
    <id>http://armsword.com/2019/09/18/solve-presto-system-load-too-high/</id>
    <published>2019-09-18T11:04:09.000Z</published>
    <updated>2020-11-03T07:37:50.314Z</updated>
    
    <content type="html"><![CDATA[<h2 id="背景："><a href="#背景：" class="headerlink" title="背景："></a>背景：</h2><p>我们Presto有个集群，每6.5天会出现System load过高问题，这个集群有个特点，只服务于一个业务方，且SQL基本相似。如图所示：Sys load很高（20-40%），严重影响查询性能。<br><img src="/img/2019/09/01.png" width="800" align=left /></p><p>业务SQL查询时间表现为：</p><p>ScanFilterAndProjectOperator（Source Stage）阶段有机器有明显的长尾现象，比如20台机器，正常这个Operator执行时间只需要1S，但是有几台机器会耗时几分钟。而重启服务后，查询恢复正常。</p><a id="more"></a><p>先jstack看下Presto在干啥：<br><img src="/img/2019/09/02.png"><br>如图所示，Presto通过ScanFilterAndProjectOperator类执行filter过滤，其中这个filter方法是Codegen生成。</p><p>pstack查看下线程栈：<br><img src="/img/2019/09/03.png"></p><p>看到程序主要CPU浪费在Deoptimization::uncommon_trap里了。为什么呢？再说这个问题之前，我们先说下JIT。</p><h2 id="JIT："><a href="#JIT：" class="headerlink" title="JIT："></a>JIT：</h2><p>为了提高热点代码（Hot Spot Code）的执行效率，在运行时，虚拟机将会把这些代码编译成与本地平台相关的机器码，并进行各种层次的优化，完成这个任务的编译器称为即时编译器（JIT）。</p><p>“热点代码”两类:</p><ul><li>被多次调用的方法</li><li>被多次执行的循环体 – 尽管编译动作是由循环体所触发的，但编译器依然会以整个方法（而不是单独的循环体）作为编译对象。<br>编译器根据概率选择一些大多数时候都能提升运行速度的优化手段，当优化的假设不成立，出现“罕见陷阱”（Uncommon Trap）时可以通过逆优化（Deoptimization）退回到解释状态继续执行。</li></ul><p>如：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> <span class="function"><span class="title">test</span>(<span class="params"><span class="built_in">Object</span> input</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (input == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// do something</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果input一直不为空，执行1W次时，那么上述代码将优化为（类似GCC的 -O2优化算法）：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> <span class="function"><span class="title">test</span>(<span class="params"><span class="built_in">Object</span> input</span>)</span> &#123;</span><br><span class="line"><span class="comment">// do something</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是如果之后出现input为空，那么将会触发Uncommon Trap，通过逆优化（Deoptimization）退回到解释状态继续执行。所以触发逆优化是正常的，但是我们的问题是程序一直在做Uncommon Trap，做了很多无用功，这是不正常的。</p><p>查阅JVM源码 <a href="https://github.com/MuniyappanV/jdk-source-code/blob/master/jdk6u21_src/hotspot/src/share/vm/opto/compile.cpp#L2694">compile::too_many_recompiles</a>，发现有参数可以控制可编译次数：</p><figure class="highlight zephir"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">bool</span> Compile::too_many_recompiles(ciMethod* method,</span><br><span class="line">                                  <span class="keyword">int</span> bci,</span><br><span class="line">                                  Deoptimization::DeoptReason reason) &#123;</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// Pick a cutoff point well within PerBytecodeRecompilationCutoff.</span></span><br><span class="line">  <span class="keyword">uint</span> bc_cutoff = (<span class="keyword">uint</span>) PerBytecodeRecompilationCutoff / <span class="number">8</span>;</span><br><span class="line">  <span class="keyword">uint</span> m_cutoff  = (<span class="keyword">uint</span>) PerMethodRecompilationCutoff / <span class="number">2</span> + <span class="number">1</span>;  <span class="comment">// not zero</span></span><br><span class="line">  Deoptimization::DeoptReason per_bc_reason</span><br><span class="line">    = Deoptimization::reason_recorded_per_bytecode_if_any(reason);</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">if</span> ((per_bc_reason == Deoptimization::Reason_none</span><br><span class="line">       || md-&gt;has_trap_at(bci, reason) != <span class="number">0</span>)</span><br><span class="line">      <span class="comment">// The trap frequency measure we care about is the recompile count:</span></span><br><span class="line">      &amp;&amp; md-&gt;trap_recompiled_at(bci)</span><br><span class="line">      &amp;&amp; md-&gt;overflow_recompile_count() &gt;= bc_cutoff) &#123;</span><br><span class="line">    <span class="comment">// Do not emit a trap here if it has already caused recompilations.</span></span><br><span class="line">    <span class="comment">// Also, if there are multiple reasons, or if there is no per-BCI record,</span></span><br><span class="line">    <span class="comment">// assume the worst.</span></span><br><span class="line">    <span class="keyword">if</span> (log())</span><br><span class="line">      log()-&gt;elem(<span class="string">&quot;observe trap=&#x27;%s recompiled&#x27; count=&#x27;%d&#x27; recompiles2=&#x27;%d&#x27;&quot;</span>,</span><br><span class="line">                  Deoptimization::trap_reason_name(reason),</span><br><span class="line">                  md-&gt;trap_count(reason),</span><br><span class="line">                  md-&gt;overflow_recompile_count());</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (trap_count(reason) != <span class="number">0</span></span><br><span class="line">             &amp;&amp; decompile_count() &gt;= m_cutoff) &#123;</span><br><span class="line">    <span class="comment">// Too many recompiles globally, and we have seen this sort of trap.</span></span><br><span class="line">    <span class="comment">// Use cumulative decompile_count, not just md-&gt;decompile_count.</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> (log())</span><br><span class="line">      log()-&gt;elem(<span class="string">&quot;observe trap=&#x27;%s&#x27; count=&#x27;%d&#x27; mcount=&#x27;%d&#x27; decompiles=&#x27;%d&#x27; mdecompiles=&#x27;%d&#x27;&quot;</span>,</span><br><span class="line">                  Deoptimization::trap_reason_name(reason),</span><br><span class="line">                  md-&gt;trap_count(reason), trap_count(reason),</span><br><span class="line">                  md-&gt;decompile_count(), decompile_count());</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// The coast is clear.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>PerMethodRecompilationCutoff 可以控制recompiles次数，触发uncommon_trap时，且too_many_recompiles为true时，其行为为Deoptimization::Action_none。<br><a href="https://github.com/MuniyappanV/jdk-source-code/blob/master/jdk6u21_src/hotspot/src/share/vm/opto/graphKit.cpp#L1807">https://github.com/MuniyappanV/jdk-source-code/blob/master/jdk6u21_src/hotspot/src/share/vm/opto/graphKit.cpp#L1807</a></p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> GraphKit::uncommon_trap(<span class="keyword">int</span> trap_request,</span><br><span class="line">                             ciKlass* klass, <span class="keyword">const</span> char* comment,</span><br><span class="line">                             <span class="keyword">bool</span> must_throw,</span><br><span class="line">                             <span class="keyword">bool</span> keep_exact_action) &#123;</span><br><span class="line">  Deoptimization::DeoptReason reason = Deoptimization::trap_request_reason(trap_request);</span><br><span class="line">  Deoptimization::DeoptAction action = Deoptimization::trap_request_action(trap_request);</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">switch</span> (action) &#123;</span><br><span class="line">  <span class="keyword">case</span> Deoptimization::Action_maybe_recompile:</span><br><span class="line">  <span class="keyword">case</span> Deoptimization::Action_reinterpret:</span><br><span class="line">    <span class="comment">// Temporary fix for 6529811 to allow virtual calls to be sure they</span></span><br><span class="line">    <span class="comment">// get the chance to go from mono-&gt;bi-&gt;mega</span></span><br><span class="line">    <span class="keyword">if</span> (!keep_exact_action &amp;&amp;</span><br><span class="line">        Deoptimization::trap_request_index(trap_request) &lt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">        too_many_recompiles(reason)) &#123;</span><br><span class="line">      <span class="comment">// This BCI is causing too many recompilations.</span></span><br><span class="line">      action = Deoptimization::Action_none;</span><br><span class="line">      trap_request = Deoptimization::make_trap_request(reason, action);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      C-&gt;set_trap_can_recompile(<span class="literal">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">case</span> Deoptimization::Action_make_not_entrant:</span><br><span class="line">    C-&gt;set_trap_can_recompile(<span class="literal">true</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure><p>我们写个测试程序，测试PerMethodRecompilationCutoff 参数，参数超过recompile次数时性能表现：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">public <span class="keyword">class</span> DeoptimizationTest</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="constructor">DeoptimizationTest()</span> &#123;&#125;</span><br><span class="line">    static <span class="built_in">int</span> some<span class="constructor">ComplicatedFunction(<span class="params">double</span> <span class="params">x</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        return (<span class="built_in">int</span>) (<span class="module-access"><span class="module"><span class="identifier">Math</span>.</span></span>pow(x, x) + <span class="module-access"><span class="module"><span class="identifier">Math</span>.</span></span>log(x) + <span class="module-access"><span class="module"><span class="identifier">Math</span>.</span></span>sqrt(x));</span><br><span class="line">    &#125;</span><br><span class="line">    static void hot<span class="constructor">Method(<span class="params">int</span> <span class="params">iteration</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (iteration &lt; <span class="number">20</span>) &#123;</span><br><span class="line">            some<span class="constructor">ComplicatedFunction(1.23)</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (iteration &lt; <span class="number">40</span>) &#123;</span><br><span class="line">            some<span class="constructor">ComplicatedFunction(1.23)</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (iteration &lt; <span class="number">60</span>) &#123;</span><br><span class="line">            some<span class="constructor">ComplicatedFunction(1.23)</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (iteration &lt; <span class="number">80</span>) &#123;</span><br><span class="line">            some<span class="constructor">ComplicatedFunction(1.23)</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            some<span class="constructor">ComplicatedFunction(1.23)</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    static void hot<span class="constructor">MethodWrapper(<span class="params">int</span> <span class="params">iteration</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">int</span> count = <span class="number">100_000</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            hot<span class="constructor">Method(<span class="params">iteration</span>)</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    public static void main(String<span class="literal">[]</span> args)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> k = <span class="number">0</span>; k &lt; <span class="number">100</span>; k++) &#123;</span><br><span class="line">            long start = <span class="module-access"><span class="module"><span class="identifier">System</span>.</span></span>nano<span class="constructor">Time()</span>;</span><br><span class="line">            hot<span class="constructor">MethodWrapper(<span class="params">k</span> + 1)</span>;</span><br><span class="line">            <span class="module-access"><span class="module"><span class="identifier">System</span>.</span></span>out.println(<span class="string">&quot;iteration &quot;</span> + k + <span class="string">&quot;: &quot;</span> + (<span class="module-access"><span class="module"><span class="identifier">System</span>.</span></span>nano<span class="constructor">Time()</span> - start)<span class="operator"> / </span><span class="number">1_000_000</span> + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行代码：javac DeoptimizationTest.java &amp;&amp; java -XX:PerMethodRecompilationCutoff=3  DeoptimizationTest<br><img src="/img/2019/09/04.png" width="800" align=left /><br>指定PerMethodRecompilationCutoff为3，意思是PerMethodRecompilationCutoff/2 + 1 = 2，那么第三个分支将触发大量Deoptimization::Action_none操作，如上图所示，发现第59次迭代时，查询变慢了接近600倍。</p><p>火焰图见下图，可以看到耗时与我们Presto遇到的问题非常相似，耗时主要在Deoptimization::uncommon_trap：<br><img src="/img/2019/09/05.png"></p><h2 id="解决方案："><a href="#解决方案：" class="headerlink" title="解决方案："></a>解决方案：</h2><p>既然当达到阈值会触发Deoptimization::Action_none，那么最简单的解决方法是不让他达到阈值就好了。修改JVM参数：</p><p>-XX:PerMethodRecompilationCutoff<br>-XX:PerBytecodeRecompilationCutoff</p><p>这2个值尽量调大一些，比如2000和500以上。</p><p>线上我找了一台机器测试了一周，CPU表现为：</p><p><img src="/img/2019/09/06.png"></p><p>因为这个问题，应该是个JVM Bug，社区找了下关键词，发现JDK 11.0.6 好像解了这个问题，但是这个版本还没有release，无法验证，等版本发布后再验证下。</p><p><a href="https://bugs.java.com/bugdatabase/view_bug.do?bug_id=8227523">https://bugs.java.com/bugdatabase/view_bug.do?bug_id=8227523</a></p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://www.slideshare.net/dougqh/jvm-mechanics-when-does-the">https://www.slideshare.net/dougqh/jvm-mechanics-when-does-the</a></li><li>社区<a href="http://wsmithril-wiki.github.io/">wsmithril</a>网友提供的资料</li></ul>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;背景：&quot;&gt;&lt;a href=&quot;#背景：&quot; class=&quot;headerlink&quot; title=&quot;背景：&quot;&gt;&lt;/a&gt;背景：&lt;/h2&gt;&lt;p&gt;我们Presto有个集群，每6.5天会出现System load过高问题，这个集群有个特点，只服务于一个业务方，且SQL基本相似。如图所示：Sys load很高（20-40%），严重影响查询性能。&lt;br&gt;&lt;img src=&quot;/img/2019/09/01.png&quot; width=&quot;800&quot; align=left /&gt;&lt;/p&gt;
&lt;p&gt;业务SQL查询时间表现为：&lt;/p&gt;
&lt;p&gt;ScanFilterAndProjectOperator（Source Stage）阶段有机器有明显的长尾现象，比如20台机器，正常这个Operator执行时间只需要1S，但是有几台机器会耗时几分钟。而重启服务后，查询恢复正常。&lt;/p&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>Presto兼容Hive SQL的一些改造工作</title>
    <link href="http://armsword.com/2019/03/31/presto-compatible-hive-syntax/"/>
    <id>http://armsword.com/2019/03/31/presto-compatible-hive-syntax/</id>
    <published>2019-03-31T08:23:32.000Z</published>
    <updated>2020-11-03T07:37:50.314Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>Presto是一款优秀的分布式SQL查询引擎，适用于即席查询和报表分析等业务，其使用了ANSI SQL语法和语义，使用标准是SQL-92和SQL:2016。但是因为很多业务方一直使用Hive离线引擎来做SQL分析，而Hive使用类似SQL的语法（HQL）。为了使用户能平滑的将业务迁移到Presto上或者能让SQL同时跑到Presto及Hive引擎上，我们对Presto语法及一些算子等做了二次兼容开发，来最大限度降低用户迁移成本。接下来我们介绍下我们的主要兼容工作。</p><h2 id="一、权限认证"><a href="#一、权限认证" class="headerlink" title="一、权限认证"></a>一、权限认证</h2><p>Presto默认如果使用Password，那么必须使用Kerberos协议。因为公司离线数据没有Kerberos协议，所以我们改进了权限认证机制，与离线复用一套权限机制，用户只需要去离线申请权限，指定用户名和密码，即可查询Presto引擎和访问离线数据，并且提供了以下几种方式访问公司Presto，且与公司离线权限体系打通，分别为JDBC、Cli、Go、Python、R、NodeJS。</p><h2 id="二、行为修改"><a href="#二、行为修改" class="headerlink" title="二、行为修改"></a>二、行为修改</h2><h3 id="整数相除返回浮点数"><a href="#整数相除返回浮点数" class="headerlink" title="整数相除返回浮点数"></a>整数相除返回浮点数</h3><p>Presto整数相除沿用了Java整数相除的特性，我们需要修改函数算子行为，如IntegerOperators，将divide相关的函数修改为返回浮点数结果。比如：</p><a id="more"></a><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:<span class="keyword">default</span>&gt; select <span class="number">5</span>/<span class="number">2</span>;</span><br><span class="line"> _col0</span><br><span class="line"> -------</span><br><span class="line">    <span class="number">2.5</span></span><br><span class="line">(<span class="number">1</span> row)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="类型隐式转换"><a href="#类型隐式转换" class="headerlink" title="类型隐式转换"></a>类型隐式转换</h3><p>Presto类型比较与Java一样，使用的是强类型，即int与varchar是无法比较的，但是Hive是支持隐式转换的，所以这种隐式类型转换的问题，用户遇到的比较多。我们做了修改，在对应的算子里，添加了新的语义行为，事例如下：</p><figure class="highlight sqf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:<span class="keyword">default</span>&gt; <span class="built_in">select</span> <span class="string">&#x27;1&#x27;</span> &gt; <span class="number">2</span>;</span><br><span class="line"> <span class="variable">_col0</span></span><br><span class="line"> -------</span><br><span class="line">  <span class="literal">false</span></span><br><span class="line"> (<span class="number">1</span> row)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="过滤表达式隐式转换"><a href="#过滤表达式隐式转换" class="headerlink" title="过滤表达式隐式转换"></a>过滤表达式隐式转换</h3><p>大量的SQL，where条件里会带有比较语句，可能会出现int与varchar比较的场景，所以我们做了过滤表达式隐式转换，方法是在ExpressionAnalyzer里通过visitComparisonExpression来cast左右表达式为同一类型。</p><h3 id="Array-Functions兼容"><a href="#Array-Functions兼容" class="headerlink" title="Array Functions兼容"></a>Array Functions兼容</h3><p>主要与Hive保持兼容，做了如下修改：</p><ul><li>array index从0开始<figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:default&gt; select array[<span class="string">1,2,3</span>][<span class="symbol">0</span>];</span><br><span class="line"> <span class="emphasis">_col0</span></span><br><span class="line"><span class="emphasis"> -------</span></span><br><span class="line"><span class="emphasis">      1</span></span><br><span class="line"><span class="emphasis">(1 row)</span></span><br><span class="line"><span class="emphasis"></span></span><br></pre></td></tr></table></figure></li><li>超过下标最大值时返回NULL</li></ul><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:default&gt; select array[<span class="string">1,2,3</span>][<span class="symbol">4</span>];</span><br><span class="line"> <span class="emphasis">_col0</span></span><br><span class="line"><span class="emphasis"> -------</span></span><br><span class="line"><span class="emphasis">  NULL</span></span><br><span class="line"><span class="emphasis">(1 row)</span></span><br><span class="line"><span class="emphasis"></span></span><br></pre></td></tr></table></figure><h3 id="substr兼容"><a href="#substr兼容" class="headerlink" title="substr兼容"></a>substr兼容</h3><ul><li>下标从0开始</li></ul><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:<span class="keyword">default</span>&gt; select substr(<span class="string">&#x27;123&#x27;</span>,<span class="number">0</span>,<span class="number">2</span>);</span><br><span class="line">_col0</span><br><span class="line"> -------</span><br><span class="line">  <span class="number">12</span></span><br><span class="line">(<span class="number">1</span> row)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="in-操作符可包含字符串"><a href="#in-操作符可包含字符串" class="headerlink" title="in 操作符可包含字符串"></a>in 操作符可包含字符串</h3><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:<span class="literal">default</span>&gt; <span class="function"><span class="keyword">select</span> id <span class="keyword">from</span> test <span class="keyword">where</span> id <span class="title">in</span> (<span class="params"><span class="string">&#x27;1001&#x27;</span>,<span class="number">1002</span></span>) limit 1</span>;</span><br><span class="line">_col0</span><br><span class="line"> -------</span><br><span class="line">  <span class="number">1001</span></span><br><span class="line">(<span class="number">1</span> row)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="sum-支持字符串相加"><a href="#sum-支持字符串相加" class="headerlink" title="sum 支持字符串相加"></a>sum 支持字符串相加</h3><p>sum的参数包含字符串时，可进行计算, 即 select sum(id) from test; 如果id为varchar也可以进行计算了。</p><h3 id="JOIN条件隐式转换"><a href="#JOIN条件隐式转换" class="headerlink" title="JOIN条件隐式转换"></a>JOIN条件隐式转换</h3><p>实现了StatementRewrite子类，通过visit JOIN来获得左右表达式进行cast，因为一些财务业务涉及到大精度问题，所以将精度范围改为DECIMAL(38,10)。</p><h3 id="cast"><a href="#cast" class="headerlink" title="cast"></a>cast</h3><p>因为Presto字符串类型为varchar，Hive为String，所以支持了cast as string语法，事例：</p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">presto:<span class="keyword">default</span>&gt; select <span class="keyword">cast</span> (<span class="number">1</span> as <span class="built_in">string</span>);</span><br><span class="line"> _col0</span><br><span class="line"> -------</span><br><span class="line">  <span class="number">1</span></span><br><span class="line">(<span class="number">1</span> row)</span><br></pre></td></tr></table></figure><h2 id="三、新的语法支持"><a href="#三、新的语法支持" class="headerlink" title="三、新的语法支持"></a>三、新的语法支持</h2><h3 id="amp-操作符"><a href="#amp-操作符" class="headerlink" title="&amp; 操作符"></a>&amp; 操作符</h3><p>SqlBase.g4文件里添加新的 &amp; 语法，然后实现对应的Operator。事例为：</p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:<span class="keyword">default</span>&gt; select <span class="number">1</span> &amp; <span class="number">3</span>;</span><br><span class="line"> _col0</span><br><span class="line"> -------</span><br><span class="line">      <span class="number">1</span></span><br><span class="line">(<span class="number">1</span> row)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="rlike"><a href="#rlike" class="headerlink" title="rlike"></a>rlike</h3><p>与like相似，实现了rlike，比如查询select id from test rlike ‘xxx’; 目前还有部分功能需要完善。</p><h2 id="四、UDF"><a href="#四、UDF" class="headerlink" title="四、UDF"></a>四、UDF</h2><p>UDF除了添加了一些特殊业务的UDF外，比如isInPolygon多边形计算，安全函数等，还添加了一些常用的Hive UDF，分别为：</p><h4 id="字符串相关"><a href="#字符串相关" class="headerlink" title="字符串相关"></a>字符串相关</h4><ul><li>md5</li><li>concat_ws</li><li>nvl</li><li>json</li></ul><h3 id="日期相关"><a href="#日期相关" class="headerlink" title="日期相关"></a>日期相关</h3><ul><li>unix_timestamp</li><li>fromUtcTimestamp</li><li>toUtcTimestamp</li><li>dateSub</li><li>diffDate</li></ul><h3 id="int-bigint函数"><a href="#int-bigint函数" class="headerlink" title="int/bigint函数"></a>int/bigint函数</h3><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:<span class="keyword">default</span>&gt; select <span class="built_in">int</span>(<span class="number">2.1</span>);</span><br><span class="line"> _col0</span><br><span class="line"> -------</span><br><span class="line">      <span class="number">2</span></span><br><span class="line">(<span class="number">1</span> row)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>等等，具体想查看udf是否支持，可以直接操作连接Presto，如查询md5函数：</p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">presto:default&gt; select md5(<span class="emphasis">&#x27;1&#x27;</span>);</span><br><span class="line"><span class="emphasis">_col0</span></span><br><span class="line"><span class="emphasis">----------------------------------</span></span><br><span class="line"><span class="emphasis">c4ca4238a0b923820dcc509a6f75849b</span></span><br><span class="line"><span class="emphasis">(1 row)</span></span><br><span class="line"><span class="emphasis"></span></span><br></pre></td></tr></table></figure><p>有结果即表示支持该函数。</p><h2 id="五、其他兼容"><a href="#五、其他兼容" class="headerlink" title="五、其他兼容"></a>五、其他兼容</h2><h3 id="Hive视图"><a href="#Hive视图" class="headerlink" title="Hive视图"></a>Hive视图</h3><p>在visitTable里获得table信息，判断是否是视图，如果是视图，那么获取原始SQL查询，递归调用Analyze。</p><h3 id="插入数据临时文件目录"><a href="#插入数据临时文件目录" class="headerlink" title="插入数据临时文件目录"></a>插入数据临时文件目录</h3><p>由于涉及到权限问题，insert数据时，用户临时数据插入到临时目录会没有权限，所以修改了可配置临时文件插入目录参数。</p><h3 id="支持可读取Hive递归子分区"><a href="#支持可读取Hive递归子分区" class="headerlink" title="支持可读取Hive递归子分区"></a>支持可读取Hive递归子分区</h3><p>正常表存储格式为 /table/partition/file，这种数据是可以正常访问的，但是如果数据存储格式为 /table/partition/directory/file 时，Presto访问时返回数据为空。而Hive里面的解决方法为请求时添加以下参数：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="builtin-name">set</span> hive.mapred.supports.<span class="attribute">subdirectories</span>=<span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line"><span class="builtin-name">set</span> mapreduce.input.fileinputformat.input.dir.<span class="attribute">recursive</span>=<span class="literal">true</span>;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>我们设置了参数，让默认即可递归子目录，参数为：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">hive.recursive-directories</span>=<span class="literal">true</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="Key-not-present-in-map"><a href="#Key-not-present-in-map" class="headerlink" title="Key not present in map"></a>Key not present in map</h3><p>Presto在处理MAP时，当KEY 不存在时，会报错，而Hive返回NULL，通过Presto内部参数解决：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">deprecated.legacy-map-subscript</span>=<span class="literal">true</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>当然整体兼容Hive并不止这些，比如函数兼容类似Java里面的多态，需要找到合适的函数；还有least/greast等函数支持，还有一些函数null处理问题，太细节了，这里就不一一介绍了。由于Hive兼容是一个任重而道远的事情，这里还是推荐用户最好使用ANSI SQL以及管理好自己数据，这以后无论迁移还是使用，都会方便不少，减少不必要的麻烦。</p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;Presto是一款优秀的分布式SQL查询引擎，适用于即席查询和报表分析等业务，其使用了ANSI SQL语法和语义，使用标准是SQL-92和SQL:2016。但是因为很多业务方一直使用Hive离线引擎来做SQL分析，而Hive使用类似SQL的语法（HQL）。为了使用户能平滑的将业务迁移到Presto上或者能让SQL同时跑到Presto及Hive引擎上，我们对Presto语法及一些算子等做了二次兼容开发，来最大限度降低用户迁移成本。接下来我们介绍下我们的主要兼容工作。&lt;/p&gt;
&lt;h2 id=&quot;一、权限认证&quot;&gt;&lt;a href=&quot;#一、权限认证&quot; class=&quot;headerlink&quot; title=&quot;一、权限认证&quot;&gt;&lt;/a&gt;一、权限认证&lt;/h2&gt;&lt;p&gt;Presto默认如果使用Password，那么必须使用Kerberos协议。因为公司离线数据没有Kerberos协议，所以我们改进了权限认证机制，与离线复用一套权限机制，用户只需要去离线申请权限，指定用户名和密码，即可查询Presto引擎和访问离线数据，并且提供了以下几种方式访问公司Presto，且与公司离线权限体系打通，分别为JDBC、Cli、Go、Python、R、NodeJS。&lt;/p&gt;
&lt;h2 id=&quot;二、行为修改&quot;&gt;&lt;a href=&quot;#二、行为修改&quot; class=&quot;headerlink&quot; title=&quot;二、行为修改&quot;&gt;&lt;/a&gt;二、行为修改&lt;/h2&gt;&lt;h3 id=&quot;整数相除返回浮点数&quot;&gt;&lt;a href=&quot;#整数相除返回浮点数&quot; class=&quot;headerlink&quot; title=&quot;整数相除返回浮点数&quot;&gt;&lt;/a&gt;整数相除返回浮点数&lt;/h3&gt;&lt;p&gt;Presto整数相除沿用了Java整数相除的特性，我们需要修改函数算子行为，如IntegerOperators，将divide相关的函数修改为返回浮点数结果。比如：&lt;/p&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>Presto的一些基本概念</title>
    <link href="http://armsword.com/2018/08/11/the-basic-concepts-of-presto/"/>
    <id>http://armsword.com/2018/08/11/the-basic-concepts-of-presto/</id>
    <published>2018-08-11T14:42:32.000Z</published>
    <updated>2020-11-03T07:37:50.323Z</updated>
    
    <content type="html"><![CDATA[<h2 id="模型"><a href="#模型" class="headerlink" title="模型"></a>模型</h2><p>Presto 是 Facebook 开源的 MPP (Massive Parallel Processing) SQL 引擎，其理念来源于一个叫 Volcano 的并行数据库，该数据库提出了一个并行执行 SQL 的模型，核心思想就是 Operator Model 和 Iterator Model。</p><ul><li>Operator Model<br>即通过各种 Operator 组成一棵树，树的根节点负责结果输出，树的叶子节点是各种 TableScan。这棵树被称作 Plan（执行计划），数据库里又被细分为逻辑执行计划和物理执行计划。这棵树是由 SQL 经过词法、语法分析及语义分析后，生成一个 AST（Abstract Syntax Tree），一般经过 Visitor 模式遍历后生成。原始数据通过叶子节点 TableScan 读取数据，然后经过各个 Operator的计算，包括（TableScan、Project、Filter、Exchange、Agg、Join、TaskOutput等）产出结果。</li><li>Iterator Model<br>顾名思义就是一个递归迭代过程，Plan 树的各节点有三个状态，Open、GetNext及Close。从根节点 Open 开始递归调用 GetNext 获取数据，即父节点递归调用子节点接口直到没有结果为止，然后Close。</li></ul><a id="more"></a><h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><h3 id="Stage"><a href="#Stage" class="headerlink" title="Stage"></a>Stage</h3><p>MPP的理念就是能尽量细粒度的将 SQL 并行执行，以一个 SQL 2个表 JOIN 后 Agg 为例，那么每个表都可以单独并行执行去 Scan 数据（互不影响），然后进行 Join 和 Agg。所以执行计划（Plan）将执行 PlanFragment，即将一个树分块变为各个子树，每个子树可以并行的在多台机器上执行，这个 Fragment 被称为 Stage。</p><p>Presto根据 Stage 的用途，分为四种stage:</p><ul><li>Coordinator_Only:一般表示 DDL,DML 的 Stage</li><li>Single：用于聚合子 stages 数据，并最终将数据输出给终端用户。比如每个查询中的根节点（Root Stage）</li><li>Fixed：用于接收子 Stage 产生的数据，并在集群中对这些数据进行聚合或分组计算</li><li>Source：连接数据源，从数据源读取数据</li></ul><p>我们以简单的SQL查询为例，SQL为select id from table limit 1; 这个SQL简单来说，就干了2件事，一是Scan数据，另外是Limit，而这2件事，可以并行执行，所以如图所示，其分为2个Stage：<br><img src="/img/2018/08/stage.png"></p><p>Stage 1 为Scan数据和Limit，这里Limit是下推优化。Stage 0为最终结果输出。</p><p>同时Presto UI里可以看到每个Stage详细信息，以及每个Stage需要的Task数（可以认为Worker数），如图所示：<br><img src="/img/2018/08/stages.png"></p><h3 id="Exchange"><a href="#Exchange" class="headerlink" title="Exchange"></a>Exchange</h3><p>连接不同的 Stage，用于不同 Stage 之间的数据交互。数据的交互有一些Operator实现，比如数据是Hash分发还是完全Replicate等。从上图可以看到Stage 1 和Stage 0 需要交互，通过Exchange实现。</p><h3 id="Task"><a href="#Task" class="headerlink" title="Task"></a>Task</h3><p>Stage 有多个 Task 组成。Stage 并不会运行，其实个抽象的概念，其只是负责管理 Task 和封装建模。Stage 实际运行的是Task。每个Task处理一个或者多个Split，每个Task被分配到每台机器上执行。每个Task都有对应的输入和输出。同一个Stage下的Task是个并行的概念，做的事情是相同的。<br>如下图所示，我们可以看到每个Tasks的相信信息，其中0.x表示Stage 0，1.x表示Stage 1，同时我们也可以看到每个Task执行花费的时间，读取的数据大小以及每个Task处理的Split数目。一个Stage包含一个或多个Task，每个Task做的事情是一样的，所以每个Stage的花费时间由最慢的Task决定，比如Scan HDFS数据，可能会因为某些Data Node阻塞导致Task阻塞。<br><img src="/img/2018/08/tasks.png"></p><h3 id="Driver"><a href="#Driver" class="headerlink" title="Driver"></a>Driver</h3><p>Task 被分解成一个或者多个 Driver，并行执行多个 Driver 的方式来实现 Task 的并行执行。Driver 是作用于一个 Split 的一系列 Operator 的集合。一个 Driver 处理一个 Split，产生输出由 Task 收集并传递给下游的 Stage 中的一个 Task。一个 Driver 拥有一个输入和输出。</p><h3 id="Operator"><a href="#Operator" class="headerlink" title="Operator"></a>Operator</h3><p>Operator 表示对一个 Split 的一种操作。比如过滤、转换等。 一个 Operator 一次读取一个 Split 的数据，将 Operator 所表示的计算、操作作用于 Split 的数据上，产生输出。每个 Operator 会以 Page 为最小处理单位分别读取输入数据和产生输出数据。Operator 每次只读取一个 Page,输出产生一个 Page。</p><h3 id="Split"><a href="#Split" class="headerlink" title="Split"></a>Split</h3><p>一个分片表示大的数据集合中的一个小子集,与 MapReduce 中的 Split 概念类似。对于Hive中的表，一个Split就是HDFS文件的一个分片。根据文件格式是否分片（如ORC,Parquet），该Split可能是一个Block的大小，也可能是整个文件。</p><h3 id="Page"><a href="#Page" class="headerlink" title="Page"></a>Page</h3><p>Presto 中处理的最小数据单元。一个 Page 对象包括多个 Block 对象，而每个 Block 对象是一个字节数组，存储一个字段的若干行。多个 Block 的横切的一行表示真实的一行数据。一个 Page 最大 1MB，最多 16 * 1024 行数据</p><h3 id="Pipeline"><a href="#Pipeline" class="headerlink" title="Pipeline"></a>Pipeline</h3><p>Stage 里有很多 Operator，这些 Operator 可能并行度是不一样的，比如 Scan 数据并行就很大，但是最后聚合数据，并行一般为1。所以 PlanFragment 又会被切分为若干 Pipeline，每个 Pipeline 由一组 Operator 组成，这些 Operator 被设置同样的并行度。Pipeline 之间会通过 LocalExchangeOperator 来传递数据。</p><p>在 Presto UI 上我们可以看到 Pipeline信息，如下图所示，Stage 0 主要是将 Exchange 的数据，做最后的 limit，所以其可以细分为 2 个步骤，LocalExchangeOperator 及 LimitOperator，这2个动作的并行度是不一样的，Exchange 可以多个线程去做，而 Limit 只需要一个线程。从图中我们可以看到 Driver 和 Operator 信息，其中 Driver 的数目就是这个 Pipeline 的并行度。<br><img src="/img/2018/08/pipeline.png"></p><p>因为后续会陆续介绍 Presto 的一些执行流程，为了防止被一些概念绕晕，所以本文主要是对 Presto 的一些概念和专有名词做了一些科普和解释。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>《Presto技术内幕》</li><li>《Presto基本概念》</li></ul>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;模型&quot;&gt;&lt;a href=&quot;#模型&quot; class=&quot;headerlink&quot; title=&quot;模型&quot;&gt;&lt;/a&gt;模型&lt;/h2&gt;&lt;p&gt;Presto 是 Facebook 开源的 MPP (Massive Parallel Processing) SQL 引擎，其理念来源于一个叫 Volcano 的并行数据库，该数据库提出了一个并行执行 SQL 的模型，核心思想就是 Operator Model 和 Iterator Model。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Operator Model&lt;br&gt;即通过各种 Operator 组成一棵树，树的根节点负责结果输出，树的叶子节点是各种 TableScan。这棵树被称作 Plan（执行计划），数据库里又被细分为逻辑执行计划和物理执行计划。这棵树是由 SQL 经过词法、语法分析及语义分析后，生成一个 AST（Abstract Syntax Tree），一般经过 Visitor 模式遍历后生成。原始数据通过叶子节点 TableScan 读取数据，然后经过各个 Operator的计算，包括（TableScan、Project、Filter、Exchange、Agg、Join、TaskOutput等）产出结果。&lt;/li&gt;
&lt;li&gt;Iterator Model&lt;br&gt;顾名思义就是一个递归迭代过程，Plan 树的各节点有三个状态，Open、GetNext及Close。从根节点 Open 开始递归调用 GetNext 获取数据，即父节点递归调用子节点接口直到没有结果为止，然后Close。&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>Presto内存管理原理和调优</title>
    <link href="http://armsword.com/2018/05/22/the-memory-management-and-tuning-experience-of-presto/"/>
    <id>http://armsword.com/2018/05/22/the-memory-management-and-tuning-experience-of-presto/</id>
    <published>2018-05-22T10:32:44.000Z</published>
    <updated>2020-11-03T07:37:50.319Z</updated>
    
    <content type="html"><![CDATA[<h2 id="内存池"><a href="#内存池" class="headerlink" title="内存池"></a>内存池</h2><p>Presto有三种内存池，分别为GENERAL_POOL、RESERVED_POOL、SYSTEM_POOL。这三个内存池占用的内存大小是由下面算法进行分配的：</p><figure class="highlight reasonml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">builder.put(RESERVED_POOL, <span class="keyword">new</span> <span class="constructor">MemoryPool(RESERVED_POOL, <span class="params">config</span>.<span class="params">getMaxQueryMemoryPerNode</span>()</span>));</span><br><span class="line"></span><br><span class="line">builder.put(SYSTEM_POOL, <span class="keyword">new</span> <span class="constructor">MemoryPool(SYSTEM_POOL, <span class="params">systemMemoryConfig</span>.<span class="params">getReservedSystemMemory</span>()</span>));</span><br><span class="line"></span><br><span class="line">long maxHeap = <span class="module-access"><span class="module"><span class="identifier">Runtime</span>.</span></span>get<span class="constructor">Runtime()</span>.max<span class="constructor">Memory()</span>;</span><br><span class="line">maxMemory = <span class="keyword">new</span> <span class="constructor">DataSize(<span class="params">maxHeap</span> - <span class="params">systemMemoryConfig</span>.<span class="params">getReservedSystemMemory</span>()</span>.<span class="keyword">to</span><span class="constructor">Bytes()</span>, BYTE);</span><br><span class="line">DataSize generalPoolSize = <span class="keyword">new</span> <span class="constructor">DataSize(Math.<span class="params">max</span>(0, <span class="params">maxMemory</span>.<span class="params">toBytes</span>()</span> - config.get<span class="constructor">MaxQueryMemoryPerNode()</span>.<span class="keyword">to</span><span class="constructor">Bytes()</span>), BYTE);</span><br><span class="line">builder.put(GENERAL_POOL, <span class="keyword">new</span> <span class="constructor">MemoryPool(GENERAL_POOL, <span class="params">generalPoolSize</span>)</span>);</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>梳理这块代码对应的逻辑和配置文件，得出RESERVED_POOL大小由config.properties里的query.max-memory-per-node指定；SYSTEM_POOL由config.properties里的resources.reserved-system-memory指定，如果不指定，默认值为Runtime.getRuntime().maxMemory() * 0.4，即0.4 * Xmx值；而GENERAL_POOL值为 总内存（Xmx值）- 预留的（max-memory-per-node）- 系统的（0.4 * Xmx）。</p><p>而这三种内存池分别用于不同的地方，分析代码和阅读Presto开发手册，大体可以定位出：</p><ul><li>GENERAL_POOL is the memory pool used by the physical operators in a query.</li><li>SYSTEM_POOL is mostly used by the exchange buffers and readers/writers.</li><li>RESERVED_POOL is for running a large query when the general pool becomes full.</li></ul><a id="more"></a><p>简单说GENERAL_POOL用于普通查询的physical operators；SYSTEM_POOL用于读写buffer；而RESERVED_POOL比较特殊，大部分时间里是不参与计算的，只有当同时满足如下情形下，才会被使用，然后从所有查询里获取占用内存最大的那个查询，然后将该查询放到 RESERVED_POOL 里执行，同时注意RESERVED_POOL只能用于一个Query。</p><p>1、GENERAL_POOL有节点出现阻塞节点（block node）情况，即该node内存不足<br>2、RESERVED_POOL没有被使用</p><p>GENERAL_POOL、RESERVED_POOL、SYSTEM_POOL应配合合理的值，如果并发比较大时，建议SYSTEM_POOL保持默认或者稍微再大一点。目前我的经验配置是SYSTEM_POOL为1/3 * Xmx(虽然我们并发较多，但是依然调低了此值)；RESERVED_POOL 为 1/9 * XMX。</p><p>当然你可以通过HTTP请求查看每台Worker的/v1/status，来预估具体需要配置多大的内存，如图所示，显示了各内存池的使用量。</p><img src="/img/2018/05/presto.png" width = "66%" />        <h2 id="内存限制和管理"><a href="#内存限制和管理" class="headerlink" title="内存限制和管理"></a>内存限制和管理</h2><h3 id="单机维度"><a href="#单机维度" class="headerlink" title="单机维度"></a>单机维度</h3><ul><li><p>GENERAL_POOL每次内存申请时，都会判断内存使用量是否超过了最大内存，如果超过了就报错，错误为“Query exceeded local memory limit of x”，这保护了Presto会无限申请内存，只会导致当前查询出错。同时，如果该节点的GENERAL_POOL可使用内存以及可回收内存为0，那么认为该node为Block node。</p></li><li><p>RESERVED_POOL可以认为是查询最大的SQL，其能满足GENERAL_POOL的内存限制策略，那么肯定会满足RESERVED_POOL的策略（复用了GENERAL_POOL策略）。</p></li><li><p>RESERVED_POOL目前版本未发现可以限制内存，所以当并发非常高，且scan的数据非常大时，有低概率会引起OOM问题。但是配合Resource Group，内存设置合理，也基本会避免OOM问题。</p></li></ul><h3 id="集群维度"><a href="#集群维度" class="headerlink" title="集群维度"></a>集群维度</h3><p>同时满足以下两点时，Presto便认为集群超出要求的内存了：</p><ul><li>GENERAL_POOL出现阻塞节点（Block node）</li><li>RESERVED_POOL已经被使用</li></ul><p>当判断出集群超出CLuster Memory时，有两种方式管理内存：<br>1、挨个遍历每个查询，判断当前查询占用的总内存是否超过了query.max-memory（config.properties里配置），如果超过了，那么该查询就被failed。<br>2、如果query.max-memory配置的不合理，值非常大，那么可能过了5秒（默认时间）依然不满足第一种情形，那么将会使用第二种方法管理查询。第二种管理方法又分为两种小的管理，根据LowMemoryKillerPolicy来决定Kill查询策略，其分为total-reservation和total-reservation-on-blocked-nodes。配置total-reservation的作用是kill掉所有查询里最费内存的查询；而total-reservation-on-blocked-nodes杀死在内存不足（阻塞）的节点上使用最多内存的查询。</p><h2 id="Resource-Groups"><a href="#Resource-Groups" class="headerlink" title="Resource Groups"></a>Resource Groups</h2><p><a href="https://prestodb.io/docs/current/admin/resource-groups.html">Resource Groups</a> 可以认为是Presto实现了一个弱资源限制和隔离功能。其可以为每个group指定队列大小、并发大小、内存使用大小。为每个group设置合理的hardConcurrencyLimit（最大并发数）、softMemoryLimit（内存最大使用值）及maxQueued（队列大小）一方面可以使不同业务影响降低，另一方面也大概率避免OOM问题，当然善于运用user及做下二次开发，就可以让Presto支持多用户共用同一分组和权限认证功能。</p><h2 id="OOM"><a href="#OOM" class="headerlink" title="OOM"></a>OOM</h2><p>即使按照上述内存管理做了调优，但Presto依然会遇到OOM问题，此时会显示”INTERNAL_ERROR”，仔细查看错误原因为报类似的错误：</p><figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Encountered too many errors talking <span class="built_in">to</span> <span class="keyword">a</span> worker node. The node may have crashed <span class="keyword">or</span> be under too much <span class="built_in">load</span>. This is probably <span class="keyword">a</span> transient issue, so please retry your query <span class="keyword">in</span> <span class="keyword">a</span> few minutes.</span><br></pre></td></tr></table></figure><p>如果是这种情形，大概率是JVM OOM了。如果确定了确实是JVM OOM的原因，那么可以参考我们的JVM（G1 GC）调参经验，以下配置添加到jvm.config里会明显避免OOM问题。</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">-XX:<span class="attribute">G1ReservePercent</span>=15</span><br><span class="line">-XX:<span class="attribute">InitiatingHeapOccupancyPercent</span>=40</span><br><span class="line">-XX:<span class="attribute">ConcGCThreads</span>=8</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>好了，本文简单介绍了下Presto内存管理方面的原理和一些经验，因为Presto相关的技术文章比较少，且关注的用户越来越多，所以我会不定期更新一些Presto相关的技术文章，如果有错误的地方也请及时提醒，有关注Presto的同行也欢迎与我讨论交流。</p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;内存池&quot;&gt;&lt;a href=&quot;#内存池&quot; class=&quot;headerlink&quot; title=&quot;内存池&quot;&gt;&lt;/a&gt;内存池&lt;/h2&gt;&lt;p&gt;Presto有三种内存池，分别为GENERAL_POOL、RESERVED_POOL、SYSTEM_POOL。这三个内存池占用的内存大小是由下面算法进行分配的：&lt;/p&gt;
&lt;figure class=&quot;highlight reasonml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;builder.put(RESERVED_POOL, &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;constructor&quot;&gt;MemoryPool(RESERVED_POOL, &lt;span class=&quot;params&quot;&gt;config&lt;/span&gt;.&lt;span class=&quot;params&quot;&gt;getMaxQueryMemoryPerNode&lt;/span&gt;()&lt;/span&gt;));&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;builder.put(SYSTEM_POOL, &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;constructor&quot;&gt;MemoryPool(SYSTEM_POOL, &lt;span class=&quot;params&quot;&gt;systemMemoryConfig&lt;/span&gt;.&lt;span class=&quot;params&quot;&gt;getReservedSystemMemory&lt;/span&gt;()&lt;/span&gt;));&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;long maxHeap = &lt;span class=&quot;module-access&quot;&gt;&lt;span class=&quot;module&quot;&gt;&lt;span class=&quot;identifier&quot;&gt;Runtime&lt;/span&gt;.&lt;/span&gt;&lt;/span&gt;get&lt;span class=&quot;constructor&quot;&gt;Runtime()&lt;/span&gt;.max&lt;span class=&quot;constructor&quot;&gt;Memory()&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;maxMemory = &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;constructor&quot;&gt;DataSize(&lt;span class=&quot;params&quot;&gt;maxHeap&lt;/span&gt; - &lt;span class=&quot;params&quot;&gt;systemMemoryConfig&lt;/span&gt;.&lt;span class=&quot;params&quot;&gt;getReservedSystemMemory&lt;/span&gt;()&lt;/span&gt;.&lt;span class=&quot;keyword&quot;&gt;to&lt;/span&gt;&lt;span class=&quot;constructor&quot;&gt;Bytes()&lt;/span&gt;, BYTE);&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;DataSize generalPoolSize = &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;constructor&quot;&gt;DataSize(Math.&lt;span class=&quot;params&quot;&gt;max&lt;/span&gt;(0, &lt;span class=&quot;params&quot;&gt;maxMemory&lt;/span&gt;.&lt;span class=&quot;params&quot;&gt;toBytes&lt;/span&gt;()&lt;/span&gt; - config.get&lt;span class=&quot;constructor&quot;&gt;MaxQueryMemoryPerNode()&lt;/span&gt;.&lt;span class=&quot;keyword&quot;&gt;to&lt;/span&gt;&lt;span class=&quot;constructor&quot;&gt;Bytes()&lt;/span&gt;), BYTE);&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;builder.put(GENERAL_POOL, &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;constructor&quot;&gt;MemoryPool(GENERAL_POOL, &lt;span class=&quot;params&quot;&gt;generalPoolSize&lt;/span&gt;)&lt;/span&gt;);&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;梳理这块代码对应的逻辑和配置文件，得出RESERVED_POOL大小由config.properties里的query.max-memory-per-node指定；SYSTEM_POOL由config.properties里的resources.reserved-system-memory指定，如果不指定，默认值为Runtime.getRuntime().maxMemory() * 0.4，即0.4 * Xmx值；而GENERAL_POOL值为 总内存（Xmx值）- 预留的（max-memory-per-node）- 系统的（0.4 * Xmx）。&lt;/p&gt;
&lt;p&gt;而这三种内存池分别用于不同的地方，分析代码和阅读Presto开发手册，大体可以定位出：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;GENERAL_POOL is the memory pool used by the physical operators in a query.&lt;/li&gt;
&lt;li&gt;SYSTEM_POOL is mostly used by the exchange buffers and readers/writers.&lt;/li&gt;
&lt;li&gt;RESERVED_POOL is for running a large query when the general pool becomes full.&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>分布式SQL查询引擎Presto原理介绍</title>
    <link href="http://armsword.com/2017/12/05/presto/"/>
    <id>http://armsword.com/2017/12/05/presto/</id>
    <published>2017-12-05T03:31:11.000Z</published>
    <updated>2020-11-03T07:37:50.310Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>我们实时引擎组新引入了一款分布式SQL查询引擎，名字叫Presto，目前已经调研和测试了2个月了，并且期间某平台也从impala平台迁入到了Presto平台，查询性能有了2-3倍的提升（各种原因导致），所以本文将结合作者这段时间的测试和调研研究，来揭开Presto的神秘面纱。</p><h2 id="Presto是神马"><a href="#Presto是神马" class="headerlink" title="Presto是神马"></a>Presto是神马</h2><p>Presto是由Facebook开发的一个分布式SQL查询引擎， 它被设计为用来专门进行高速、实时的数据分析。它的产生是为了解决Hive的MapReduce模型太慢以及不能通过BI或Dashboards直接展现HDFS数据等问题。Presto是一个纯粹的计算引擎，它不存储数据，其通过Connector获取第三方Storage服务的数据。</p><h2 id="历史"><a href="#历史" class="headerlink" title="历史"></a>历史</h2><ul><li>2012年秋季，Facebook启动Presto项目</li><li>2013年冬季，Presto开源</li><li>2017年11月，11888 commits，203 releases，198 contributors</li></ul><h2 id="功能和优点"><a href="#功能和优点" class="headerlink" title="功能和优点"></a>功能和优点</h2><ul><li>Ad-hoc，期望查询时间秒级或几分钟</li><li>比Hive快10倍</li><li>支持多数据源，如Hive、Kafka、MySQL、MonogoDB、Redis、JMX等，也可自己实现Connector</li><li>Client Protocol: HTTP+JSON, support various languages(Python, Ruby, PHP, Node.js Java)</li><li>支持JDBC/ODBC连接</li><li>ANSI SQL，支持窗口函数，join，聚合，复杂查询等</li></ul><h2 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h2><ul><li>Master-Slave架构</li><li>三个模块<ul><li>Coordinator、Discovery Service、Worker</li></ul></li><li>Connector</li></ul><a id="more"></a><p>Presto沿用了通用的Master-Slave架构，Coordinator即Presto的Master，Worker即其Slave，Discovery Service就是用来保存Worker结点信息的，通过HTTP协议通信，而Connector用于获取第三方存储的Metadata及原始数据等。</p><p>Coordinator负责解析SQL语句，生成执行计划，分发执行任务给Worker节点执行；Worker节点负责实际执行查询任务。Worker节点启动后向Discovery Server服务注册，Coordinator从Discovery Server获得可以正常工作的Worker节点。假如配置了Hive Connector，需要配置一个Hive MetaStore服务为Presto提供Hive元信息，Worker节点与HDFS交互读取数据。</p><h2 id="部署方式"><a href="#部署方式" class="headerlink" title="部署方式"></a>部署方式</h2><p>Presto常见的部署方式如下图所示：<br><img src="/img/2017/12/1.jpg"><br>Coordinator与Discovery Server耦合在一起混合部署，然后部署多台Worker。然而这个有个问题，就是Coordinator存在单点问题，我们目前线上使用ip漂移的方法（网卡绑定多ip）。如下图所示：<br><img src="/img/2017/12/2.jpg" width = "71%" />        </p><h2 id="查询流程"><a href="#查询流程" class="headerlink" title="查询流程"></a>查询流程</h2><p><img src="/img/2017/12/3.jpg">    </p><h3 id="整体查询流程为："><a href="#整体查询流程为：" class="headerlink" title="整体查询流程为："></a>整体查询流程为：</h3><ul><li>Client使用HTTP协议发送一个query请求。</li><li>通过Discovery Server发现可用的Server。</li><li>Coordinator构建查询计划（Connector插件提供Metadata）</li><li>Coordinator向workers发送任务</li><li>Worker通过Connector插件读取数据</li><li>Worker在内存里执行任务（Worker是纯内存型计算引擎）</li><li>Worker将数据返回给Coordinator，之后再Response Client</li></ul><h2 id="SQL执行流程"><a href="#SQL执行流程" class="headerlink" title="SQL执行流程"></a>SQL执行流程</h2><p><img src="/img/2017/12/4.jpg"><br>当Coordinator收到一个Query，其SQL执行流程如上图所示。SQL通过Anltr3解析为AST（抽象语法树），然后通过Connector获取原始数据的Metadata信息，这里会有一些优化，比如缓存Metadata信息等，根据Metadata信息生成逻辑计划，然后会依次生成分发计划和执行计划，在执行计划里需要去Discovery里获取可用的node列表，然后根据一定的策略，将这些计划分发到指定的Worker机器上，Worker机器再分别执行。</p><h2 id="与Hive比较"><a href="#与Hive比较" class="headerlink" title="与Hive比较"></a>与Hive比较</h2><p><img src="/img/2017/12/5.jpg"><br>上图显示了MapReduce与Presto的执行过程的不同点，MR每个操作要么需要写磁盘，要么需要等待前一个stage全部完成才开始执行，而Presto将SQL转换为多个stage，每个stage又由多个tasks执行，每个tasks又将分为多个split。所有的task是并行的方式进行允许，stage之间数据是以pipeline形式流式的执行，数据之间的传输也是通过网络以Memory-to-Memory的形式进行，没有磁盘io操作。这也是Presto性能比Hive快很多倍的决定性原因。</p><h2 id="实现低延时的原理"><a href="#实现低延时的原理" class="headerlink" title="实现低延时的原理"></a>实现低延时的原理</h2><ul><li>完全基于内存的并行计算</li><li>流水线</li><li>本地化计算</li><li>动态编译执行计划</li><li>小心使用内存和数据结构</li><li>类BlinkDB的近似查询</li><li>GC控制</li></ul><p>当然其优化方法也包括了一些传统的SQL优化原理，关于这些优化细节等后续文章详细介绍。</p><h2 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h2><p>前面介绍了Presto的各种优点，其实其也有一些缺点，主要缺点为以下三条：</p><ul><li>No fault tolerance；当一个Query分发到多个Worker去执行时，当有一个Worker因为各种原因查询失败，那么Master会感知到，整个Query也就查询失败了，而Presto并没有重试机制，所以需要用户方实现重试机制。</li><li>Memory Limitations for aggregations, huge joins；比如多表join需要很大的内存，由于Presto是纯内存计算，所以当内存不够时，Presto并不会将结果dump到磁盘上，所以查询也就失败了，但最新版本的Presto已支持写磁盘操作，这个待后续测试和调研。</li><li>MPP(Massively Parallel Processing )架构；这个并不能说其是一个缺点，因为MPP架构就是解决大量数据分析而产生的，但是其缺点也很明显，假如我们访问的是Hive数据源，如果其中一台Worke由于load问题，数据处理很慢，那么整个查询都会受到影响，因为上游需要等待上游结果。<br>这篇文章就先介绍这里吧，后续会陆续更新一系列Presto相关的文章，欢迎关注。</li></ul><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://tech.meituan.com/presto.html">https://tech.meituan.com/presto.html</a></p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;我们实时引擎组新引入了一款分布式SQL查询引擎，名字叫Presto，目前已经调研和测试了2个月了，并且期间某平台也从impala平台迁入到了Presto平台，查询性能有了2-3倍的提升（各种原因导致），所以本文将结合作者这段时间的测试和调研研究，来揭开Presto的神秘面纱。&lt;/p&gt;
&lt;h2 id=&quot;Presto是神马&quot;&gt;&lt;a href=&quot;#Presto是神马&quot; class=&quot;headerlink&quot; title=&quot;Presto是神马&quot;&gt;&lt;/a&gt;Presto是神马&lt;/h2&gt;&lt;p&gt;Presto是由Facebook开发的一个分布式SQL查询引擎， 它被设计为用来专门进行高速、实时的数据分析。它的产生是为了解决Hive的MapReduce模型太慢以及不能通过BI或Dashboards直接展现HDFS数据等问题。Presto是一个纯粹的计算引擎，它不存储数据，其通过Connector获取第三方Storage服务的数据。&lt;/p&gt;
&lt;h2 id=&quot;历史&quot;&gt;&lt;a href=&quot;#历史&quot; class=&quot;headerlink&quot; title=&quot;历史&quot;&gt;&lt;/a&gt;历史&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;2012年秋季，Facebook启动Presto项目&lt;/li&gt;
&lt;li&gt;2013年冬季，Presto开源&lt;/li&gt;
&lt;li&gt;2017年11月，11888 commits，203 releases，198 contributors&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;功能和优点&quot;&gt;&lt;a href=&quot;#功能和优点&quot; class=&quot;headerlink&quot; title=&quot;功能和优点&quot;&gt;&lt;/a&gt;功能和优点&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;Ad-hoc，期望查询时间秒级或几分钟&lt;/li&gt;
&lt;li&gt;比Hive快10倍&lt;/li&gt;
&lt;li&gt;支持多数据源，如Hive、Kafka、MySQL、MonogoDB、Redis、JMX等，也可自己实现Connector&lt;/li&gt;
&lt;li&gt;Client Protocol: HTTP+JSON, support various languages(Python, Ruby, PHP, Node.js Java)&lt;/li&gt;
&lt;li&gt;支持JDBC/ODBC连接&lt;/li&gt;
&lt;li&gt;ANSI SQL，支持窗口函数，join，聚合，复杂查询等&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;架构&quot;&gt;&lt;a href=&quot;#架构&quot; class=&quot;headerlink&quot; title=&quot;架构&quot;&gt;&lt;/a&gt;架构&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;Master-Slave架构&lt;/li&gt;
&lt;li&gt;三个模块&lt;ul&gt;
&lt;li&gt;Coordinator、Discovery Service、Worker&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Connector&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    
    <category term="presto" scheme="http://armsword.com/tags/presto/"/>
    
  </entry>
  
  <entry>
    <title>说说常见搜索引擎的分布式解决方法</title>
    <link href="http://armsword.com/2017/01/16/distributed-solutions-for-search-engines/"/>
    <id>http://armsword.com/2017/01/16/distributed-solutions-for-search-engines/</id>
    <published>2017-01-16T08:36:49.000Z</published>
    <updated>2020-11-03T07:37:50.315Z</updated>
    
    <content type="html"><![CDATA[<p>随着索引数据的增大以及请求的增多，分布式搜索是最好的一种解决方案，主要解决两个问题，其一是能让单台机器load所有索引数据到内存中，其二是请求延时大，解决请求latency问题。我之前为团队写了篇专利，内容是关于分布式搜索解决方案的，所以粗略的看了下大部分开源的搜索引擎是怎么实现分布式的，后面的文章我会简单说下常见的搜索引擎的分布式解决方案。</p><p>首先我们先说下几个简单概念，分布式搜索都是M*N（横向切分数据，纵向切分流量）这个维度去解决问题的，虽然不同产品或场景概念不完全相同，读者可以简单认为一份完整的数据，被均分为M份，每一份被称为一个分配（Shard或者Partition），然后提供对每个Shard提供N份副本（Replica）。那么分布式的设计就围绕着以下问题：</p><ul><li>如何选择合适的分片(Shard)，副本(Replica)的数量</li><li>如何做路由，即怎么在所有Shard里找到一份完整的数据（找到对应的机器列表）</li><li>如何做负载均衡</li><li>如果提高服务的可扩展性</li><li>如何提高服务的服务能力（QPS），当索引和搜索并发量增大时，如何平滑解决</li><li>如何更新索引，全量和增量索引的更新解决方法</li><li>如果提高服务的稳定性，单台服务挂掉怎么不影响整体服务等等</li></ul><p>下面就说下常见的搜索引擎的分布式解决方案，因为开源的搜索产品基本上都没有在工作中用过，对代码细节并不是太了解，只是研究了下其原理，所以理解的会有些偏差，看官们如果发现错误直接指出即可。</p><a id="more"></a><h2 id="Sphinx-Coreseek"><a href="#Sphinx-Coreseek" class="headerlink" title="Sphinx/Coreseek"></a>Sphinx/Coreseek</h2><p>Sphinx的流程还是很简单的，可以看下其流程图：<br><img src="/img/2017/01/sphinx.png" width = "71%" /><br>需要支持分布式的话，需要改下配置，大致是这样子的：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">index dist</span><br><span class="line">&#123;</span><br><span class="line"> <span class="built_in"> type </span>= distributed</span><br><span class="line">  local = chunk1</span><br><span class="line">  agent = localhost:9312:chunk2  本地</span><br><span class="line">  agent = 192.168.100.2:9312:chunk3  远程</span><br><span class="line">  agent = 192.168.100.3:9312:chunk4  远程</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>从图中也可以看出，需要在配置列表里配置好其他shard的地址。查询过程为：</p><ul><li>连接到远程代理</li><li>执行查询<ul><li>对本地索引进行查询</li><li>接收来自远程代理的搜索结果</li></ul></li><li>将所有结果合并，删除重复项</li><li>将合并后的结果返回给客户端</li></ul><p>索引数据复制同步的方法也是常用的两种：</p><ul><li>主从同步</li><li>增量更新索引</li></ul><p>方法也是设置crontab，添加2个选项，一个是重建主索引，一个是增量索引更新。</p><p>当然为了避免单点以及增加服务能力，肯定有多个Replica，解决方法应该也是配置或者haproxy相关的方法解决，从上面可以看出，Sphinx很难用，自动化能力太弱，所以很多大厂要么不再使用Sphinx要么基于其二次开发。</p><h2 id="Solr"><a href="#Solr" class="headerlink" title="Solr"></a>Solr</h2><p>Solr提供了两种方案来应对访问压力，其一是Replication，另一个是SolrCloud。我们此处只说Replication原理。<br>Replication采用了Master/Slave模式，也就是说由一个主索引和多个从索引构成，从索引从主索引复制索引，主索引负责更新索引，从索引负责同步索引和查询。本质上是读写分离的思想，MySQL/Redis等数据库也多是这种方式部署的。有两种部署方式：</p><ul><li><p>第一种</p><img src="/img/2017/01/solr.png" width = "71%" /></li><li><p>第二种</p><img src="/img/2017/01/solr_repeater.png" width = "71%" /></li></ul><p>与第一种相比多了一层Repeater，Repeater既扮演了Master角色，又扮演了Slave功能，主要解决单个Master下Slave太多，Master压力太大的问题。</p><p>Master与Slave之间的通信是无状态的http连接，Slave端发送不同的Command从Master端获得数据。原理就是Master那边有个标志位和版本号，用于获取正确的数据版本，然后数据扔到Slave临时目录下，数据完整后，再覆盖原有数据。多个副本的方法应该与Sphinx相似，一般也是通过通过上游负载均衡模块如Nginx，HaProxy来分流。</p><h2 id="SolrCloud"><a href="#SolrCloud" class="headerlink" title="SolrCloud"></a>SolrCloud</h2><p>因为Solr Replication不好用，本质上还不算真正分布式的，所以Solr从4.0开始支持SolrCloud模式。特性不少，主要说两个吧：</p><ul><li>配置文件统一管理，扔到Zookeeper上</li><li>自动做负载均衡和故障恢复，不再需要Nginx或HaProxy的支持</li></ul><h3 id="逻辑图"><a href="#逻辑图" class="headerlink" title="逻辑图"></a>逻辑图</h3><img src="/img/2017/01/solrcloud.jpg" width = "71%" />- Collection：逻辑意义上一份完整的索引- Shard：上文已说，就是索引的1/N分片- Replica：Shard的一个拷贝<p>每个Shard，即相同的Replica下都会有一个leader，leader选举由Zookeeper完成。虽然有leader的概念，但是其实SolrCloud分布式是去中心化的，意思就是说，leader和非leader都能提供查询功能（也有修改和删除功能，搜索场景应用不多吧？），而更新索引，创建Collection/Shard/Replica（即扩容）只能由leader完成，避免产生并发修改问题，当非leader节点收到修改操作请求时，将信息存储在zookeeper中相应节点上，leader节点会一直对该节点进行watch，发现变化就实时做处理。</p><h3 id="Zookeeper信息"><a href="#Zookeeper信息" class="headerlink" title="Zookeeper信息"></a>Zookeeper信息</h3><p><img src="/img/2017/01/solr_zookeeper.png" alt="图片出处见参考链接"></p><h3 id="创建索引"><a href="#创建索引" class="headerlink" title="创建索引"></a>创建索引</h3><ul><li>任意节点收到创建索引请求后，转换成json格式存储到zk的/overseer/collection-queue-work的children节点上</li><li>leader线程一直监控collection-queue-work节点，检查到变化后，取出json数据，根据信息计算出需要创建的shard、replica，将创建具体replica的请求转向各对应节点</li><li>各节点创建完具体的replica后，将该节点的状态（创建成功与否等）更新到/overseer/queue的children节点上</li><li>leader线程监控/overseer/queue节点，将overseer/queue的children节点的状态更新至/clusterstate.json</li><li>各节点同步/clusterstate.json，整个集群状态得到更新，新索引创建成功</li></ul><h3 id="更新索引"><a href="#更新索引" class="headerlink" title="更新索引"></a>更新索引</h3><ul><li>根据路由规则计算出该doc所属shard，并找出该doc所属的shard对应的leader</li><li>如果当前Replica是对应Shard且是leader，首先更新本地索引，然后再将doc转向该Shard的其余Replica</li></ul><h3 id="扩容-缩容"><a href="#扩容-缩容" class="headerlink" title="扩容/缩容"></a>扩容/缩容</h3><ul><li>停掉某台Solr，更新集群状态到/clusterstate.json</li><li>增加一台Solr，从leader出复制相同的数据，然后配置写到/clusterstate.json</li></ul><h3 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h3><ul><li>去中心的，leader和非leader一样功能，Replica接收搜索请求时，从Zookeeper中获取该Replica对应的Collection及所有的Shard和Replica</li><li>将请求发送到该Collection下对应的Shard，然后负载均衡到对应的Replica</li></ul><p>SolrCloud也有其他功能，比如Optimization，就是一个运行在leader机器的进程，复杂压缩索引和归并Segment；近实时搜索等。总体看SolrCloud解决了Solr Replication遇到的一些问题，比Sphinx更好用，更自动化。</p><h3 id="一号店"><a href="#一号店" class="headerlink" title="一号店"></a>一号店</h3><p>很多大一点的厂商如果不自研搜索引擎的话，并没有使用SolrCloud，而多基于Solr/Lucence。比如一号店的分布式搜索解决方案，如下所示：<br><a href="http://www.infoq.com/cn/articles/yhd-11-11-distributed-search-engine-architecture">http://www.infoq.com/cn/articles/yhd-11-11-distributed-search-engine-architecture</a></p><p>Broker就相当于Proxy，扮演了路由功能，很多厂商做的与一号店有相似之处。因为没有leader选举，所以索引的更新就由其他模块来做了。</p><h2 id="ElasticSearch"><a href="#ElasticSearch" class="headerlink" title="ElasticSearch"></a>ElasticSearch</h2><p>ElasticSearch的倒排索引也是基于Lucence实现的。功能强大，不仅提供了实时搜索功能，还有分析功能，DB-Engines上面的搜索引擎排名，目前已经超越Solr排名第一位了。因为太强大了，功能也特别多，我研究还不够深，简单说下吧。<br>es会将集群名字相同的机器归为一个集群（业务），所以先说下启动过程。</p><h3 id="启动过程"><a href="#启动过程" class="headerlink" title="启动过程"></a>启动过程</h3><img src="/img/2017/01/es_start.png" width = "71%" /><p>当ElasticSearch的节点启动后，它会利用多播(multicast)(或者单播，如果用户更改了配置)寻找集群中的其它节点，并与之建立连接。</p><h3 id="leader选举"><a href="#leader选举" class="headerlink" title="leader选举"></a>leader选举</h3><p>与SolrCloud相似，也是去中心化的，但是没有使用Zookeeper，而是自己实现了分布式锁，选主的流程叫做 ZenDiscovery（详情见第三个参考链接）：</p><ul><li>节点启动后先ping（这里的ping是 Elasticsearch 的一个RPC命令。如果 discovery.zen.ping.unicast.hosts 有设置，则ping设置中的host，否则尝试ping localhost 的几个端口， Elasticsearch 支持同一个主机启动多个节点）</li><li>Ping的response会包含该节点的基本信息以及该节点认为的master节点</li><li>选举开始，先从各节点认为的master中选，规则很简单，按照id的字典序排序，取第一个</li><li>如果各节点都没有认为的master，则从所有节点中选择，规则同上。这里有个限制条件就是 discovery.zen.minimum_master_nodes，如果节点数达不到最小值的限制，则循环上述过程，直到节点数足够可以开始选举</li><li>最后选举结果是肯定能选举出一个master，如果只有一个local节点那就选出的是自己</li><li>如果当前节点是master，则开始等待节点数达到 minimum_master_nodes，然后提供服务</li><li>如果当前节点不是master，则尝试加入master</li></ul><p>选举完leader后，主节点leader会去读取集群状态信息；因为主节点会监控其他节点，当其他节点出现故障时，会进行恢复工作。在这个阶段，主节点会去检查哪些分片可用，决定哪些分片作为主分片。</p><h3 id="分片"><a href="#分片" class="headerlink" title="分片"></a>分片</h3><p>es在创建索引时，自己设置好分片个数，默认5个，整个过程类似于分裂的概念，如下图所示：<br><img src="/img/2017/01/es_shard.svg" width = "71%" /></p><p>至于读写、写操作等于SolrCloud相似，等我细研究后后续再说吧，也可以说下实时索引怎么做的，细节很多，下次再说吧。至于文中为什么不说Lucence，因为Lucence其实就是个index lib，只是解决倒排、正排索引怎么存放的，并不是一个完整的搜索引擎解决方案。而ES为什么能脱颖而出的主要原因是配套设施完善，工具，Web UI都是非常赞的，对于很多开源产品，它能后来居上的主要原因就是它真实的能解决用户遇到的问题或者比其他产品更好用。搜索引擎发展这么多年了，架构这块能做的基本上大家都差不太多，最后能脱颖而出的肯定是第三方工具做的更完善，更好用的了。</p><p>PS：<br>至于阿里搜索怎么做的，可以参考下这个文档，包括了阿里搜索里用到的很多基础模块了：<br><a href="https://share.weiyun.com/f66e79d9f6897d0aac683361531cf00d">https://share.weiyun.com/f66e79d9f6897d0aac683361531cf00d</a></p><p>参考链接：<br><a href="http://blog.haohtml.com/archives/13724">http://blog.haohtml.com/archives/13724</a><br><a href="http://www.voidcn.com/blog/u011026968/article/p-4922079.html">http://www.voidcn.com/blog/u011026968/article/p-4922079.html</a><br><a href="http://jolestar.com/elasticsearch-architecture/">http://jolestar.com/elasticsearch-architecture/</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;随着索引数据的增大以及请求的增多，分布式搜索是最好的一种解决方案，主要解决两个问题，其一是能让单台机器load所有索引数据到内存中，其二是请求延时大，解决请求latency问题。我之前为团队写了篇专利，内容是关于分布式搜索解决方案的，所以粗略的看了下大部分开源的搜索引擎是怎么实现分布式的，后面的文章我会简单说下常见的搜索引擎的分布式解决方案。&lt;/p&gt;
&lt;p&gt;首先我们先说下几个简单概念，分布式搜索都是M*N（横向切分数据，纵向切分流量）这个维度去解决问题的，虽然不同产品或场景概念不完全相同，读者可以简单认为一份完整的数据，被均分为M份，每一份被称为一个分配（Shard或者Partition），然后提供对每个Shard提供N份副本（Replica）。那么分布式的设计就围绕着以下问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如何选择合适的分片(Shard)，副本(Replica)的数量&lt;/li&gt;
&lt;li&gt;如何做路由，即怎么在所有Shard里找到一份完整的数据（找到对应的机器列表）&lt;/li&gt;
&lt;li&gt;如何做负载均衡&lt;/li&gt;
&lt;li&gt;如果提高服务的可扩展性&lt;/li&gt;
&lt;li&gt;如何提高服务的服务能力（QPS），当索引和搜索并发量增大时，如何平滑解决&lt;/li&gt;
&lt;li&gt;如何更新索引，全量和增量索引的更新解决方法&lt;/li&gt;
&lt;li&gt;如果提高服务的稳定性，单台服务挂掉怎么不影响整体服务等等&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;下面就说下常见的搜索引擎的分布式解决方案，因为开源的搜索产品基本上都没有在工作中用过，对代码细节并不是太了解，只是研究了下其原理，所以理解的会有些偏差，看官们如果发现错误直接指出即可。&lt;/p&gt;</summary>
    
    
    
    <category term="搜索引擎" scheme="http://armsword.com/categories/%E6%90%9C%E7%B4%A2%E5%BC%95%E6%93%8E/"/>
    
    
  </entry>
  
  <entry>
    <title>Minder：一个分布式启停进程服务</title>
    <link href="http://armsword.com/2016/10/29/minder/"/>
    <id>http://armsword.com/2016/10/29/minder/</id>
    <published>2016-10-29T11:54:14.000Z</published>
    <updated>2020-11-03T07:37:50.320Z</updated>
    
    <content type="html"><![CDATA[<h2 id="关于"><a href="#关于" class="headerlink" title="关于"></a>关于</h2><p>打算造个轮子，先备份下简单的设计手册吧。Minder是一个分布式启停进程服务，主要用于以下目的。</p><ul><li>命令或远程RPC调用启动和停止进程</li><li>当进程挂掉，可以拉起来进程</li><li>获得机器对应的系统信息，如CPU，内存，硬盘等等</li></ul><h2 id="设计"><a href="#设计" class="headerlink" title="设计"></a>设计</h2><h3 id="方案一（不采用）"><a href="#方案一（不采用）" class="headerlink" title="方案一（不采用）"></a>方案一（不采用）</h3><p>分为Server和Client端</p><h3 id="Server"><a href="#Server" class="headerlink" title="Server"></a>Server</h3><p>Server在每台机器上都需要安装，Server是个常驻进程，负责真正的启停进程，同时需要一个Check线程，用于判断进程状态，当程序挂掉以便重启进程，重启进程可以在配置里有个次数限制。</p><a id="more"></a><h3 id="Client"><a href="#Client" class="headerlink" title="Client"></a>Client</h3><p>Client就是一个工具，用于向指定的Server发送请求，Server收到请求，启动进程。因为协议使用RPC，为了方便，使用脚本语言编写更好。</p><h3 id="程序流程"><a href="#程序流程" class="headerlink" title="程序流程"></a>程序流程</h3><p>Client发送请求，根据Client命令，判断是启动还是停止进程，假设是start进程，那么通过RPC的stub_client发送到Server的stub_server，之后调用对应的startProcess函数，通过fork+exec启动进程，同时Server需要有个Loop线程，判断进程的状态，以便重启进程。因为一个Server需要启动不同的服务，同时Server也可能会挂掉，所以Server需要持久化进程的信息，以便Server挂掉后恢复这些进程信息。</p><h3 id="细节"><a href="#细节" class="headerlink" title="细节"></a>细节</h3><ul><li> 想象下我们启动进程都需要哪些参数？</li></ul><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">      <span class="attr">&quot;processInfos&quot;</span>: [</span><br><span class="line">           &#123;</span><br><span class="line">               <span class="attr">&quot;isDaemon&quot;</span>: <span class="literal">true</span>,   <span class="comment">// 是否是守护进程</span></span><br><span class="line">               <span class="attr">&quot;processName&quot;</span>: <span class="string">&quot;app&quot;</span>,  <span class="comment">// 进程名字</span></span><br><span class="line">               <span class="attr">&quot;restartInterval&quot;</span>: <span class="number">10</span>,  <span class="comment">// 重启时间间隔</span></span><br><span class="line">               <span class="attr">&quot;restartCountLimit&quot;</span>: <span class="number">10</span>,  <span class="comment">// 重启次数</span></span><br><span class="line">               <span class="attr">&quot;signal&quot;</span>: <span class="number">9</span>,            <span class="comment">// 停止进程的信号值，kill函数对应的值</span></span><br><span class="line">               <span class="attr">&quot;parameters&quot;</span>: <span class="string">&quot;-m cluster -n 1&quot;</span>,  <span class="comment">// 进程需要的参数</span></span><br><span class="line">               <span class="attr">&quot;environmentVariable&quot;</span> :  &#123;   <span class="comment">// 需要的环境变量</span></span><br><span class="line">                   <span class="attr">&quot;key&quot;</span> : <span class="string">&quot;key&quot;</span>,</span><br><span class="line">                   <span class="attr">&quot;value&quot;</span> : <span class="string">&quot;value&quot;</span></span><br><span class="line">              &#125;            </span><br><span class="line">          &#125;</span><br><span class="line">       ] </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因为我们请求是Client工具向Server发送请求的，通信方式为RPC，所以上述参数都为PB格式。</p><ul><li><p>Server端启动进程方式为fork+exec，停止进程为kill发送信号。</p></li><li><p>Client只是个工具，发送请求的方式可以为以下命令： minder_client -m start -s &quot;10.10.10.10:8888&quot; -p /bin/sleep 大意就是向Server发送请求，请求启动sleep进程。</p></li></ul><h3 id="架构图"><a href="#架构图" class="headerlink" title="架构图"></a>架构图</h3><img src="/img/2016/10/minder_1.png" width = "67%" /><h3 id="方案二"><a href="#方案二" class="headerlink" title="方案二"></a>方案二</h3><p>分为Manager、Slave和Tools，第一版本只有一个Manager，Slave在每台机器都有。如果看懂方案一，方案二许多细节就可以不用说了，只见到说下每个模块干啥的吧。</p><h3 id="Manager"><a href="#Manager" class="headerlink" title="Manager"></a>Manager</h3><p>只有Manager给用户打交道，用户向Manager提交需要启动的机器ip，进程信息，Manager向Slave发送消息。同时如果用户需要获取Slave机器的机器状态，如CPU，内存以及磁盘信息等，也是通过Manager获取的。</p><h3 id="Slave"><a href="#Slave" class="headerlink" title="Slave"></a>Slave</h3><p>用户Slave是不与用户打交道的，Slave用于接受Manager的请求，启停进程，获取Slave机器状态，然后将对应的信息返回给Manager。</p><h3 id="Tools"><a href="#Tools" class="headerlink" title="Tools"></a>Tools</h3><p>封装了对应的RPC请求，用户向Manager发送请求。</p><h3 id="程序流程-1"><a href="#程序流程-1" class="headerlink" title="程序流程"></a>程序流程</h3><p>Manager接受用户的请求，根据请求，Manager向Slave发送请求，Slave接受到请求后执行对应的操作。同时Manager持久化对应的信息。因为Manger必须知道Slave的机器ip，如果这个需要Slave主动汇报给Manager，四层心跳既可以。</p><h3 id="架构图-1"><a href="#架构图-1" class="headerlink" title="架构图"></a>架构图</h3><img src="/img/2016/10/minder.png" width = "67%" /><p>当然，这个设计也是有很多改进的方法，比如Manager挂了怎么办，Manager存在单点问题，既然启动进程，怎么可以改善下搞成灰度发布，等等。因为时间问题，我工作也非常忙，这个我就不细说了。轮子有空造，其实写出来方案，思路基本上就清晰了。</p><p>另外，其实写这篇文字时候，与微信朋友圈里与朋友们交流了下想法，发现了2个系统是可以参考的，一个是Hadoop Ambari，另一个是Cloudera Manager。</p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;关于&quot;&gt;&lt;a href=&quot;#关于&quot; class=&quot;headerlink&quot; title=&quot;关于&quot;&gt;&lt;/a&gt;关于&lt;/h2&gt;&lt;p&gt;打算造个轮子，先备份下简单的设计手册吧。Minder是一个分布式启停进程服务，主要用于以下目的。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;命令或远程RPC调用启动和停止进程&lt;/li&gt;
&lt;li&gt;当进程挂掉，可以拉起来进程&lt;/li&gt;
&lt;li&gt;获得机器对应的系统信息，如CPU，内存，硬盘等等&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;设计&quot;&gt;&lt;a href=&quot;#设计&quot; class=&quot;headerlink&quot; title=&quot;设计&quot;&gt;&lt;/a&gt;设计&lt;/h2&gt;&lt;h3 id=&quot;方案一（不采用）&quot;&gt;&lt;a href=&quot;#方案一（不采用）&quot; class=&quot;headerlink&quot; title=&quot;方案一（不采用）&quot;&gt;&lt;/a&gt;方案一（不采用）&lt;/h3&gt;&lt;p&gt;分为Server和Client端&lt;/p&gt;
&lt;h3 id=&quot;Server&quot;&gt;&lt;a href=&quot;#Server&quot; class=&quot;headerlink&quot; title=&quot;Server&quot;&gt;&lt;/a&gt;Server&lt;/h3&gt;&lt;p&gt;Server在每台机器上都需要安装，Server是个常驻进程，负责真正的启停进程，同时需要一个Check线程，用于判断进程状态，当程序挂掉以便重启进程，重启进程可以在配置里有个次数限制。&lt;/p&gt;</summary>
    
    
    
    <category term="分布式系统" scheme="http://armsword.com/categories/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
    
  </entry>
  
  <entry>
    <title>一些常见的搜索查询树优化方法</title>
    <link href="http://armsword.com/2016/10/12/optimize-query-search-tree/"/>
    <id>http://armsword.com/2016/10/12/optimize-query-search-tree/</id>
    <published>2016-10-12T11:33:21.000Z</published>
    <updated>2020-11-03T07:37:50.315Z</updated>
    
    <content type="html"><![CDATA[<p>搜索引擎服务收到一个Query后，一般引擎这边是这么搞得，解析语法，生成后缀表达式，即查询搜索树（Search Tree）。搜索查询树负责求交、求并和过滤。所以这个地方也是性能关键点。所以在解析语法后，一般要做查询搜索树优化，减少求交，求并和过滤操作的次数，以此来提高搜索服务的QPS和查询Latency。</p><p>因为同事在负责新实时索引的性能优化，我正好负责将全量索引格式迁移到新实时索引上面，即实时索引的代码即支持全量索引也支持实时索引，减少运维和代码维护成本。我在查阅代码的同事，顺便与同事沟通了一些优化方法，进入全量索引代码里发现老的全量索引里已经有一些优化方法了，这些方法应该是市面上常见的方法，多数人也是应该知道的，这里记录下吧。主流优化操作主要包括以下四种：</p><h3 id="全AND操作"><a href="#全AND操作" class="headerlink" title="全AND操作"></a>全AND操作</h3><p>如果父子节点都是AND，则可以合并，如（A &amp; B）&amp;  (C &amp; D)，优化后为 A &amp; B &amp; C &amp; D ，能提早发现交为空，并退出，如下图所示。</p><a id="more"></a><img src="/img/2016/10/and.png" width = "67%" />优化后为：<img src="/img/2016/10/and_opt.png" width = "67%" /><h3 id="全OR操作"><a href="#全OR操作" class="headerlink" title="全OR操作"></a>全OR操作</h3><p>如果父子节点都是OR，则可以合并。如 （A | B）| (C | D)，优化后为 A | B | C | D，能提早发现为满并退出。如下图所示：<br><img src="/img/2016/10/OR.png" width = "67%" /><br>优化后为：<br><img src="/img/2016/10/or_opt.png" width = "67%" /></p><h3 id="将子节点的OR上提"><a href="#将子节点的OR上提" class="headerlink" title="将子节点的OR上提"></a>将子节点的OR上提</h3><p>如 (A | B) &amp; (C &amp;D)，优化后为 A |(B &amp; C | D)，查找次数减少。<br><img src="/img/2016/10/mix.png" width = "67%" /><br>优化后为：<br><img src="/img/2016/10/mix_opt.png" width = "67%" /></p><h3 id="按照Doc数量排序"><a href="#按照Doc数量排序" class="headerlink" title="按照Doc数量排序"></a>按照Doc数量排序</h3><p>如 A &amp; B &amp; C，可以按照Doc数量排序后查询，Sum(A) &lt; Sum(B) &lt; Sum(C)，则A &amp; B &amp; C可以尽早发现为空并退出。</p><p>当然查询搜索树优化不止这么些方法，比如之前就知道可以使用WAND提升长Query的检索速度，很久没看了，细节记不清了，就不说了。以后需要的时候再研究下。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;搜索引擎服务收到一个Query后，一般引擎这边是这么搞得，解析语法，生成后缀表达式，即查询搜索树（Search Tree）。搜索查询树负责求交、求并和过滤。所以这个地方也是性能关键点。所以在解析语法后，一般要做查询搜索树优化，减少求交，求并和过滤操作的次数，以此来提高搜索服务的QPS和查询Latency。&lt;/p&gt;
&lt;p&gt;因为同事在负责新实时索引的性能优化，我正好负责将全量索引格式迁移到新实时索引上面，即实时索引的代码即支持全量索引也支持实时索引，减少运维和代码维护成本。我在查阅代码的同事，顺便与同事沟通了一些优化方法，进入全量索引代码里发现老的全量索引里已经有一些优化方法了，这些方法应该是市面上常见的方法，多数人也是应该知道的，这里记录下吧。主流优化操作主要包括以下四种：&lt;/p&gt;
&lt;h3 id=&quot;全AND操作&quot;&gt;&lt;a href=&quot;#全AND操作&quot; class=&quot;headerlink&quot; title=&quot;全AND操作&quot;&gt;&lt;/a&gt;全AND操作&lt;/h3&gt;&lt;p&gt;如果父子节点都是AND，则可以合并，如（A &amp;amp; B）&amp;amp;  (C &amp;amp; D)，优化后为 A &amp;amp; B &amp;amp; C &amp;amp; D ，能提早发现交为空，并退出，如下图所示。&lt;/p&gt;</summary>
    
    
    
    <category term="搜索引擎" scheme="http://armsword.com/categories/%E6%90%9C%E7%B4%A2%E5%BC%95%E6%93%8E/"/>
    
    
  </entry>
  
  <entry>
    <title>dlog:高性能线程安全的C++日志库</title>
    <link href="http://armsword.com/2016/09/10/a-high-performace-and-thread-safe-cpp-log-lib/"/>
    <id>http://armsword.com/2016/09/10/a-high-performace-and-thread-safe-cpp-log-lib/</id>
    <published>2016-09-10T13:53:09.000Z</published>
    <updated>2020-11-03T07:37:50.322Z</updated>
    
    <content type="html"><![CDATA[<p>打算闲暇时光写点靠谱的东西比如名字服务、KV存储等基础服务类软件，所以周末就先从封装一些基础库开始。上周末写了个C++日志库，支持多线程，性能也还不错，今天就做了下测试和修改了下bug。这里说下该库的使用方法，以及实现思路以及一些性能调优方法。欢迎交流和指教。</p><h3 id="源码地址"><a href="#源码地址" class="headerlink" title="源码地址"></a>源码地址</h3><p><a href="https://github.com/armsword/dlog">https://github.com/armsword/dlog</a></p><h3 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h3><p>执行build.sh，在上层目录里会生成build文件，测试的可执行文件在release/bin目录下。</p><a id="more"></a><h3 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h3><p>将etc文件下的dlog.json扔到可执行文件的当前目录（当然只要能让可执行文件找到dlog.json即可），每个函数包含logger文件夹下的Log.h头文件，在主函数里调用DLOG_INIT初始化一次，之后在每个需要打印log的文件里调用DLOG_LOG即可。<br>如实例所示：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;dlog/logger/Log.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> dlog::logger;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    DLOG_INIT();</span><br><span class="line">    </span><br><span class="line">    DLOG_LOG(WARN, <span class="string">&quot;test the log level using log lib!&quot;</span>);</span><br><span class="line">    DLOG_LOG(DEBUG, <span class="string">&quot;Hello World, %d&quot;</span>,<span class="number">2016</span>);</span><br><span class="line">    DLOG_LOG(INFO, <span class="string">&quot;test Log C/C++ lib&quot;</span>);</span><br><span class="line">    DLOG_LOG(ERROR, <span class="string">&quot;Hello everyone, this is my blog: http://armsword.com/; Welcome to visit it,Thank you!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="输出样例"><a href="#输出样例" class="headerlink" title="输出样例"></a>输出样例</h3><p>目录名：dlog.log.20160911.0<br>日志内容：</p><figure class="highlight crystal"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[<span class="number">2016</span>-<span class="number">0</span>9-<span class="number">11</span> <span class="number">15</span>:<span class="number">0</span>5:<span class="number">30.510</span>] [WARN] [<span class="number">4246443808</span>] [<span class="regexp">/home/</span>Github/dlog/example/LogTest.<span class="symbol">cpp:</span><span class="number">8</span>] [main] test the log level using log <span class="class"><span class="keyword">lib</span>!</span></span><br><span class="line">[<span class="number">2016</span>-<span class="number">0</span>9-<span class="number">11</span> <span class="number">15</span>:<span class="number">0</span>5:<span class="number">30.510</span>] [DEBUG] [<span class="number">4246443808</span>] [<span class="regexp">/home/</span>Github/dlog/example/LogTest.<span class="symbol">cpp:</span><span class="number">9</span>] [main] Hello World, <span class="number">2016</span></span><br><span class="line">[<span class="number">2016</span>-<span class="number">0</span>9-<span class="number">11</span> <span class="number">15</span>:<span class="number">0</span>5:<span class="number">30.510</span>] [INFO] [<span class="number">4246443808</span>] [<span class="regexp">/home/</span>Github/dlog/example/LogTest.<span class="symbol">cpp:</span><span class="number">10</span>] [main] test Log C/C++ <span class="class"><span class="keyword">lib</span></span></span><br><span class="line">[<span class="number">2016</span>-<span class="number">0</span>9-<span class="number">11</span> <span class="number">15</span>:<span class="number">0</span>5:<span class="number">30.510</span>] [ERROR] [<span class="number">4246443808</span>] [<span class="regexp">/home/</span>Github/dlog/example/LogTest.<span class="symbol">cpp:</span><span class="number">11</span>] [main] Hello everyone, this is my <span class="symbol">blog:</span> <span class="symbol">http:</span>/<span class="regexp">/armsword.com/</span>; Welcome to visit it,Thank you!</span><br></pre></td></tr></table></figure><h3 id="该日志库功能"><a href="#该日志库功能" class="headerlink" title="该日志库功能"></a>该日志库功能</h3><ul><li>包含四种日志级别，分别为WARN、DEBUG、INFO、ERROR，日志级别大小依次递增</li><li>可配置输出日志路径</li><li>可配置输出日志前缀</li><li>可定义输出的日志级别，默认DEBUG</li><li>可定义日志文件切分大小</li><li>支持多线程程序</li><li>可定义日志往磁盘刷新的方式</li><li>支持每天切换新的日志文件</li><li>支持log文件被删除时，从新建立日志文件</li></ul><h3 id="dlog-json配置"><a href="#dlog-json配置" class="headerlink" title="dlog.json配置"></a>dlog.json配置</h3><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">&quot;log_path&quot;</span>: <span class="string">&quot;./log&quot;</span>,    <span class="comment">// 日志路径</span></span><br><span class="line">    <span class="attr">&quot;log_prefix&quot;</span>: <span class="string">&quot;dlog&quot;</span>,   <span class="comment">// 日志前缀</span></span><br><span class="line">    <span class="attr">&quot;log_level&quot;</span>: <span class="string">&quot;DEBUG&quot;</span>,   <span class="comment">// 输出日志级别，DEBUG表示大于等于DEBUG级别的日志都打印</span></span><br><span class="line">    <span class="attr">&quot;max_file_size&quot;</span>: <span class="number">200</span>,   <span class="comment">// 日志切分大小，单位m</span></span><br><span class="line">    <span class="attr">&quot;async_flush&quot;</span>: <span class="literal">true</span>     <span class="comment">// 日志往磁盘刷新方式，true表示异步，false表示同步，建议选择true</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="性能"><a href="#性能" class="headerlink" title="性能"></a>性能</h3><p>在测试机上测试了下机器的硬盘真实io写速度</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">time dd <span class="attribute">if</span>=/dev/zero <span class="attribute">of</span>=test.dbf <span class="attribute">bs</span>=8k <span class="attribute">count</span>=300000 <span class="attribute">oflag</span>=direct</span><br><span class="line">记录了300000+0 的读入</span><br><span class="line">记录了300000+0 的写出</span><br><span class="line">2457600000字节(2.5 GB)已复制，38.0385 秒，64.6 MB/秒</span><br><span class="line"></span><br><span class="line">注：<span class="attribute">oflag</span>=direct 表示使用DirectIO方式，不使用文件系统的buffer等</span><br></pre></td></tr></table></figure><p>而我用dlog写入3.5G（1000W条数据），用时大约51、52s，计算下来，写速度大约69M/s，与上面测试的磁盘io数据64.6 MB/s比起来，性能还算不错（因为cache问题，要比裸写磁盘性能要好），当然由于时间问题，我测试还不够充分，以后有机会继续优化下再测试看下。</p><h3 id="一些性能调优技巧"><a href="#一些性能调优技巧" class="headerlink" title="一些性能调优技巧"></a>一些性能调优技巧</h3><ul><li>为了避免锁竞争，使用了一种更为高效的线程局部存储方法，就是使用关键字__thread来定义变量，__thread是GCC内置的线程局部存储设施(Thread-Local Storage)，凡是带有__thread的变量，每个线程都拥有该变量的一份拷贝，且互不干扰。</li><li>使用likely，unlikely来提高CPU分支预测正确率来提高性能。我们定义likely、unlikely如下：<figure class="highlight lisp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#define likely(<span class="name">x</span>) __builtin_expect((<span class="name">x</span>), <span class="number">1</span>)</span><br><span class="line">#define unlikely(<span class="name">x</span>) __builtin_expect((<span class="name">x</span>), <span class="number">0</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>其中__builtin_expect是gcc提供的函数。顾名思义，likely表示这件事很大概率会发生 ：）</li><li>使用loop线程来判断文件是否需要切分（打开新的fd），并且open、close这种费时操作，只在loop线程里完成，不阻塞log输出线程（一些小技巧保证线程安全）。</li></ul><p>本日志库主要是为了满足个人需求，当然即使公司业务，没特殊需求的话也足够用了，所以并没有写的像log4j或者log4cxx那么复杂，但即便如此该日志库还有一些提高空间，等我有空再继续优化下，欢迎交流和指教，谢谢。</p><h3 id="致谢："><a href="#致谢：" class="headerlink" title="致谢："></a>致谢：</h3><p>因为很久没用Cmake了（之前在阿里用ascons），基本上都忘光了。并且阿里的一些经验使我比较在意代码目录的组织方式，搜索后发现一篇文章讲Cmake非常不错，作者很用心，非常感谢。</p><p><a href="http://hahack.com/codes/cmake/">Cmake入门实战</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;打算闲暇时光写点靠谱的东西比如名字服务、KV存储等基础服务类软件，所以周末就先从封装一些基础库开始。上周末写了个C++日志库，支持多线程，性能也还不错，今天就做了下测试和修改了下bug。这里说下该库的使用方法，以及实现思路以及一些性能调优方法。欢迎交流和指教。&lt;/p&gt;
&lt;h3 id=&quot;源码地址&quot;&gt;&lt;a href=&quot;#源码地址&quot; class=&quot;headerlink&quot; title=&quot;源码地址&quot;&gt;&lt;/a&gt;源码地址&lt;/h3&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/armsword/dlog&quot;&gt;https://github.com/armsword/dlog&lt;/a&gt;&lt;/p&gt;
&lt;h3 id=&quot;编译&quot;&gt;&lt;a href=&quot;#编译&quot; class=&quot;headerlink&quot; title=&quot;编译&quot;&gt;&lt;/a&gt;编译&lt;/h3&gt;&lt;p&gt;执行build.sh，在上层目录里会生成build文件，测试的可执行文件在release/bin目录下。&lt;/p&gt;</summary>
    
    
    
    <category term="Linux" scheme="http://armsword.com/categories/Linux/"/>
    
    
  </entry>
  
  <entry>
    <title>Earlybird:Twitter实时搜索引擎</title>
    <link href="http://armsword.com/2016/08/17/earlybird/"/>
    <id>http://armsword.com/2016/08/17/earlybird/</id>
    <published>2016-08-16T16:49:46.000Z</published>
    <updated>2020-11-03T07:37:50.320Z</updated>
    
    <content type="html"><![CDATA[<p>简单说下，2011年左右的Paper，Paper里说Twitter的实时索引和检索系统叫做Earlybird，Paper中主要讲了2件事，第一个就是支持Twitter的实时索引的倒排索引结构是怎么样的，第二个就是利用Java并发模型，处理并发读写。</p><h4 id="该引擎功能："><a href="#该引擎功能：" class="headerlink" title="该引擎功能："></a>该引擎功能：</h4><ul><li>低延时、高吞吐能力；</li><li>能处理突发峰值（Weibo的特性）；</li><li>突出时效性，时间越近，排名应该越靠前；</li><li>该实时引擎支持AND、OR、NOT以及短语查询，实时性大约10S，查询latency 50ms；</li></ul><h4 id="该实时引擎，基于Lucene，使用Java开发，理由是："><a href="#该实时引擎，基于Lucene，使用Java开发，理由是：" class="headerlink" title="该实时引擎，基于Lucene，使用Java开发，理由是："></a>该实时引擎，基于Lucene，使用Java开发，理由是：</h4><ul><li>利用已存在的Lucene代码，并用来做全量索引；</li><li>适用于Twitter以JVM为中心的开发环境；</li><li>利用Java和JVM提供容易理解的并发模型；</li></ul><p>其实上面几条理由与阿里很多开发项目类似，但是阿里的搜索引擎是C++编写的，质量也是非常不错的，叫问天(HA3)。</p><a id="more"></a><h2 id="索引构建过程："><a href="#索引构建过程：" class="headerlink" title="索引构建过程："></a>索引构建过程：</h2><p>Earlybird处理三种信号：</p><ul><li>静态信号，初次建立索引时被添加；</li><li>共鸣信号，动态更新的，比如tweet转发次数；</li><li>搜索用户信息，用来个性化排序；</li></ul><p><img src="/img/2016/08/earlybird.png"></p><ol><li>用户发布tweet，之后被发送到一个队列，叫做Ingestion Pipeline里。这些tweet会被分词，并被加上一些meta信息，比如语言等；</li><li>为了处理大容量，tweet按照哈希算法，分发到各个Earlybird服务上，之后将tweet实时地建立索引；</li><li>如上图所示，有一个Updater服务，它推送共鸣信号到Earlybird服务，用于动态地更新索引，比如tweet转发信息等。</li></ol><h2 id="查询过程如下："><a href="#查询过程如下：" class="headerlink" title="查询过程如下："></a>查询过程如下：</h2><ol><li>当查询请求过来，先到达前端“Blender”，Blender解析请求，并将搜索用户的本地社交图谱信息也下发到Earlybird服务；</li><li>Earlybird服务执行相关性计算并排序，并将排序好的tweet（排序分数高的，最近发生的tweets）返回给Blender；</li><li>Blender合并各个Earlybird返回的列表，并重排序后返回给用户；</li></ol><h2 id="Term字典"><a href="#Term字典" class="headerlink" title="Term字典"></a>Term字典</h2><p>Twitter实时索引现在不支持一些高级查询，比如通配符。所以term词典使用hash表实现。没有选择Java默认的HashMap，因为其不是GC友好的，Earlybird实现了个开链法的哈希表（个人猜测是不是与Redis的渐进式哈希相似呢？）每个Term被分配了一个唯一且单调递增的id作为key，value包含以下两部分信息：</p><ul><li>Term对应的倒排索引数据长度；</li><li>指向倒排索引数据末尾的指针；</li></ul><h2 id="动态（active）索引"><a href="#动态（active）索引" class="headerlink" title="动态（active）索引"></a>动态（active）索引</h2><p>Paper里首先介绍了现在存在的索引组织方式不适合实时索引，如将新发生的tweet放到postings链最后面，但是读取时候需要倒序读取，这个是不支持现在流行的压缩算法的，而如果将新的数据放到倒排索引前面，这带来的问题就是内存分配的问题。Earlybird使用了一个更简单的方式，分离索引读和写的方式。</p><p>每个实例维护了多个索引分段（目前是12个instance），每个分段保存相对较少量的tweet（目前是2^23~840万 tweets）。新增加的tweets被分到同一个segment（分段）中，满了之后再放下一个，这样，在任何时候只有一个分段被更改，其他都是只读状态，当一个segment满了，停止接收新的tweets时，会做只读优化。</p><p>postings列表在优化之前直接使用整数数组，按照docid升序存储，并且不做压缩，因此查找可以直接二分查找。postings增长时分配空间以pool为单位，分配空间按指数预留空间，分为4个，大小分别为应2^1、2^4、2^7、2^11 。如果一个pool满了，另一个pool将会分配。每个pool里面有若干个slice，每个slice保存一个term的postings列表，每个postings列表里面存储着一个term的postings信息，每个postings是一个32-bit的整数，24-bit用作文档id；8-bit用作位置id，保存term在tweet中出现的位置。因为tweet有140字符限制，所以8-bit足够用。在建立索引时，先尝试填满2^1pool中的slice，如果填满，就转到2^4的pool的slice，以此类推。term 字典中保存term对应的最后一个pool的中postings list尾部指针。如下图所示：<br><img src="/img/2016/08/slice.png"></p><h2 id="索引优化"><a href="#索引优化" class="headerlink" title="索引优化"></a>索引优化</h2><p>一旦动态索引停止接收新的tweets时，即此segment的tweets超过840W时，后台执行只读索引优化。优化过程中，根据老的索引数据，新的索引数据会被创建，原始索引数据不做变化，一旦完成构建，原始索引数据将被新的索引替换。postings链分为两种，长的和短的，以长度1000为界限。短的，即postings链长度小于1000，postings保持原样（24-bit文档id加上8-bit的位置信息），但是postings会按照时间逆序排列。对于长的postings链，采用了基于block的压缩算法，PForDelta和Simple9。</p><h2 id="并发管理"><a href="#并发管理" class="headerlink" title="并发管理"></a>并发管理</h2><p>对于实时索引来说，一个重要的需求就是在多线程环境下处理好并发读和写操作。当然这个只用于动态索引当中，静态索引（优化后的）只有读操作。因为不懂Java，这块简单说下吧，Paper里说Java和JVM提供了一个非常容易理解的并发内存模型。11个只读段并发读不需要锁，唯一的可读可写段使用volatile关键字实现高效同步，然后也使用了 jvm memory barrier。懂Java得同学，通过以下新tweet添加处理过程，可能就明白一二了。</p><p>tweet里的每个term，首先去查询对应的词典入口，在字典中，term被映射到term id。它包括2个信息，Term对应的倒排索引数据长度和指向倒排索引数据末尾的指针。通过末尾的指针，信息被添加到新的postings链中。如果没有足够的空间插入新的postings信息，新的slice被分配。如果term是第一次遇到，它被添加到词典并且分配下一个term id。postings被添加到slice时，term并发的增加计数，指向postings链尾巴的指针也被并发更新。当tweet里的所有term都被处理后，增加maxDoc变量，此变量表示目前遇到的最大文档id。</p><p>简单就写这么多吧，没看明白的同学可以自己仔细阅读下twitter的这篇实时索引Paper吧，地址：</p><p><a href="http://www.umiacs.umd.edu/~jimmylin/publications/Busch_etal_ICDE2012.pdf">http://www.umiacs.umd.edu/~jimmylin/publications/Busch_etal_ICDE2012.pdf</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;简单说下，2011年左右的Paper，Paper里说Twitter的实时索引和检索系统叫做Earlybird，Paper中主要讲了2件事，第一个就是支持Twitter的实时索引的倒排索引结构是怎么样的，第二个就是利用Java并发模型，处理并发读写。&lt;/p&gt;
&lt;h4 id=&quot;该引擎功能：&quot;&gt;&lt;a href=&quot;#该引擎功能：&quot; class=&quot;headerlink&quot; title=&quot;该引擎功能：&quot;&gt;&lt;/a&gt;该引擎功能：&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;低延时、高吞吐能力；&lt;/li&gt;
&lt;li&gt;能处理突发峰值（Weibo的特性）；&lt;/li&gt;
&lt;li&gt;突出时效性，时间越近，排名应该越靠前；&lt;/li&gt;
&lt;li&gt;该实时引擎支持AND、OR、NOT以及短语查询，实时性大约10S，查询latency 50ms；&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&quot;该实时引擎，基于Lucene，使用Java开发，理由是：&quot;&gt;&lt;a href=&quot;#该实时引擎，基于Lucene，使用Java开发，理由是：&quot; class=&quot;headerlink&quot; title=&quot;该实时引擎，基于Lucene，使用Java开发，理由是：&quot;&gt;&lt;/a&gt;该实时引擎，基于Lucene，使用Java开发，理由是：&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;利用已存在的Lucene代码，并用来做全量索引；&lt;/li&gt;
&lt;li&gt;适用于Twitter以JVM为中心的开发环境；&lt;/li&gt;
&lt;li&gt;利用Java和JVM提供容易理解的并发模型；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;其实上面几条理由与阿里很多开发项目类似，但是阿里的搜索引擎是C++编写的，质量也是非常不错的，叫问天(HA3)。&lt;/p&gt;</summary>
    
    
    
    <category term="搜索引擎" scheme="http://armsword.com/categories/%E6%90%9C%E7%B4%A2%E5%BC%95%E6%93%8E/"/>
    
    
  </entry>
  
  <entry>
    <title>记一次线上Bug的查找过程和思路</title>
    <link href="http://armsword.com/2016/08/10/find-socket-bug-in-proxy/"/>
    <id>http://armsword.com/2016/08/10/find-socket-bug-in-proxy/</id>
    <published>2016-08-10T13:33:40.000Z</published>
    <updated>2020-11-03T07:37:50.321Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>简单说下问题情况，Proxy即作为客户端又有服务端功能，其接受QS（Query Server）的请求，之后向BS（索引服务）发送请求，然后根据BS的返回结果Merge后返回给QS。不能泄露太多东西，所以本文主要是整理一些知识点和问题查找思路。</p><h2 id="问题和现象："><a href="#问题和现象：" class="headerlink" title="问题和现象："></a>问题和现象：</h2><ul><li>Proxy机器上出现大量的CLOSE_WAIT。</li><li>Proxy失去服务能力，内存使用不为0，CPU使用率为0。</li><li>机器报警，TcpListenOverFlows。</li></ul><figure class="highlight properties"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">命令查看：</span></span><br><span class="line"><span class="attr">netstat</span> <span class="string">-n | awk &#x27;/^tcp/ &#123;++S[$NF]&#125; END &#123;for(a in S) print a, S[a]&#125;&#x27;</span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="attr">结果：</span></span><br><span class="line"><span class="attr">TIME_WAIT</span> <span class="string">31927</span></span><br><span class="line"><span class="attr">CLOSE_WAIT</span> <span class="string">1570</span></span><br><span class="line"><span class="attr">ESTABLISHED</span> <span class="string">40</span></span><br><span class="line"> </span><br><span class="line"><span class="attr">TIME_WAIT：表示主动关闭，通过优化系统内核参数可容易解决。</span></span><br><span class="line"><span class="attr">CLOSE_WAIT：表示被动关闭，需要从程序本身出发。</span></span><br><span class="line"><span class="attr">ESTABLISHED：表示正在通信</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="attr">注：以上数据非真实线上情况，只为举例</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="一些知识点"><a href="#一些知识点" class="headerlink" title="一些知识点"></a>一些知识点</h2><h3 id="TCP握手图"><a href="#TCP握手图" class="headerlink" title="TCP握手图"></a>TCP握手图</h3></div><div  align="center">    <img src="/img/2016/08/tcp.jpg" width = "550" height = "660" alt="" align=center /></div><h3 id="TIME-WAIT"><a href="#TIME-WAIT" class="headerlink" title="TIME_WAIT:"></a>TIME_WAIT:</h3><p>Linux系统下，TCP/IP连接断开后，会以TIME_WAIT状态保留一定的时间（2MSL：max segment lifetime），默认为4分钟，然后才会关闭回收资源。当并发请求过多的时候，就会产生大量的 TIME_WAIT状态的连接，无法及时断开的话，会占用大量的端口资源和服务器资源。<br>状态保持2MSL的原因：<br>1.防止上一次连接中的包，迷路后重新出现，影响新连接（经过2MSL，上一次连接中所有的重复包都会消失）<br>2.可靠的关闭TCP连接。在主动关闭方发送的最后一个 ack(fin) ，有可能丢失，这时被动方会重新发fin, 如果这时主动方处于 CLOSED 状态 ，就会响应 rst 而不是 ack。所以主动方要处于 TIME_WAIT 状态，而不能是 CLOSED 。另外这么设计TIME_WAIT 会定时的回收资源，并不会占用很大资源的，除非短时间内接受大量请求或者受到攻击。<br>一些解决方法：<br>通过修改/etc/sysctl.conf文件，服务器能够快速回收和重用那些TIME_WAIT的资源 。 </p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">#表示开启SYN Cookies。当出现SYN等待队列溢出时，启用cookies来处理，可防范少量SYN攻击，默认为<span class="number">0</span>，表示关闭</span><br><span class="line">net.ipv4.tcp_syncookies = <span class="number">1</span>  </span><br><span class="line"></span><br><span class="line">#表示开启重用。允许将TIME-WAIT sockets重新用于新的TCP连接，默认为<span class="number">0</span>，表示关闭  </span><br><span class="line">net.ipv4.tcp_tw_reuse = <span class="number">1</span>  </span><br><span class="line"></span><br><span class="line">#表示开启TCP连接中TIME-WAIT sockets的快速回收，默认为<span class="number">0</span>，表示关闭  </span><br><span class="line">net.ipv4.tcp_tw_recycle = <span class="number">1</span></span><br><span class="line"></span><br><span class="line">#表示如果套接字由本端要求关闭，这个参数决定了它保持在FIN-WAIT<span class="number">-2</span>状态的时间  </span><br><span class="line">net.ipv4.tcp_fin_timeout=<span class="number">30</span></span><br><span class="line"></span><br><span class="line">生效命令：</span><br><span class="line">/sbin/sysctl -p</span><br></pre></td></tr></table></figure><h3 id="CLOSE-WAIT："><a href="#CLOSE-WAIT：" class="headerlink" title="CLOSE_WAIT："></a>CLOSE_WAIT：</h3><p>CLOSE_WAIT表示被动关闭，出现这种问题，基本是就是客户端连接异常或者自己没有迅速回收资源。<br>什么情况下，连接处于CLOSE_WAIT状态呢？</p><ul><li>在被动关闭连接情况下，在已经接收到FIN，但是还没有发送自己的FIN的时刻，连接处于CLOSE_WAIT状态。通常来讲，CLOSE_WAIT状态的持续时间应该很短，正如SYN_RCVD状态。但是在一些特殊情况下，就会出现连接长时间处于CLOSE_WAIT状态的情况。</li><li>出现大量close_wait的现象，主要原因是某种情况下对方关闭了socket链接，但是我方忙与读或者写，没有关闭连接（CLOSE_WAIT应该是默认2小时才会关闭）。代码需要判断socket，一旦读到0，断开连接，read返回负，检查一下errno，如果不是AGAIN，就断开连接。</li><li>scoket阻塞，无超时或非阻塞处理。</li></ul><h3 id="EPOLL"><a href="#EPOLL" class="headerlink" title="EPOLL:"></a>EPOLL:</h3><ul><li>EPOLLIN：连接，对端发送普通数据，对端socket正常关闭。</li><li>EPOLLPRI：带外数据，文件描述符有紧急的数据可读。</li><li>EPOLLOUT：数据可写。</li><li>对端正常关闭（close()，shell下kill或ctr+c），触发EPOLLIN和EPOLLRDHUP，但是不触发EPOLLERR和EPOLLHUP。//重要</li><li>对端异常断开连接（如网线），不会触发任何事件。判断方式是向已经断开的socket写或者读，会发生EPOLLERR错误。</li></ul><h3 id="socket接口的几个知识点："><a href="#socket接口的几个知识点：" class="headerlink" title="socket接口的几个知识点："></a>socket接口的几个知识点：</h3><ul><li>read总是在接收缓冲区有数据时立即返回，而不是等到给定的read buffer填满时返回。<br>只有当receive buffer为空时，blocking模式才会等待，而nonblock模式下会立即返回-1（errno = EAGAIN或EWOULDBLOCK）</li><li>blocking的write只有在缓冲区足以放下整个buffer时才返回（与blocking read并不相同）<br>nonblock write则是返回能够放下的字节数，之后调用则返回-1（errno = EAGAIN或EWOULDBLOCK）<br>对于blocking的write有个特例：当write正阻塞等待时对面关闭了socket，则write则会立即将剩余缓冲区填满并返回所写的字节数，再次调用则write失败（connection reset by peer）</li></ul><h3 id="Bug查找"><a href="#Bug查找" class="headerlink" title="Bug查找"></a>Bug查找</h3><p>通过以上的知识点，我们就可以很好的定位问题原因了，因为Proxy即作为客户端，又有服务端功能，其转发QS的结果到BS之后，再将BS的结果merge后回复给QS。仔细排查代码，发现2处引起Proxy大量出现CLOSE_TIMEWAIT。<br>&lt;1&gt;、Proxy将BS的结果merge后，发送QS时，socket是阻塞的，也未做超时处理。<br>&lt;2&gt;、QS超时（3S），会主动关闭socket，而Proxy这层没有关闭fd，可能原作者认为会触发EPOLLERR和EPOLLHUP（这里面做了处理）。</p><h2 id="参考链接："><a href="#参考链接：" class="headerlink" title="参考链接："></a>参考链接：</h2><p>1、<a href="http://www.cnblogs.com/promise6522/archive/2012/03/03/2377935.html">http://www.cnblogs.com/promise6522/archive/2012/03/03/2377935.html</a><br>2、<a href="http://originlee.com/2015/04/22/nonblocking-tcp-socket/">http://originlee.com/2015/04/22/nonblocking-tcp-socket/</a> </p>]]></content>
    
    
    <summary type="html">&lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;简单说下问题情况，Proxy即作为客户端又有服务端功能，其接受QS（Query Server）的请求，之后向BS（索引服务）发送请求，然后根据BS的返回结果Merge后返回给QS。不能泄露太多东西，所以本文主要是整理一些知识点和问题查找思路。&lt;/p&gt;
&lt;h2 id=&quot;问题和现象：&quot;&gt;&lt;a href=&quot;#问题和现象：&quot; class=&quot;headerlink&quot; title=&quot;问题和现象：&quot;&gt;&lt;/a&gt;问题和现象：&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;Proxy机器上出现大量的CLOSE_WAIT。&lt;/li&gt;
&lt;li&gt;Proxy失去服务能力，内存使用不为0，CPU使用率为0。&lt;/li&gt;
&lt;li&gt;机器报警，TcpListenOverFlows。&lt;/li&gt;
&lt;/ul&gt;
&lt;figure class=&quot;highlight properties&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;10&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;11&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;12&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;13&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;14&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;15&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;16&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;命令查看：&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;netstat&lt;/span&gt; &lt;span class=&quot;string&quot;&gt;-n | awk &amp;#x27;/^tcp/ &amp;#123;++S[$NF]&amp;#125; END &amp;#123;for(a in S) print a, S[a]&amp;#125;&amp;#x27;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt; &lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt; &lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;结果：&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;TIME_WAIT&lt;/span&gt; &lt;span class=&quot;string&quot;&gt;31927&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;CLOSE_WAIT&lt;/span&gt; &lt;span class=&quot;string&quot;&gt;1570&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;ESTABLISHED&lt;/span&gt; &lt;span class=&quot;string&quot;&gt;40&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt; &lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;TIME_WAIT：表示主动关闭，通过优化系统内核参数可容易解决。&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;CLOSE_WAIT：表示被动关闭，需要从程序本身出发。&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;ESTABLISHED：表示正在通信&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;attr&quot;&gt;注：以上数据非真实线上情况，只为举例&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;</summary>
    
    
    
    <category term="Linux" scheme="http://armsword.com/categories/Linux/"/>
    
    
  </entry>
  
  <entry>
    <title>浅谈Linux ulimit以及max memory locked</title>
    <link href="http://armsword.com/2016/07/09/linux-max-locked-memory-ulimit/"/>
    <id>http://armsword.com/2016/07/09/linux-max-locked-memory-ulimit/</id>
    <published>2016-07-09T13:39:42.000Z</published>
    <updated>2020-11-03T07:37:50.317Z</updated>
    
    <content type="html"><![CDATA[<p>刚入职，我在索引这个组，需要熟悉下之前的搜索索引模块的代码，以便后续开发。我在新申请的测试机器上编译代码部署索引这个模块就报错了。定位出现问题的地方，代码大致流程是这样子的：</p><figure class="highlight gcode"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="comment">(conf-&gt;use_memlock_for_mmap)</span> &#123;</span><br><span class="line">    <span class="comment">// add MAP_LOCKED to lock the buffer</span></span><br><span class="line"></span><br><span class="line">    attr_hash = <span class="comment">(AttrIndexInfo*)</span>mmap<span class="comment">(NULL, fsize, PROT_WRITE | PROT_READ, MAP_SHARED | MAP_LOCKED, fd, 0)</span>;</span><br><span class="line"></span><br><span class="line">&#125; else &#123;</span><br><span class="line"></span><br><span class="line">    attr_hash = <span class="comment">(AttrIndexInfo*)</span>mmap<span class="comment">(NULL, fsize, PROT_WRITE | PROT_READ, MAP_SHARED, fd, 0)</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而参数里配置了use_memlock_for_mmap 选项，于是代码就走到第一个mmap逻辑处。</p><p>使用ulimit -a查看下系统属性：</p><figure class="highlight mel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">[armsword@search-querybs1 ~]$ ulimit -a</span><br><span class="line">core <span class="keyword">file</span> <span class="keyword">size</span>          (blocks, -c) <span class="number">4194304</span></span><br><span class="line">data seg <span class="keyword">size</span>           (kbytes, -d) unlimited</span><br><span class="line">scheduling priority             (-e) <span class="number">0</span></span><br><span class="line"><span class="keyword">file</span> <span class="keyword">size</span>               (blocks, -f) unlimited</span><br><span class="line">pending signals                 (-i) <span class="number">256726</span></span><br><span class="line"><span class="keyword">max</span> locked <span class="keyword">memory</span>       (kbytes, -l) <span class="number">64</span></span><br><span class="line"><span class="keyword">max</span> <span class="keyword">memory</span> <span class="keyword">size</span>         (kbytes, -m) unlimited</span><br><span class="line">open files                      (-n) <span class="number">1000000</span></span><br><span class="line">pipe <span class="keyword">size</span>            (<span class="number">512</span> bytes, -p) <span class="number">8</span></span><br><span class="line">POSIX message queues     (bytes, -q) <span class="number">819200</span></span><br><span class="line">real-time priority              (-r) <span class="number">0</span></span><br><span class="line">stack <span class="keyword">size</span>              (kbytes, -s) <span class="number">10240</span></span><br><span class="line">cpu time               (seconds, -t) unlimited</span><br><span class="line"><span class="keyword">max</span> user processes              (-u) <span class="number">102400</span></span><br><span class="line">virtual <span class="keyword">memory</span>          (kbytes, -v) unlimited      （注：相当于limits.conf中的as和cgroup中的<span class="keyword">memory</span>.limit_in_bytes.）</span><br><span class="line"><span class="keyword">file</span> locks                      (-x) unlimited</span><br></pre></td></tr></table></figure><a id="more"></a><p>我们知道索引程序在启动时候，需要加载倒排/正排等索引原文件，这些文件是比较大的，几十G或者过百G也是可能的，但是系统下的max locked memory对应值是64k，而mmap选项也设置了MAP_LOCKED选项，此选项的意思就是lock一段地址范围内已map的内存，相应的数据在unlock之前一直存在于物理内存中，不会被回收。对于应用程序来说，可以将内存中一些对程序性能影响较大的数据lock起来，避免非预期的页面回收或者换入/换出引起性能波动。所以将max locked memory设置为unlimited就解决问题。</p><p>linux系统提供了以下几个系统调用用于内存的lock和unlock。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mlock/munlock：<span class="keyword">lock</span>/<span class="keyword">unlock</span>一段地址范围内已<span class="keyword">map</span>的内存</span><br><span class="line"></span><br><span class="line">mlockall/munlockall：<span class="keyword">lock</span>进程虚拟地址空间内已<span class="keyword">map</span>的内存，还可以选择对于此后新<span class="keyword">map</span>的空间是否自动<span class="keyword">lock</span></span><br><span class="line"></span><br><span class="line">mmap使用MAP_LOCKED选项时表示在mmap的同时，对相应地址范围进行mlock</span><br></pre></td></tr></table></figure><p>一般情况下，我们使用ulimit多用于提高性能，最常用的地方就是设置打开文件描述符的数量，web服务器等需要大量的文件句柄，一旦开太小，比如默认1024，在句柄使用完毕的时候，系统就频繁出现emfile错误，这时候系统很容易陷入不可用。但是如果设定太大了，又会有这样的副作用。很多服务器程序是事件派遣的，比如说用epoll，程序在启动的时候通常会根据最大的文件句柄数来预留内部的slot，一个slot貌似要占用几K的资源，如果你设定文件句柄数目太大，就可能无端的浪费了几百M内存。所以要设置一个合适的值有利于提高程序性能。</p><p>ulimit 用于限制 shell 启动进程所占用的资源，支持以下各种类型的限制，具体楼上已列出，注意下H/S选项的意义，</p><p>-H 设置硬资源限制，硬资源限制用于控制软限制。限定一旦设置只有root用户可以增加硬限制，普通用户只能减少自己的硬限制大小。<br>-S 设置弹性资源限制，弹性限制用于限制具体的用户或者进程。设置后普通用户可以增加，但是不能超过硬限制大小。<br>如果不指定-S或者-H，那么弹性资源限制和硬限制将同时设置。</p><p>作为临时限制，ulimit 可以作用于通过使用其命令登录的 shell 会话，在会话终止时便结束限制，并不影响于其他 shell 会话。而对于长期的固定限制，修改 /etc/security/limits.conf 文件即可。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;刚入职，我在索引这个组，需要熟悉下之前的搜索索引模块的代码，以便后续开发。我在新申请的测试机器上编译代码部署索引这个模块就报错了。定位出现问题的地方，代码大致流程是这样子的：&lt;/p&gt;
&lt;figure class=&quot;highlight gcode&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;10&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;comment&quot;&gt;(conf-&amp;gt;use_memlock_for_mmap)&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;comment&quot;&gt;// add MAP_LOCKED to lock the buffer&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    attr_hash = &lt;span class=&quot;comment&quot;&gt;(AttrIndexInfo*)&lt;/span&gt;mmap&lt;span class=&quot;comment&quot;&gt;(NULL, fsize, PROT_WRITE | PROT_READ, MAP_SHARED | MAP_LOCKED, fd, 0)&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125; else &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    attr_hash = &lt;span class=&quot;comment&quot;&gt;(AttrIndexInfo*)&lt;/span&gt;mmap&lt;span class=&quot;comment&quot;&gt;(NULL, fsize, PROT_WRITE | PROT_READ, MAP_SHARED, fd, 0)&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;而参数里配置了use_memlock_for_mmap 选项，于是代码就走到第一个mmap逻辑处。&lt;/p&gt;
&lt;p&gt;使用ulimit -a查看下系统属性：&lt;/p&gt;
&lt;figure class=&quot;highlight mel&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;10&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;11&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;12&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;13&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;14&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;15&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;16&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;17&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;[armsword@search-querybs1 ~]$ ulimit -a&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;core &lt;span class=&quot;keyword&quot;&gt;file&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;size&lt;/span&gt;          (blocks, -c) &lt;span class=&quot;number&quot;&gt;4194304&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;data seg &lt;span class=&quot;keyword&quot;&gt;size&lt;/span&gt;           (kbytes, -d) unlimited&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;scheduling priority             (-e) &lt;span class=&quot;number&quot;&gt;0&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;file&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;size&lt;/span&gt;               (blocks, -f) unlimited&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;pending signals                 (-i) &lt;span class=&quot;number&quot;&gt;256726&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;max&lt;/span&gt; locked &lt;span class=&quot;keyword&quot;&gt;memory&lt;/span&gt;       (kbytes, -l) &lt;span class=&quot;number&quot;&gt;64&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;max&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;memory&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;size&lt;/span&gt;         (kbytes, -m) unlimited&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;open files                      (-n) &lt;span class=&quot;number&quot;&gt;1000000&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;pipe &lt;span class=&quot;keyword&quot;&gt;size&lt;/span&gt;            (&lt;span class=&quot;number&quot;&gt;512&lt;/span&gt; bytes, -p) &lt;span class=&quot;number&quot;&gt;8&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;POSIX message queues     (bytes, -q) &lt;span class=&quot;number&quot;&gt;819200&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;real-time priority              (-r) &lt;span class=&quot;number&quot;&gt;0&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;stack &lt;span class=&quot;keyword&quot;&gt;size&lt;/span&gt;              (kbytes, -s) &lt;span class=&quot;number&quot;&gt;10240&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;cpu time               (seconds, -t) unlimited&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;max&lt;/span&gt; user processes              (-u) &lt;span class=&quot;number&quot;&gt;102400&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;virtual &lt;span class=&quot;keyword&quot;&gt;memory&lt;/span&gt;          (kbytes, -v) unlimited      （注：相当于limits.conf中的as和cgroup中的&lt;span class=&quot;keyword&quot;&gt;memory&lt;/span&gt;.limit_in_bytes.）&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;file&lt;/span&gt; locks                      (-x) unlimited&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;</summary>
    
    
    
    <category term="Linux" scheme="http://armsword.com/categories/Linux/"/>
    
    
  </entry>
  
</feed>
